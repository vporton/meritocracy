#!/usr/bin/env node
// Synthetic-only low-cycle proof for the treasury private payment-operation
// adapter. It creates neither a treasury API nor any custody/chain material.
const path = require("node:path"), fs = require("node:fs"), crypto = require("node:crypto");
const { PocketIc, PocketIcServer } = require(path.resolve(__dirname, "../../node_modules/ic-mops/node_modules/pic-js-mops"));
const { IDL } = require(path.resolve(__dirname, "../../node_modules/ic-mops/node_modules/@icp-sdk/core/lib/cjs/candid/index.js"));
const { Principal } = require(path.resolve(__dirname, "../../node_modules/ic-mops/node_modules/@icp-sdk/core/lib/cjs/principal/index.js"));
const [bin, wasm] = process.argv.slice(2);
if (!bin || !wasm) throw new Error("Expected PocketIC binary and treasury payment-operation fixture Wasm");
const Hash = IDL.Vec(IDL.Nat8), P = IDL.Principal;
const Input = IDL.Record({ logicalId: IDL.Text, operationId: IDL.Text, obligationId: IDL.Text, assetId: IDL.Text, amountBaseUnits: IDL.Nat, assetDecimals: IDL.Nat8, destinationHash: Hash, desiredVersion: IDL.Nat64, contentHash: Hash });
const Result = IDL.Variant({ acknowledged: IDL.Null, blocked: IDL.Null, conflict: IDL.Null, storageError: IDL.Null });
const ChunkHash = IDL.Record({ hash: Hash }), Upload = IDL.Record({ canister_id: P, chunk: Hash });
const Install = IDL.Record({ arg: Hash, chunk_hashes_list: IDL.Vec(ChunkHash), mode: IDL.Variant({ install: IDL.Null, upgrade: IDL.Opt(IDL.Record({ skip_pre_upgrade: IDL.Opt(IDL.Bool), wasm_memory_persistence: IDL.Opt(IDL.Variant({ keep: IDL.Null, replace: IDL.Null })) })) }), sender_canister_version: IDL.Opt(IDL.Nat64), store_canister: IDL.Opt(P), canister_id: P, target_canister: P, wasm_module_hash: Hash });
const management = Principal.fromText("aaaaa-aa"), lowInstallationCycles = 100_000_000_000n;
const idl = ({ IDL: C }) => C.Service({ retainOperationThenWrite: C.Func([Input], [Result], []), retainOperationForOperatorRepair: C.Func([Input], [Result], []), retryRetainedOperation: C.Func([], [Result], []), repairRetainedOperation: C.Func([], [Result], []) });
const hash = n => Uint8Array.from({ length: 32 }, (_, i) => (n + i) % 256);
function expect(value, tag, label) { if (Object.keys(value).length !== 1 || !(tag in value)) throw new Error(`${label}: expected ${tag}, got ${JSON.stringify(value)}`); }
async function mgmt(pic, sender, method, type, value) { return pic.client.updateCall({ canisterId: management, sender, method, payload: new Uint8Array(IDL.encode([type], [value])) }); }
async function install(pic, sender, id, mode = { install: null }) { const bytes = fs.readFileSync(wasm), chunks = []; for (let offset = 0; offset < bytes.length; offset += 1_000_000) { const chunk = new Uint8Array(bytes.subarray(offset, Math.min(offset + 1_000_000, bytes.length))); await mgmt(pic, sender, "upload_chunk", Upload, { canister_id: id, chunk }); chunks.push({ hash: new Uint8Array(crypto.createHash("sha256").update(chunk).digest()) }); } await mgmt(pic, sender, "install_chunked_code", Install, { arg: new Uint8Array(), chunk_hashes_list: chunks, mode, sender_canister_version: [], store_canister: [], canister_id: id, target_canister: id, wasm_module_hash: new Uint8Array(crypto.createHash("sha256").update(bytes).digest()) }); }
async function main() { const server = await PocketIcServer.start({ binPath: bin, ttl: 60, showRuntimeLogs: false, showCanisterLogs: false }); const pic = await PocketIc.create(server.getUrl()); try { const installer = Principal.fromUint8Array(Uint8Array.of(8, 42)); const id = await pic.createCanister({ sender: installer, controllers: [installer], cycles: lowInstallationCycles }); await install(pic, installer, id); const actor = pic.createActor(idl, id); actor.setPrincipal(installer); const input = { logicalId: "payment-operation:v1:low-cycles", operationId: "operation:low-cycles", obligationId: "obligation:low-cycles", assetId: "icrc1:fixture", amountBaseUnits: 1n, assetDecimals: 8, destinationHash: hash(7), desiredVersion: 1n, contentHash: hash(9) }; expect(await actor.retainOperationThenWrite(input), "blocked", "low-cycle operation is retained but not written"); expect(await actor.retainOperationThenWrite({ ...input, contentHash: hash(10) }), "blocked", "retained operation rejects a replacement"); expect(await actor.retryRetainedOperation(), "blocked", "low-cycle retry has no private write"); await install(pic, installer, id, { upgrade: [{ skip_pre_upgrade: [], wasm_memory_persistence: [{ keep: null }] }] }); expect(await actor.retryRetainedOperation(), "blocked", "upgraded low-cycle retry retains intent but has no private write"); if ((await pic.addCycles(id, 2_000_000_000_000n)) < 1_000_000_000_000n) throw new Error("low-cycle proof failed to replenish disposable treasury fixture"); expect(await actor.retryRetainedOperation(), "acknowledged", "replenished retry writes only retained operation"); expect(await actor.retryRetainedOperation(), "blocked", "consumed operation cannot replay"); const interrupted = { ...input, logicalId: "payment-operation:v1:operator-repair", operationId: "operation:operator-repair", obligationId: "obligation:operator-repair", destinationHash: hash(11), contentHash: hash(12) }; expect(await actor.retainOperationForOperatorRepair(interrupted), "blocked", "interrupted operation retains no automatic write path"); expect(await actor.retryRetainedOperation(), "blocked", "ordinary retry cannot bypass operator repair"); actor.setPrincipal(Principal.fromUint8Array(Uint8Array.of(8, 99))); expect(await actor.repairRetainedOperation(), "blocked", "outsider cannot repair retained operation"); actor.setPrincipal(installer); await install(pic, installer, id, { upgrade: [{ skip_pre_upgrade: [], wasm_memory_persistence: [{ keep: null }] }] }); expect(await actor.repairRetainedOperation(), "acknowledged", "authorized no-input repair writes retained operation after upgrade"); expect(await actor.repairRetainedOperation(), "blocked", "consumed repaired operation cannot replay"); console.log("treasury embedded payment-operation low-cycle and operator-repair proof passed"); } finally { await pic.tearDown(); await server.stop(); } }
main().catch(error => { console.error(error.stack || error.message); process.exitCode = 1; });
