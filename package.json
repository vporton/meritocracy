{
  "name": "meritocracy-app",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "version": "1.0.0",
  "description": "Full-stack Node.js + React application with Prisma ORM",
  "scripts": {
    "dev": "npx concurrently --kill-others-on-fail --kill-others \"npm run dev-backend\" \"npm run dev-frontend\"",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",
    "build": "npm run build-backend && npm run build-frontend",
    "build-backend": "cd backend && npm run build --prefix .",
    "build-frontend": "cd frontend && npm run build --prefix .",
    "start": "npm run setup && npx concurrently --kill-others-on-fail --kill-others \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "npm start --prefix backend",
    "start-frontend": "npm start --prefix frontend",
    "setup": "npm run db:setup",
    "db:setup": "cd backend && npx prisma migrate deploy --config prisma/prisma.config.js",
    "db:reset": "cd backend && npx prisma db push --force-reset --config prisma/prisma.config.js",
    "db:dev": "cd backend && npx prisma migrate dev --config prisma/prisma.config.js",
    "db:seed": "true",
    "dev:stop": "pkill -f 'npm run dev' || true && pkill -f nodemon || true && pkill -f vite || true && pkill -f 'node src/index.js' || true",
    "deploy:staging": "./deploy/deploy-staging.sh",
    "prereqs": "docker build -t vporton/fly -f deploy/Dockerfile.fly . && docker image push vporton/fly",
    "test": "cd backend && npm run test"
  },
  "keywords": [
    "nodejs",
    "react",
    "prisma",
    "express",
    "vite",
    "fullstack",
    "template"
  ],
  "author": "",
  "license": "MIT",
  "overrides": {
    "flexible-batches": {
      "openai": "6.22.0"
    }
  },
  "dependencies": {
    "@reown/appkit-adapter-bitcoin": "^1.8.20",
    "@reown/appkit-adapter-solana": "^1.8.20",
    "@types/node": "^25.3.0",
    "concurrently": "^10.0.3",
    "eslint": "^10.0.1",
    "prisma": "^7.8.0",
    "serve": "^14.2.6",
    "typescript": "^5.9.3"
  },
  "packageManager": "npm@11.10.1"
}
