#!/usr/bin/env node
// Synthetic capacity boundary for treasury's private immutable journal adapter.
const path = require("node:path"), fs = require("node:fs"), crypto = require("node:crypto"), os = require("node:os");
const { spawn } = require("node:child_process");
const root = path.resolve(__dirname, "../../node_modules/ic-mops/node_modules");
const { PocketIc } = require(path.join(root, "pic-js-mops"));
const { IDL } = require(path.join(root, "@icp-sdk/core/lib/cjs/candid/index.js"));
const { Principal } = require(path.join(root, "@icp-sdk/core/lib/cjs/principal/index.js"));
const [bin, wasm, reportPath] = process.argv.slice(2);
if (!bin || !wasm || !reportPath) throw new Error("Expected PocketIC binary, fixture Wasm, and report path");
const Hash = IDL.Vec(IDL.Nat8), P = IDL.Principal;
const Input = IDL.Record({ logicalId: IDL.Text, journalSequence: IDL.Nat64, operationId: IDL.Text, accountId: IDL.Text, assetId: IDL.Text, direction: IDL.Variant({ debit: IDL.Null, credit: IDL.Null }), amountBaseUnits: IDL.Nat, assetDecimals: IDL.Nat8, desiredVersion: IDL.Nat64, contentHash: Hash });
const Result = IDL.Variant({ acknowledged: IDL.Null, blocked: IDL.Null, conflict: IDL.Null, storageError: IDL.Null });
const Chunk = IDL.Record({ hash: Hash }), Upload = IDL.Record({ canister_id: P, chunk: Hash });
const Install = IDL.Record({ arg: Hash, chunk_hashes_list: IDL.Vec(Chunk), mode: IDL.Variant({ install: IDL.Null }), sender_canister_version: IDL.Opt(IDL.Nat64), store_canister: IDL.Opt(P), canister_id: P, target_canister: P, wasm_module_hash: Hash });
const management = Principal.fromText("aaaaa-aa"), idl = ({ IDL: C }) => C.Service({ writeJournal: C.Func([Input], [Result], []) });
const timeout = Number.parseInt(process.env.M1_POCKET_IC_STARTUP_TIMEOUT_MS || "120000", 10);
if (!Number.isSafeInteger(timeout) || timeout < 30000 || timeout > 120000) throw new Error("M1_POCKET_IC_STARTUP_TIMEOUT_MS must be an integer between 30000 and 120000");
const hash = n => Uint8Array.from({ length: 32 }, (_, i) => (n + i) % 256);
function expect(value, tag, label) { if (Object.keys(value).length !== 1 || !(tag in value)) throw new Error(`${label}: expected ${tag}`); }
async function update(pic, sender, method, type, value) { return pic.client.updateCall({ canisterId: management, sender, method, payload: new Uint8Array(IDL.encode([type], [value])) }); }
async function start(binPath) { const dir = fs.mkdtempSync(path.join(os.tmpdir(), "m1-treasury-journal-capacity-")), portFile = path.join(dir, "port"), process = spawn(binPath, ["--port-file", portFile, "--ttl", "60"], { stdio: ["ignore", "ignore", "ignore"] }); const stop = async () => { if (process.exitCode === null) { process.kill(); await new Promise(resolve => process.once("exit", resolve)); } fs.rmSync(dir, { recursive: true, force: true }); }; try { const deadline = Date.now() + timeout; while (Date.now() < deadline) { try { const port = Number.parseInt(fs.readFileSync(portFile, "utf8"), 10); if (Number.isInteger(port) && port > 0 && port <= 65535) return { url: `http://127.0.0.1:${port}`, stop }; } catch (error) { if (error.code !== "ENOENT") throw error; } if (process.exitCode !== null) throw new Error(`PocketIC exited before startup with status ${process.exitCode}`); await new Promise(resolve => setTimeout(resolve, 100)); } throw new Error(`PocketIC did not publish its port within ${timeout}ms`); } catch (error) { await stop(); throw error; } }
async function install(pic, sender, id) { const bytes = fs.readFileSync(wasm), hashes = []; for (let offset = 0; offset < bytes.length; offset += 1000000) { const chunk = new Uint8Array(bytes.subarray(offset, Math.min(offset + 1000000, bytes.length))); await update(pic, sender, "upload_chunk", Upload, { canister_id: id, chunk }); hashes.push({ hash: new Uint8Array(crypto.createHash("sha256").update(chunk).digest()) }); } await update(pic, sender, "install_chunked_code", Install, { arg: new Uint8Array(), chunk_hashes_list: hashes, mode: { install: null }, sender_canister_version: [], store_canister: [], canister_id: id, target_canister: id, wasm_module_hash: new Uint8Array(crypto.createHash("sha256").update(bytes).digest()) }); }
async function scenario(pic, installer, name, count) { const id = await pic.createCanister({ sender: installer, controllers: [installer], cycles: 20000000000000n }); await install(pic, installer, id); const actor = pic.createActor(idl, id); actor.setPrincipal(installer); const before = await pic.getCyclesBalance(id); const invalid = { logicalId: "x".repeat(513), journalSequence: 9999n, operationId: "operation:invalid", accountId: "treasury:available", assetId: "icrc1:fixture", direction: { debit: null }, amountBaseUnits: 1n, assetDecimals: 8, desiredVersion: 1n, contentHash: hash(250) }; expect(await actor.writeJournal(invalid), "blocked", `${name} oversized logical ID is blocked before storage`); let encodedInputBytes = 0; for (let ordinal = 0; ordinal < count; ordinal += 1) { const input = { logicalId: `treasury-journal:v1:embedded-capacity:${name}:${ordinal}`, journalSequence: BigInt(ordinal + 1), operationId: `operation:embedded-capacity:${name}:${ordinal}`, accountId: "treasury:available", assetId: "icrc1:fixture", direction: { debit: null }, amountBaseUnits: BigInt(ordinal + 1), assetDecimals: 8, desiredVersion: 1n, contentHash: hash(ordinal) }; encodedInputBytes += IDL.encode([Input], [input]).byteLength; expect(await actor.writeJournal(input), "acknowledged", `${name} write ${ordinal}`); expect(await actor.writeJournal(input), "acknowledged", `${name} exact retry ${ordinal}`); } return { name, writes: count, exactRetries: count, encodedInputBytes, fixtureCycleDelta: (before - await pic.getCyclesBalance(id)).toString() }; }
async function main() { const server = await start(bin), pic = await PocketIc.create(server.url); try { const installer = Principal.fromUint8Array(Uint8Array.of(8, 42)); const report = { schemaVersion: 1, component: "M1 treasury private journal adapter capacity proof", emulator: "PocketIC synthetic-only", scenarios: [await scenario(pic, installer, "expected", 16), await scenario(pic, installer, "two_x", 32)], rejection: { logicalIdBytes: 513, result: "blocked before private collection insertion" }, limitations: ["Cycle deltas are emulator measurements, not a production instruction or cycle budget.", "This covers only the treasury private journal adapter; balanced-set, archive, and accounting evidence remain required."] }; fs.writeFileSync(reportPath, `${JSON.stringify(report, null, 2)}\n`, { mode: 0o600, flag: "wx" }); console.log(JSON.stringify(report)); } finally { await pic.tearDown(); await server.stop(); } }
main().catch(error => { console.error(error.stack || error.message); process.exitCode = 1; });
