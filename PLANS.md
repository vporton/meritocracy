# ICP migration execution plan

Last updated: 2026-09-22

Status: `M1_EMPIRICAL_SCHEMA_AND_STORAGE_DESIGN` — IN_PROGRESS. The owner approved the consolidated three-custom-canister target on 2026-09-19. The AGPL-3.0-only license/metadata change remains complete. Existing storage-authority work is un-deployed, non-authoritative experiment evidence only; it has no target data, deployment, or legacy behavior change.

Latest M1 evidence (2026-09-16): the read-only PostgreSQL inventory command produced a private mode-`0600` aggregate report for all 22 required physical tables from the operator-approved PostgreSQL account. The report records 22 relation entries, 17 sequences, eight status histograms, a redaction projection for every table, and no foreign-key or unique-index collisions; it is intentionally retained outside version control. PostgreSQL catalog `name[]` aggregates are now explicitly cast to `text[]`, matching the `pg` client's decoded type and preserving the command's fail-closed metadata validation. Focused regression tests and the backend TypeScript build pass. It remains aggregate-only, transaction-read-only evidence collection; no production mutation, export, replication artifact, or target authority claim is made.

## Status vocabulary

- `NOT_STARTED`: no implementation exists.
- `IN_PROGRESS`: active work is within an approved milestone.
- `IMPLEMENTED`: code exists but has not passed every acceptance/reconciliation check.
- `VERIFIED`: acceptance criteria passed and evidence is recorded.
- `BLOCKED`: a listed dependency or one of the four approval gates is unmet.
- `INTENTIONALLY_CHANGED`: legacy behavior was unsafe/incorrect and the approved target behavior differs.

## Global execution protocol

For every milestone:

1. Restate its invariants and acceptance criteria.
2. Implement the smallest coherent change in a reviewable commit.
3. Add or update tests, including negative/failure-path tests.
4. Run formatting, type checking, builds, upgrade checks, and relevant tests.
5. Review the diff for regressions, authorization gaps, replay/reentrancy, unbounded work, data loss, and secret/fund exposure.
6. Update this file and `docs/icp/PARITY_CHECKLIST.md` with evidence.
7. Continue to the next unblocked milestone, stopping only at the four gates below.

No production mutation, production logical replication, DNS cutover, mainnet canister deployment, legacy-key transfer, or real-asset transfer is allowed before its stated milestone and gate.

### Legacy safety bridge during migration

The legacy Node/PostgreSQL service remains recoverable until M10, but this plan does not approve continued real-asset custody or automatic payment retry through its known unsafe paths. Until a separately approved, narrowly scoped legacy-hardening change proves otherwise, production payment senders stay paused, no missing wallet key is generated, and an unknown legacy send is held for manual chain reconciliation rather than reset or retried. Any exception must name the asset/scope, accountable operator, compensating controls, expiry, and rollback action; it does not waive the target G3/G4 requirements.

The legacy browser bearer-token path is rollback-only. If it remains Internet-facing before M10, it needs an explicitly approved compatibility hardening plan for bearer storage and CSP; it must not be treated as equivalent to the target Candid-principal authentication model.

## Implementation task-card requirements

Every implementation prompt derived from a milestone must stand alone. It must state its objective; the applicable milestone, gate, invariants, and security constraints; the files/subsystems to inspect; exact required and prohibited changes; tests/validation and evidence commands; acceptance/completion criteria; and its rollback boundary. It must explicitly preserve the legacy application, production data, production signing authority, and unrelated user changes unless the approved milestone says otherwise. A task card that depends on a prior design decision must name that decision and stop as `BLOCKED` when its gate or evidence is absent.

## Approval gates

These are the only planned approval stops.

| Gate | Status | Decision being approved | Evidence required to request approval |
| --- | --- | --- | --- |
| G1 — Target architecture | **AMENDED AND APPROVED 2026-09-19** | Replace the prior core/workflow/archive/evidence/storage microservice topology with exactly three custom canisters: certified frontend assets, consolidated application, and isolated unified treasury; retain only an M1-proven private embedded store; prohibit additional custom canisters without a measured G1 exception | Owner approved the consolidation and implementation in this task. `ARCHITECTURE.md` records the three-canister boundary, private-module authorization model, capacity/cycle thresholds, rollback, and exception process. The completed AGPL-3.0-only text remains in `LICENSE.txt` and metadata commit `9122ff0`. |
| G2 — Database schema and migration design | BLOCKED by G1 | Concrete Motoko/ZenDB types/indexes, exact legacy transformations, canonical export format, importer protocol, OAuth-to-caller binding protocol, reconciliation queries, cutover delta mechanism, public-identifier disclosure boundary, and PII lifecycle policy | Owner-approved G2 input for controller, disclosure, deletion/retention, Didit minimum-attestation, global cross-border contractor terms, AML/sanctions eligibility, transactional-email, and DAO/upgrade principles is recorded in `docs/icp/G2_PRIVACY_AND_GOVERNANCE_DECISION.md`; it is not G2 approval. Remaining evidence: read-only production inventory; exact ZenDB source/dependency/Candid/Wasm pins and Motoko `identify` package version/source/package/API hashes; authoritative-mutation, logical-ID, RBAC, OAuth state/PKCE/caller-binding, upgrade, recovery, outbound-call, and quota proofs; the reviewed global cross-border terms/compliance policy, current AML/sanctions controls, and per-field recorded legal basis, retention/deletion, backup, access-audit, and anti-evasion/accounting exception implementation; Didit/Resend source, dependency/API, DPA/transfer/retention, credential-scope, and minimum-payload proofs; PostgreSQL logical-decoding/exported-snapshot, replica-identity, commit-order, WAL-retention, and sensitive-column redaction proofs; schema/property tests; golden export/import dry run; revised `SCHEMA_MAPPING.md` and runbook |
| G3 — Wallet custody and authorization | BLOCKED by G2 | Asset custody per network, unified Chain Fusion treasury and SNS controller model, payout authorization, finality/reorg policy, legacy-key retirement, incident response | Threat model; test-key prototypes; replay/ambiguous-send/finality tests; local/PocketIC SNS-controller and governance-recovery drills; recorded human decision naming the production SNS launch/ownership model, applicable `sns_init`/tokenomics or existing-SNS configuration, and root-handoff/recovery policy; independent wallet review; revised `WALLET_SECURITY.md` |
| G4 — Testnet-to-production migration | BLOCKED by G3 | A non-custodial mainnet SNS testflight with an approved cycle budget, then mainnet production deployment, production data cutover, DNS/frontend cutover, and separately authorized real-asset movement | Full dress rehearsal from sanitized snapshot; source/destination and financial reconciliation; rollback rehearsal; controller/module plus exact ZenDB pin/RBAC/intent checks; testnet parity; reviewed G4 testflight/recovery runbook with isolated canister IDs, bounded approved cycles, test-only derivation/environment, no custodial assets, and an abort proof; independent security sign-off |

Approval of G4 authorizes only the reviewed runbook steps. Each real-asset transfer remains a deliberate manual runbook action with recorded transaction details; automated tests never move real assets.

## Milestone plan

### M0 — Repository discovery and planning baseline

Status: **VERIFIED** (documentation-only)

Dependencies: none.

Invariants:

- The legacy source remains unchanged and runnable.
- No database, external account, canister, or asset is mutated.
- Unknown live-data facts remain explicitly unknown rather than guessed.

Scope/evidence:

- Audited 193 tracked files, 21 Prisma models, 18 SQL migrations, 22 physical tables (including unmanaged `ai_result_migration_exceptions`), all Prisma call sites, 17 explicit Prisma transactions, REST routes, auth/authorization, task graph, timers/cron, wallet adapters, frontend calls, tests, Docker/Fly/GitHub Actions, scripts, and checked-in operational documentation.
- Current live row counts are unavailable because no `DATABASE_URL` is set. Numeric capacity figures in design documents are target envelopes, not claims about production data.
- Identified legacy financial and authorization defects that must be reconciled, not copied; see the risk register below and `PARITY_CHECKLIST.md`.
- Baseline verification under Node.js v26.5.0/npm 12.0.1 (`nvm use stable`): root backend+frontend production build passed; backend security suite passed (1/1). Frontend lint did not reach source analysis because ESLint 10 requires `eslint.config.js` while the repository still uses the legacy configuration. This pre-existing tooling gap is recorded below and must be corrected in a small post-G1 tooling commit.

23. **IN_PROGRESS 2026-09-11 (synthetic proof runner cache boundary):** `scripts/icp/run-embedded-identity-role-proof.sh` now copies its already-selected PocketIC binary byte-for-byte to a disposable writable `/tmp` directory before passing it to `pic-js-mops`. The client validates its binary with `chmod`, which cannot modify Mops' read-only cache; the copy preserves the selected binary while retaining the proof's no-DFX, no-identity, no-wallet, and no-network-configuration boundary. Shell and JavaScript syntax checks and `mops test IdentityRoleRecovery` pass; the synthetic fixture Wasm was built and hash-recorded. The isolated PocketIC execution still exceeds the workspace's interactive 30-second command ceiling, so no pass result is claimed. This change neither exposes a storage method nor makes the adapter authoritative.

24. **IMPLEMENTED 2026-09-11 (M1 upgrade-proof CI coverage):** `.github/workflows/m1-storage-authority-upgrade.yml` runs its isolated PocketIC proof jobs for its path-filtered pull requests and manual dispatches. Branch-push coverage is supplied by the consolidated core identity/role recovery proof workflow, avoiding duplicate Actions runs for a `feat/icp` push. The jobs, 20-minute limits, pinned dependencies, synthetic-only inputs, no-DFX/no-wallet boundary, and artifact retention are unchanged.

Acceptance:

- Required planning files exist and cross-reference each other.
- Every Prisma model/physical table and explicit transaction has a proposed disposition.
- No application-code diff exists.

Rollback: revert only the planning-document commit.

Suggested commits:

1. `docs: record ICP migration architecture and engineering rules`
2. `docs: map schema, custody, migration, rollback, and parity`

### G1 — Target architecture approval

Status: **AMENDED AND APPROVED 2026-09-19**.

The 2026-08-01 approval remains evidence for the completed AGPL-3.0-only licensing change. The owner approved the 2026-09-19 three-custom-canister consolidation stated in `docs/icp/ARCHITECTURE.md`. It still does not authorize production mutation, a wallet policy, an SNS launch, or any asset transfer.

### M1 — Empirical schema and storage design

Status: IN_PROGRESS.

Dependencies: recorded G1 consolidation amendment; read-only access to a production snapshot or a DBA-produced inventory report.

Invariants:

- Inventory queries are read-only and redact secret values.
- No production table, slot, trigger, row, or index is changed during sizing.
- The read-only inventory records PostgreSQL version and logical-decoding prerequisites, every table's replica identity and update/delete key, active prepared-transaction policy, publication/slot capacity, WAL-retention limits, and the approved source-side redaction projection for every credential/secret column. No general base or delta artifact may contain `SystemSecret.value`, session bearer values, raw email/KYC verification values, private keys, or credentials.
- The completed AGPL-3.0-only license and metadata change remains part of the repository baseline; no additional relicensing work is in scope for M1.
- ZenDB is the proposed persistent store for imported PostgreSQL/Prisma records and target document collections, including identity, balances, payment operations, replay journals, and migration receipts where M1 proves the required atomicity, boundedness, recovery, and upgrade behavior. Motoko application code remains the enforcement point for authorization, canonical encodings, and financial constraints; ZenDB constraints/indexes are defense in depth, not a substitute.
- **Approved topology, 2026-09-19:** the former storage-authority canister is not a target service. The consolidated application and treasury actors own private in-process storage modules; there is no storage Candid boundary or runtime grant matrix. Each public actor method remains the authorization boundary and private modules accept only typed internal calls. `M1_OPERATOR_HANDOFF.md` preserves the previous decision as historical scaffold evidence.
- M1 must prove bounded mutation, upgrade, recovery, archive/retention, low-cycle, and repair/resume behavior for each private collection. The only remaining custom-canister call between application domains is application-to-treasury; it journals before the call, uses an immutable logical ID/hash, and recovery leaves no partially activated authority. If a collection cannot meet its proof, G2 must record a narrowly scoped native-Motoko exception and its rationale rather than silently adding a storage service.
- An application logical ID/idempotency key is distinct from ZenDB's internal document ID. G2 is blocked until the pinned API proves either deterministic caller-supplied document IDs or a unique indexed application logical-ID field with conflict lookup and content-hash reconciliation. For an unknown remote result, the desired hash means success; an absent insert or an unchanged expected prior version/hash permits the identical insert/CAS retry; any other hash/version is a conflict. No retry invents a new key or blindly inserts a second document.

Small changes/commits:

The numbered entries below are an immutable chronology of M1 work performed
under the earlier topology. They remain useful evidence, but none authorizes
extending, deploying, or treating `storage_authority`, core, workflow, archive,
or evidence as separate target canisters. New work uses small
application/treasury-module task cards and fresh acceptance evidence.

1. **IMPLEMENTED 2026-08-01:** Added pinned ICP/Motoko/Mops toolchain manifests, empty interfaces, and the credential-free `oauth_fixture`; `docs/icp/TOOLCHAIN_AND_OAUTH_EVIDENCE.md` records the exact `identify` package/source/API hashes, provider-capability matrix, package limitations, and validation commands. This is design/fixture evidence only: no provider, OAuth client, credential, login, target authorization, or legacy behavior has been enabled or altered.
2. **IMPLEMENTED 2026-08-01:** Added `npm run inventory:postgres --workspace backend -- --output /secure/operator-only/m1-inventory.json`, documented in `docs/icp/POSTGRES_INVENTORY.md`. It starts a `SERIALIZABLE, READ ONLY, DEFERRABLE` transaction with a read-only startup default; produces only aggregate/capability/redaction metadata for all 22 physical tables; fails on a source-table mismatch; and contains no publication, slot, trigger, outbox, DDL, or row-write SQL. The command loads an ignored `backend/.env` when present without overriding an operator-supplied environment value; its failure output is limited to a safe authentication, permission, reachability, validation, or generic-query category. Unit tests prove the source-table boundary, prohibited-value exclusion, and redaction-safe failure classification. A 2026-08-07 attempt confirmed the local `DATABASE_URL` was loaded but returned only `INVENTORY_QUERY_FAILED`, with no report produced and no URL, endpoint, credentials, SQL, or row values emitted. On 2026-09-16, the operator-approved account produced the private mode-`0600` aggregate report for all 22 required tables. It records 22 relation entries, 17 sequences, eight status histograms, every table's redaction projection, and no foreign-key or unique-index collisions; the report is intentionally not tracked. M1 acceptance remains incomplete pending the remaining benchmark, storage-authority, and migration-rehearsal evidence.
3. **IN_PROGRESS 2026-08-02:** Pinned the released ZenDB `v2.0.1` source commit, complete Mops dependency lock, reproducible generated Candid/Wasm hashes, and a local-only reproducer in `docs/icp/evidence/zendb/v2.0.1.pin.json`, `scripts/icp/verify-zendb-pin.sh`, and `docs/icp/ZENDB_PIN_AND_BASELINE.md`. The evidence also records the non-byte-reproducible generated stable signature and its G2 compatibility gate. `scripts/icp/test-zendb-rbac.sh` validates the exact source/lock then runs the candidate's synthetic PocketIC remote-canister RBAC baseline, proving ungranted and collection-scoped writer negatives plus collection isolation; its exact coverage and exclusions are pinned in `docs/icp/evidence/zendb/v2.0.1.rbac-proof.json`. `scripts/icp/test-zendb-authoritative.sh` passed its corrected local-only runner against the remote candidate: a fresh ephemeral project-local loopback replica was health-checked before creation; every DFX operation named `--network local`; creation used `--no-wallet`; DFX used its built-in anonymous identity; Mops `install` was avoided; ingress/lifecycle operations were bounded to 180 seconds plus a 10-second kill grace, and exact O3 artifact builds to 360 seconds plus the same grace; and cleanup stopped the replica. The executed fixture proves unique logical-ID rejection, bounded logical-ID/hash recovery, and opaque one-document cursor advancement; its exact scope and exclusions are recorded in `docs/icp/evidence/zendb/v2.0.1.authoritative-proof.json`. `scripts/icp/test-zendb-benchmark.sh` passes the bounded synthetic expected/2× fixture on that boundary, measuring candidate-provided query/replace/delete instructions and document/index byte figures; its exact results, rejection-limit assertions, warning, and explicit gaps are recorded in `docs/icp/evidence/zendb/v2.0.1.benchmark-proof.json`. The previous Mops 2.19.2/pic-js-mops single-ingress path remains unsuitable for its 3,281,858-byte linked actor and is not claimed as execution evidence. The candidate is not yet authoritative: close the insert/reindex/archive/low-cycle measurement gaps, and add the remaining application durable-intent tests across actual lost reply/duplicate delivery, bootstrap and post-upgrade self-grant/grant audit, archive failure, crash/upgrade recovery, low-cycle, and repair/resume cases. An unmerged or future pull request is not evidence.
   The authoritative runner forces DFX's built-in anonymous identity for every canister operation, while replica lifecycle uses an isolated, disposable DFX config that is removed at exit; it therefore cannot select a developer PEM/keyring or wallet/signing authority. **FAILED 2026-08-07:** `M1UpgradeOwner.mo` gave a synthetic owner sole controller authority over an empty local `CanisterDB`, accepted only the SHA-256/length-bound exact pinned artifact, created a bounded synthetic collection, revoked its bootstrap admin grant, and upgraded the same artifact. The post-upgrade audit trapped because ZenDB `v2.0.1` restored that revoked bootstrap admin grant. This fails the M1 post-upgrade RBAC invariant: the candidate is not authoritative for any collection. Do not repair the proof with state replacement or a broader installer. G2 must either record a named, collection-specific native-Motoko exception or approve a later exact ZenDB pin only after it passes this proof. A read-only upstream tag check on 2026-08-07 found no released successor to `v2.0.1`; unmerged/future commits remain ineligible. Remaining eligible-candidate gaps are insert/reindex/archive/low-cycle measurements and complete crash/upgrade mutation recovery.
   **Superseded authorization topology 2026-08-07:** As recorded in `docs/icp/M1_OPERATOR_HANDOFF.md`, the project owner selected an in-process ZenDB library behind a Motoko storage-authority canister. The remote-grant and restored-bootstrap-grant findings above remain diagnostic for the rejected remote topology. They neither configure nor authorize the target; M1 must now implement and prove the storage-authority's Motoko RBAC boundary.
4. **IMPLEMENTED 2026-08-01 (design/scaffolding only):** Defined versioned Motoko records and the proposed ZenDB collection/index/limit/grant catalogue in `canisters/shared/StorageTypes.mo`, `canisters/shared/StorageCatalog.mo`, and `docs/icp/M1_STORAGE_SCHEMA_AND_SAGAS.md`. The contract fixes logical-ID/hash envelopes, native mutation intents, pending-data visibility manifests, exact collection-vN migration steps, and the lost-reply reconciliation rule. It adds no actor behavior, live principal grant, deployment, or production access. Its required local benchmark, RBAC-negative, interruption, upgrade, low-cycle, and repair/resume proof remains incomplete; no collection is yet authoritative.
5. **IMPLEMENTED 2026-08-01 (design/schema/vector evidence only):** Defined `meritocracy-migration-canonical-v1` in `docs/icp/canonical-v1.schema.json`, its source-ID-to-application-logical-ID mapping and base/delta/import contract in `docs/icp/CANONICAL_EXPORT_IMPORT_CONTRACT.md`, and byte/hash golden vectors in `test/canonical-v1-vectors.json`, exercised by `npm run test:canonical --workspace backend`. The local codec is data-only and cannot read PostgreSQL, create a slot/publication, grant access, or import data. The G2/M4 proof still requires an independently matching Motoko codec, explicit approved redacted source projections, a logical slot/publication created before the base export, all base workers using that slot's exported snapshot and consistent point, complete source-transaction commit order through the barrier LSN, and disposable PostgreSQL/ZenDB fault rehearsal.
6. **IMPLEMENTED 2026-08-01 (inventory only; shipment remains blocked):** `docs/icp/DEPENDENCY_INVENTORY.md` and its machine-checked lock record identify the root workspace `package-lock.json` as the rollback and retained-frontend-toolchain closure; all direct manifest versions are exact pins and `Dockerfile` now uses `npm ci`. The production scan found 13 high, zero critical findings, grouped with actual reachability, owner, containment, and a required upgrade/removal decision. No adapter was removed. `npm run inventory:dependencies` verifies the reviewed closure offline. No affected bundle is approved for shipment until the scan is clean or a compatibility-tested, explicitly accepted disposition is recorded.
7. **IMPLEMENTED 2026-08-07 (tooling only):** Added `frontend/eslint.config.js`, pinning the TypeScript-aware parser through `typescript-eslint@8.65.0`; ESLint is now the compatible `9.39.1` root development dependency because the retained React plugin cannot run on ESLint 10. The baseline parses every TypeScript/TSX frontend source, rejects stale disable directives, preserves React hook-placement enforcement, and rejects unsafe `target="_blank"` links; the two existing external links now include `rel="noopener noreferrer"`. `npm run lint --workspace frontend` and the frontend production build pass. The lock inventory and production-only audit were refreshed: 855 production dependencies, 13 high, zero critical. All high findings remain shipment-blocking; no production dependency, adapter, route, credential, database, canister, or wallet behavior changed.
8. **IN_PROGRESS 2026-08-07 (local-only authorization scaffold):** Added `canisters/storage_authority`, its Candid baseline, stable signature baseline, and pure `StorageAuthorityPolicy` contract. The only callable methods are fixed owner-specific, bounded logical-ID authorization probes plus a governance-only policy audit; there is no generic collection/action/document interface, ZenDB API, grant API, configuration mutation, payload persistence, target data, or deployment. Its shared initializer requires six distinct non-anonymous core/workflow/treasury/archive/evidence/governance principals and rejects any initializer that is one of them, while the policy gives each application principal only its own owner scope and gives governance audit-only access. `test/StorageAuthorityPolicy.test.mo` proves anonymous/direct-user, unrelated-canister, bootstrap/deployer, governance-as-data-caller, cross-owner, malformed-ID, duplicate/anonymous configuration, and governance-audit negatives across every declared collection owner. `scripts/icp/test-storage-authority-boundary.sh` builds an isolated DFX project and local replica using only disposable canisters and test identities; its passed proof exercises anonymous direct ingress, a bootstrap/deployer direct call, and actual inter-canister core/unrelated/governance callers, including cross-owner, malformed-ID, and governance-audit cases. It then upgrades the exact same storage-authority artifact and repeats those direct-ingress and inter-canister assertions, proving that this empty actor's fixed allowlist and denials survive that local upgrade. The runner isolates even its DFX version probe and local-replica launch from ambient identities. It never configures or calls ZenDB. `mops test`, `mops check`, `mops build`, and `mops check-stable` passed (3 test files; 6 canisters). This remains non-authoritative scaffolding: a future-state upgrade, lost-reply, archive, low-cycle, repair/resume, and data-mutation recovery proof is outstanding.
9. **IMPLEMENTED 2026-08-07 (persistent policy state and PocketIC boundary proof):** The storage authority stores its fixed six-principal matrix in a private persistent field, and the generated stable signature records it. An upgrade cannot replace that matrix merely by receiving a new actor-class init argument, and no public configuration mutation exists. **Correction to item 8:** its DFX runner was not valid evidence and remains disabled; DFX 0.32 identities are global, and a local run observed an ambient home-directory identity despite the script's intended configuration isolation. No production network, canister, data, or asset was touched. The replacement `scripts/icp/test-storage-authority-boundary.sh` pins PocketIC 12.0.0 in `mops.toml`, compiles the exact scaffold and disposable caller fixture in a fresh `/tmp` directory, and uses Mops 2.19.2's lock-pinned `pic-js-mops` 0.14.8 client with synthetic principals only; it does not invoke DFX or accept an identity/wallet. It proves anonymous direct ingress and bootstrap/unrelated direct ingress are denied; each core/workflow/treasury/archive/evidence caller is allowed only its fixed owner probes; malformed IDs, cross-owner calls, unrelated canisters, and governance-as-data are denied; only governance receives the audit; and an EOP-preserving upgrade supplied a distinct valid six-principal init matrix cannot replace the persisted matrix. `mops test`, `mops check`, `mops build`, `mops check-stable`, and this PocketIC runner pass. This remains non-authoritative scaffolding: ZenDB persistence, target data, mutation/recovery, archive, low-cycle, and repair/resume proofs are outstanding.
10. **IMPLEMENTED 2026-08-07 (embedded ZenDB compiler boundary):** `fixtures/zendb/M1EmbeddedStorageProbe.mo` and `scripts/icp/test-zendb-embedded-storage.sh` copy only a hash-verified archive and fully hash-checked lock closure of the exact v2.0.1 source into a fresh `/tmp` directory, then compile a private persistent `ZenDB.Types.VersionedStableStore` against the pin's Motoko 1.4.1 compiler. The source checkout is read-only; the runner never invokes DFX, PocketIC, a network, identity, wallet, registry request, canister install, database/collection creation, document operation, or public CRUD method. The probe passed locally using the already available exact checkout. It proves only that the candidate's in-process stable-store type can compile behind an actor boundary. It does **not** make ZenDB authoritative, prove behavior/upgrade safety, or permit substituting the pin's Motoko 1.4.1 toolchain for the repository's M1-pinned 0.16.3 toolchain; a deliberate compatible toolchain/dependency decision and all bounded mutation/recovery proofs remain required before G2.
11. **IMPLEMENTED 2026-08-07 (embedded toolchain/dependency decision):** `scripts/icp/test-zendb-embedded-storage.sh --compiler=zendb|repository` uses the same hash-verified archive and dependency closure to make the compiler distinction explicit. `scripts/icp/test-zendb-embedded-toolchain-boundary.sh` first requires the pin's Motoko 1.4.1 compiler to accept the private no-CRUD probe, then requires this repository's pinned Motoko 0.16.3 compiler to reject it with the locked `core@2.4.0`/`Float32` incompatibility. **Decision:** retain the repository's pinned 0.16.3 toolchain and do not add ZenDB v2.0.1 as an embedded target dependency or introduce an unreviewed dual-compiler build path. The pin remains source/provenance and rejected-topology diagnostic evidence only; it cannot make any collection authoritative. A future candidate may reopen this decision only with an exact source/dependency/toolchain pin that compiles under the approved repository toolchain, followed by the required Candid/stable compatibility and bounded mutation/recovery proofs. An unexpected repository-compiler pass deliberately fails the boundary runner until that evidence and decision are recorded.
12. **IMPLEMENTED 2026-08-07 (exact collection/action scaffold):** Replaced the owner-wide storage-authority probes with one fixed read and one fixed write probe for every declared M1 catalogue collection, including core identity/authorization, workflow, treasury, migration/archive, and evidence collections. The Candid surface still accepts only a bounded logical ID and exposes no caller-selected collection/action, generic CRUD/filter, payload, persistence, ZenDB API, grant API, configuration mutation, target data, or deployment. The identity-free PocketIC proof now exercises both probes for every declared collection with the matching synthetic application canister, plus malformed ID, cross-owner collection, anonymous/direct-user, bootstrap/deployer, unrelated-canister, governance-as-data, governance-only audit, and persisted-matrix post-upgrade negatives. `mops test`, `mops check`, `mops build`, and `mops check-stable` pass. This is not an authoritative-store claim: the ZenDB v2.0.1 embedded dependency remains rejected under the pinned repository toolchain, and bounded writes/lookups, archive behavior, low-cycle, lost-reply/duplicate/recovery, and repair/resume proof remain G2 blockers.
13. **IMPLEMENTED 2026-08-08 (durable-recovery decision contract only):** Added the pure `canisters/shared/MutationRecovery.mo` decision contract and `test/MutationRecovery.test.mo`. It requires a durable intent to record the desired remote version as well as its 32-byte content hash, and permits a lost-reply acknowledgement only when the bounded lookup returns that exact version/hash. An absent insert or exactly unchanged complete expected CAS target permits only an identical retry; partial expectations, malformed hashes, and concurrent version/hash observations fail closed. Its bounded manifest rule refuses empty or over-500-member activation and keeps visibility pending until every member and the manifest acknowledgement are exact. `mops test` (4 test files), `mops check`, `mops build`, `mops check-stable`, and the synthetic-principal PocketIC storage-boundary proof pass. This is executable protocol evidence only: it adds no actor state, Candid method, target document, ZenDB dependency, persistence, storage mutation, deployment, or native-Motoko collection exception. Actual storage-authority lost-reply/duplicate, archive, interruption, upgrade, low-cycle, and repair/resume proofs remain required before G2.
14. **IMPLEMENTED 2026-08-08 (single-toolchain adoption):** The project owner selected the exact ZenDB Motoko `1.4.1` compiler for the repository, superseding items 10–12's earlier `0.16.3` rejection decision. `mops.toml` now pins `moc = "1.4.1"`; the two legacy `motoko-base` import aliases resolve to the exact upstream `moc-1.4.1` commit `d1b336e142bec76577e3d63d9f1fac74de48df0a`; the source-identical `identify@0.0.2` backend-only package view excludes only its upstream frontend E2E actor, which Motoko 1.4 rejects in a library search path; and the management-canister Candid declaration is vendored and hashed for explicit `--actor-idl` resolution. `mops test` (4 files), `mops check`, `mops build`, `mops check-stable`, the fixed storage-authority PocketIC proof, and both exact-source embedded ZenDB compiler probes pass with Motoko 1.4.1. This is a compiler/dependency compatibility change only: ZenDB is still not imported by a target canister, no storage method or data is enabled, and the required bounded persistence/recovery/RBAC/upgrade/low-cycle proofs remain M1/G2 blockers.
15. **IMPLEMENTED 2026-08-08 (both core package lines):** The application Mops closure now locks both `core@1.0.0` (the retained unqualified OAuth dependency) and `core@2.2.0` (available only as explicit `mo:core@2.2`). `mops check` and stable checks pass with both installed. This does not resolve ZenDB integration: ZenDB's exact source imports unqualified `mo:core`, for which Mops still selects only the `core@1` binding, so ZenDB lacks `Nat.bytes`. The candidate's isolated compiler probe still passes because it runs in ZenDB's own lock root; it is not combined-build evidence. No ZenDB target dependency, storage method, data, deployment, or compatibility fork was added. Before integration can resume, record an owner-approved exact compatible `identify` update or a minimal audited compatibility fork that makes each dependency's `core` import explicit (including source/package/API hashes, OAuth-vector revalidation, Candid/stable review, and rollback); do not silently modify the pinned source or introduce a dual compiler.
16. **IMPLEMENTED 2026-08-09 (approved `identify` compatibility fork):** Following owner approval, replaced the direct released `identify@0.0.2` dependency with the local `identify-m1-compat@0.0.2-m1.1` fork, exposed under the unchanged `identify` import name. Its only runtime-source delta is making all 103 backend `mo:core/...` imports explicitly `mo:core@1/...`; it excludes the upstream frontend E2E actor fixtures that Motoko 1.4 rejects in a library package path. `src/lib.mo` is byte-identical to the released API. `scripts/icp/verify-identify-m1-compat.sh` proves that exact narrow delta, root/lock selection, exclusion, and source/package hashes; `mops test OAuthAttempt` passes the caller/state/expiry/replay/unsupported-provider vectors. No provider, credential, OAuth callback, target authorization, ZenDB import, storage method, target data, deployment, Candid interface, or stable type changed. The original actor failure is cleared, but `mops check` now reaches an independent `hex@1.0.2` Motoko-1.4 `Text.join` API incompatibility. Do not widen this fork, alter `hex`, or introduce an alternate compiler without a new approved disposition, exact hashes, API review, OAuth-vector regression, and Candid/stable review.
17. **IMPLEMENTED 2026-09-09 (owner-directed local hex compatibility package):** Replaced the released `hex@1.0.2` package in the root and `identify-m1-compat` closures with the local `hex-m1-compat@1.0.2-m1.1` package under the unchanged `mo:hex` import. It implements only the stateless byte/nibble and formatted-text API actually used by the retained `identify` backend (`toText`, `toTextFormat`, encoder helpers, and format constants), avoiding the incompatible `Text.join` call; it has no persistent state, actor, Candid, external call, credential, OAuth protocol, target authorization, ZenDB import, storage method, target data, or deployment behavior. `test/HexM1Compat.test.mo` proves compact, upper-case, URL, empty, and verbose vectors; `scripts/icp/verify-hex-m1-compat.sh` pins the manifest/source/package hashes and both dependency edges. `mops test HexM1Compat` and `mops test OAuthAttempt` pass. The full `mops check` now advances past hex and fails independently in ZenDB's transitive `memory-collection@0.4.0` at `Nat.bytes` under the unqualified Core-1 binding. Do not expand this package's API or substitute/patch that next dependency without a separate scoped disposition and compatibility review.
18. **IMPLEMENTED 2026-09-09 (owner-approved local memory-collection compatibility package):** The reported Core-binding diagnosis was corrected by source inspection: released `memory-collection@0.4.0` already uses explicit `mo:core@2.4` imports, but its public `MemoryBTreeSet` wrapper calls a missing `BaseMemoryBTree.bytes`. The root now resolves its unchanged `mo:memory-collection@0.4` import to the local `memory-collection-m1-compat@0.4.0-m1.1` package. The fork is a verbatim copy of the exact Mops-locked release except for its package name/version and one `BaseMemoryBTree.bytes` function that aliases the existing `usedBytes` calculation; it changes neither stable layout nor allocation behavior. `scripts/icp/verify-memory-collection-m1-compat.sh` rejects any changed file outside that manifest/Base pair and pins both changed-file hashes; `mops check`, `mops test` (5 files), `mops build` (6 canisters), and `mops check-stable` pass. This clears only the combined compiler blocker. No target canister imports ZenDB, and no storage operation, target data, Candid interface, stable type, credential, deployment, or authority is enabled. The remaining required bounded persistence, lost-reply/duplicate, interruption/upgrade, archive, low-cycle, and repair/resume proofs remain M1/G2 blockers.
19. **IMPLEMENTED 2026-09-10 (embedded storage-authority type boundary):** `storage_authority` now privately initializes the exact M1-pinned in-process `ZenDB.Types.VersionedStableStore`, with `Principal.fromActor(this)` bound to the authority actor. It adds no collection, document, index, storage Candid method, generic CRUD/filter, RemoteInstance/CanisterDB actor, ZenDB grant API, target data, deployment, or authority claim; the existing fixed authorization probes remain the complete public surface. The regenerated `deployed/storage_authority.most` records the candidate store's stable representation before any deployment, so future changes must pass the normal stable-compatibility gate. `mops check storage_authority`, `mops build storage_authority`, `mops test StorageAuthorityPolicy`, and `mops test MutationRecovery` pass. The PocketIC boundary runner and full stable suite must be rerun in an environment that permits their longer linked-Wasm execution before this boundary can be treated as complete upgrade evidence. Bounded collection operations, lost-reply/duplicate recovery, interruption/upgrade, archive, low-cycle, and repair/resume proofs remain M1/G2 blockers.
20. **IMPLEMENTED 2026-09-10 (real embedded storage-authority upgrade proof):** `scripts/icp/build-storage-authority-upgrade-proof.sh` builds only the pre-embedded authority, current embedded authority, and fixed caller fixture WASMs; `scripts/icp/run-storage-authority-upgrade-proof.sh` then runs only the PocketIC proof, accepting the pinned binary through `POCKET_IC_BIN`. The proof installs the exact pre-embedded commit `4b9e51377ca351b4a162c5d102b258536d539ba2`, establishes and checks the complete fixed caller matrix, upgrades with PocketIC EOP `install_code` to the current embedded-store WASM, verifies the original matrix and all direct/inter-canister denials remain enforced, then performs a second real upgrade of the embedded artifact and repeats preservation/denial checks. Any replaced matrix, reset/bypassed authorization, or failed upgrade is an explicit failure. `.github/workflows/m1-storage-authority-upgrade.yml` runs the isolated build and proof with a 20-minute job limit, caches the pinned PocketIC toolchain, and retains the generated artifact hashes/logs. On 2026-09-10, the isolated artifact build and SHA-256 verification passed, then the full proof passed against the pinned PocketIC `12.0.0` executable copied byte-for-byte to a disposable writable `/tmp` path and supplied through `POCKET_IC_BIN`; no DFX identity, wallet, external network, target data, or deployment was used. This closes the embedded-store upgrade-evidence gap only. M1 remains IN_PROGRESS: the proof does not establish collection persistence, bounded operations, lost-reply/duplicate recovery, archive, low-cycle, or repair/resume behavior.
21. **IMPLEMENTED 2026-09-10 (identity/role mutation contract only):** Added the pure `IdentityRoleRecovery.mo` contract and Motoko vectors. It rejects anonymous principals, malformed/unbounded logical IDs and roles, non-32-byte hashes, incomplete or mismatched OAuth evidence, and any non-exact duplicate under an identity/role logical ID. Internet Identity bindings cannot carry OAuth evidence; OAuth bindings require both bounded provider and subject hash. An exact duplicate is the only acknowledged retry; a different hash fails closed and cannot implicitly replace a role. This adds no storage-authority Candid method, ZenDB collection/document, durable intent, target data, deployment, or authorization grant. The actual fixed-core collection methods must journal desired version/hash and prove bounded persistence, lost-reply/duplicate recovery, interruption/upgrade, and role-revocation history before they can be authoritative.
22. **IN_PROGRESS 2026-09-11 (fixed embedded identity/role adapter):** Added the private `canisters/storage_authority/EmbeddedIdentityRoleStore.mo` adapter and made the storage authority type-check it. It fixes only `core_principal_binding_v1` and `core_role_assignment_v1`, preserves the 32-byte hash/blob and principal representations, bounds encoded records to the catalogue document limit, uses a two-record logical-ID lookup, and allows an insert-only retry only when the previously persisted hash is exact; a different or multiply-observed value is a conflict. It exposes no actor/Candid method, generic CRUD/filter, ZenDB grant, collection configuration, caller authorization bypass, target data, deployment, or authority claim. The core canister has not yet journaled a desired version/hash or called it, so this is not a lost-reply proof. `mops test IdentityRoleRecovery` passes. The pinned Mops interpreter reaches an internal `FromCandidE` limitation when executing ZenDB's generic Candid decoder, so execution uses the added isolated synthetic Wasm/PocketIC fixture instead: it admits one binding and one role, acknowledges each exact retry, and rejects a changed hash, malformed ID, and anonymous identity. Its build and execution are separated into `build-embedded-identity-role-proof.sh` and `run-embedded-identity-role-proof.sh`, with a 20-minute CI workflow that supplies only the pinned PocketIC binary and fixture Wasm. Local JavaScript and shell syntax checks pass; the fixture build exceeded the interactive workspace's 30-second command window, so no execution result is claimed yet. This adapter must not be used or reported as persistence/recovery proof until that isolated PocketIC run passes. The full storage-authority build also exceeds the interactive workspace's 30-second command window and remains a CI/external validation task.

23. **IMPLEMENTED 2026-09-11 (synthetic embedded identity/role execution proof):** GitHub Actions [run `34621917379`](https://github.com/vporton/meritocracy/actions/runs/34621917379) passed on `feat/icp` commit `8454e70d05e40d61e2ff7cc15b0af59fdff6b754`. Its isolated `embedded-identity-role-proof` job installed the exact SHA-256-recorded fixture into a synthetic PocketIC canister through bounded 1,000,000-byte chunk uploads and verified one initial binding and role write, exact retries, changed-hash conflicts, malformed role rejection, and anonymous-identity rejection. The pinned PocketIC 12.0.0 bridge uses the standard `target_canister` plus a same-principal `canister_id` routing field required by that runner's ingress path; no DFX configuration, identity, wallet, external network, target data, storage-authority Candid method, or deployment is involved. The concurrent old-to-new-to-new storage-authority upgrade job also passed. The local fixture builder now compiles and hashes in a disposable sibling directory and publishes the Wasm/checksum pair only after a complete build; an interrupted linked-Wasm build cannot replace a prior fixture with a mismatched checksum. This proves only the two fixed adapter collections under synthetic execution; it does not establish domain-caller journaling, lost-reply/duplicate recovery, interruption/upgrade persistence of those records, archive behavior, low-cycle handling, or repair/resume, and therefore does not make any collection authoritative.

24. **IMPLEMENTED 2026-09-11 (synthetic identity/role upgrade-retention proof):** The isolated PocketIC runner performs an EOP `install_chunked_code` upgrade using only the same SHA-256-verified synthetic fixture Wasm after the initial fixed binding/role writes and negative checks. It then requires each exact retry to acknowledge and each changed-hash retry to conflict, which distinguishes retained immutable records from an empty or replaced collection. The builder's published checksum names the artifact relatively rather than its removed disposable staging path, so the runner validates the published pair before execution. Shell/JavaScript syntax checks, `mops test IdentityRoleRecovery`, the linked fixture build, and its checksum verification pass. On 2026-09-11, the same runner passed locally when executed in the isolated environment that permits PocketIC startup: it installed the fixture through bounded chunk uploads, performed the real EOP upgrade, and verified both binding and role retention through exact-retry acknowledgement and changed-hash conflicts. The workspace-sandbox attempt reached the runner but its PocketIC bridge exceeded its bounded startup deadline; this is an environment limitation, not a pass claim for that sandbox. The CI workflow retains its build/proof logs and remains the reproducible execution path. This remains synthetic-only and does not expose a storage-authority Candid method, authorize a domain caller, establish domain-caller journaling or lost-reply recovery, or make either collection authoritative.
25. **IMPLEMENTED 2026-09-11 (version-bound identity/role retry contract):** The fixed embedded binding and role record schemas now persist the immutable desired remote version together with the 32-byte content hash. Exact retry acknowledgement requires the bounded two-record lookup to find the same logical ID, version, and hash; a changed version or hash is a conflict before and after the synthetic fixture's EOP upgrade. `mops test IdentityRoleRecovery`, `mops check storage_authority`, and JavaScript syntax validation pass; `mops check-stable` reached the first four unchanged canisters before the interactive command ceiling, so no full stable-suite pass is claimed here. No Candid method, ZenDB grant, target data, deployment, caller authorization, durable core intent, or lost-reply proof was added. The next bounded proof must journal this exact version/hash in the fixed core caller before its storage-authority call and reconcile a deliberately lost reply.

26. **IMPLEMENTED 2026-09-11 (fixed core lost-reply and duplicate-delivery execution proof):** Added the isolated build/run pair `scripts/icp/build-core-identity-role-lost-reply-proof.sh` and `scripts/icp/run-core-identity-role-lost-reply-proof.sh`, plus its PocketIC bridge and 20-minute GitHub Actions job. The runner builds and SHA-256-verifies only the disposable synthetic fixtures, creates synthetic authority/core/archive canisters, supplies their known canister principals as class arguments, denies outsider direct authority write/lookup and non-operator core ingress, journals before the core's authority `await`, requires the deliberately lost reply to reject, performs an EOP upgrade of the core before recovery, and requires the configured core-only exact lookup to reconcile as `acknowledge`. It then redelivers the identical immutable request, deliberately loses that reply too, and requires a second exact lookup acknowledgement; the authority's fixed insert-or-exact-lookup path cannot create a second record or accept a changed version/hash. Archive availability is explicitly revoked after the binding receipt before the role-unavailability negative case, so that case proves no role receipt was written; re-permission is required before the later lost-reply role receipt path. Chunks are at most 1,000,000 bytes; the runner copies the pinned PocketIC binary byte-for-byte into a disposable writable `/tmp` path and uses no DFX, identity, wallet, external network, target data, deployment, or project-canister Candid method. JavaScript syntax validation, `mops test IdentityRoleIntent`, `mops check core_canister`, and `mops check storage_authority` pass. On 2026-09-11, the isolated local runner rebuilt the two fixtures, verified both SHA-256 entries, and passed the full PocketIC proof. This is not an authoritative collection claim: authority upgrade, interruption phases, archive, low-cycle, repair/resume, and all non-identity collections remain M1/G2 blockers.
27. **IMPLEMENTED 2026-09-11 (authority-upgrade lost-reply proof):** The same disposable proof upgrades the authority with EOP after its acknowledged fixed binding write and before the core upgrades or reconciles its deliberately lost reply. Its persistent fixture distinguishes a fresh collection creation from an upgrade reopen, trapping rather than recreating or replacing a retained fixed collection. After the authority upgrade, outsider direct write/lookup denials are repeated; the core then upgrades and may acknowledge recovery only when the fixed core-only lookup returns the original logical ID, version, and hash. Shell/JavaScript syntax checks, `mops test IdentityRoleIntent`, `mops check core_canister`, `mops check storage_authority`, and the linked fixture build plus SHA-256 verification pass. On 2026-09-11, the isolated disposable PocketIC run passed: it verified both fixture artifacts, performed the authority EOP upgrade before core recovery, repeated outsider denials, upgraded the core, acknowledged recovery only after the exact core-only lookup, and acknowledged the deliberately lost duplicate delivery without a second record. The runner uses a byte-identical `/tmp` copy of the pinned binary and no DFX, identity, wallet, external network, target data, or deployment. This remains synthetic-only: it does not expose a project Candid storage method, authorize real data, establish interruption/low-cycle/archive/repair behavior, or make either collection authoritative.

28. **IMPLEMENTED 2026-09-12 (pre-await interruption recovery proof):** The same disposable core fixture has a distinct interruption phase after persisting a valid immutable identity-binding intent but before it issues any authority call. Its recovery lookup must therefore observe `absent` and return only `retryIdentical`. After a real EOP core upgrade, a separate no-argument retry method can redeliver only the retained journaled logical ID/version/hash—not fresh caller input—and deliberately loses its authority reply; the existing fixed lookup must then acknowledge the exact retained tuple. `mops test IdentityRoleIntent`, `mops check core_canister`, shell syntax checks, JavaScript syntax checks, and `git diff --check` pass. GitHub Actions [run `34688239928`](https://github.com/vporton/meritocracy/actions/runs/34688239928) passed on commit `838fd7a6341a58816d3fa2da3cefec8fb0fda943`: its isolated `pocketic-proof` job built the SHA-256-verified disposable fixtures and passed the interruption, authority-upgrade, core-upgrade, lost-reply, and duplicate-delivery sequence. The workspace-sandbox runner reaches its bounded PocketIC startup deadline before creating any canister, so it supplies no local execution claim. This changes only synthetic fixtures/scripts and exposes no project Candid storage method, target data, deployment, ZenDB grant, or authority claim.

29. **IMPLEMENTED 2026-09-12 (fixed embedded authority identity/role surface):** The un-deployed `storage_authority` now opens only the fixed `core_principal_binding_v1` and `core_role_assignment_v1` embedded collections, reopening rather than recreating them after an EOP upgrade. It exposes exactly three core-canister-only methods: principal-binding write, fixed principal-binding recovery lookup, and role-assignment write. Each authorizes the configured core principal in the method body before the adapter is reached; there is no caller-selected collection/action/document ID, generic CRUD/filter, ZenDB grant/configuration API, role read, target deployment, or production data. Unauthorized writes return `blocked` and lookups return `conflict`, revealing no collection existence. The isolated lost-reply proof now builds the exact storage-authority artifact (not its former authority fixture), retains the synthetic core only, and configures distinct synthetic workflow/treasury/archive/evidence/governance principals; it has no DFX, wallet, identity, external network, or target data. `mops test IdentityRoleIntent`, `mops check storage_authority`, stable compatibility, Candid compatibility from the prior baseline, and artifact checksum verification pass. The workspace runner verified both artifacts but timed out while starting PocketIC before canister creation, so no execution pass is claimed; its existing CI job must supply that result. This is a bounded M1 collection proof surface, not an authoritative-collection claim: actual authority/core lost-reply execution, role lookup/recovery, archive, low-cycle, and repair/resume proofs remain blockers.

30. **IMPLEMENTED 2026-09-12 (fixed embedded role recovery surface and proof extension):** Added the fourth and final identity/role Candid method, `lookupCoreRoleAssignment`. It is callable only by the configured core principal, accepts only one bounded logical ID, reads at most two fixed role-assignment records, returns only `absent`, `conflict`, `storageError`, or the journaled version/hash tuple, and returns `conflict` to every unauthorized caller. It cannot reveal a role or principal, select a collection/action/filter, mutate a record, or expose a ZenDB grant/configuration API. The disposable core fixture has an independent durable role intent, journals before its fixed role-write await, deliberately loses the acknowledgement, and reconciles only through that tuple-only lookup; the PocketIC bridge now denies outsider role writes/lookups before and after the authority EOP upgrade and requires the synthetic core role lost-reply acknowledgement after the core upgrade. `mops test` (7 files), `mops check storage_authority`, `mops check core_canister`, direct fixture type-check, JavaScript/shell syntax checks, `didc check`, Candid subtyping from the prior baseline, and `git diff --check` pass. The linked-Wasm/PocketIC execution was not rerun in this interactive workspace, so this item does not claim a new execution pass; the existing isolated CI proof must run the extended artifact before it can be credited as execution evidence. No target data, deployment, generic storage API, or authoritative-collection claim was added. Role interruption/retry, archive, low-cycle, repair/resume, and all non-identity collections remain M1/G2 blockers.

31. **IMPLEMENTED 2026-09-12 (role pre-await interruption/retry proof extension):** The disposable core fixture now separately journals a validated immutable role-assignment intent and deliberately traps before its fixed authority `await`. After a core EOP upgrade, its tuple-only role lookup must observe `absent`; only a no-input retry method can then resend that durable logical-ID/version/hash tuple, deliberately lose the reply, and reconcile `acknowledge` only from the exact fixed role lookup. The pure `IdentityRoleIntent` vector proves the same absent-to-identical-retry transition for roles. `mops test IdentityRoleIntent`, `mops check storage_authority`, `mops check core_canister` (including stable compatibility), JavaScript/shell syntax checks, and `git diff --check` pass. The isolated fixture builder regenerated and checksum-recorded both Wasms; the full linked-Wasm/PocketIC execution was not run in this interactive workspace, so this item makes no local execution-pass claim and remains covered by the existing isolated CI proof. No project Candid surface, target data, deployment, generic storage API, or authoritative-collection claim was added. Archive, low-cycle, repair/resume, and all non-identity collections remain M1/G2 blockers.

32. **IMPLEMENTED 2026-09-12 (identity/role archive-acknowledgement contract only):** Added the pure `IdentityRoleArchiveRecovery` contract and vectors. An archive receipt carries only a bounded logical ID, immutable version, and 32-byte content hash; it cannot carry a principal, role, OAuth evidence, document ID, or payload. Only an exact valid tuple acknowledges an archive; an unavailable or mismatched receipt remains pending, while malformed source or receipt tuples are blocked. `mops test IdentityRoleArchiveRecovery`, `mops test IdentityRoleIntent`, and `mops check storage_authority` pass. The contract adds no archive actor call, persistence, Candid method, storage-authority authorization, target data, deployment, or authority claim. The required actual archive-failure/acknowledged-activation saga must still prove that a core-owned record cannot become active before an exact archive acknowledgement across interruption and upgrade; low-cycle, repair/resume, and all non-identity collections remain M1/G2 blockers.

33. **IMPLEMENTED 2026-09-12 (synthetic fixed identity archive activation saga):** Extended the isolated core/authority proof with `fixtures/storage_authority/IdentityRoleArchiveSink.mo`, a separate persistent synthetic archive boundary that accepts only a core-caller bounded logical-ID/version/32-byte-hash tuple and exposes no identity, role, OAuth, document, or payload data. The synthetic core journals that tuple before the archive await and remains inactive after an unavailable archive reply. Its fixed tuple-only lookup returns `remainPending` after a core EOP upgrade with no receipt; after the operator permits the sink, a deliberately lost archive acknowledgement is reconciled only by an exact receipt lookup after both archive and core EOP upgrades, at which point and only then the binding becomes active. The PocketIC bridge denies outsider archive permit and lookup ingress, checksum-verifies all three disposable Wasms, and continues to use no DFX, wallet, identity, external network, project archive method, target data, or deployment. `mops test IdentityRoleArchiveRecovery`, `mops check storage_authority`, shell syntax checks, and JavaScript syntax checks pass. The linked three-Wasm build exceeds this workspace's interactive 30-second ceiling, so no local PocketIC execution pass is claimed; the existing 20-minute isolated CI job is the required execution evidence. This proves only the identity-binding archive activation branch in synthetic fixtures; role archive activation, low-cycle, repair/resume, and all non-identity collections remain M1/G2 blockers.

34. **IMPLEMENTED 2026-09-12 (synthetic fixed role archive activation saga):** Extended the same disposable core/authority/archive proof with an independent durable role archive tuple and active flag. After its fixed role write has been reconciled, archive unavailability leaves the role inactive; the tuple and inactive state survive a core EOP upgrade and a missing receipt remains `remainPending`. Once the already-permitted synthetic sink accepts the role's tuple-only request, the reply is deliberately lost; only an exact logical-ID/version/32-byte-hash receipt lookup after both archive and core EOP upgrades activates the role. The sink remains core-only and is explicitly bounded to its fixed receipt set, rejecting conflicting versions/hashes and retaining no role, principal, OAuth, document, or payload data. `mops test IdentityRoleArchiveRecovery`, `mops check storage_authority`, shell/JavaScript syntax checks, and `git diff --check` pass. The linked three-Wasm build again exceeded the workspace's interactive 30-second ceiling before publishing artifacts, so no local PocketIC execution pass is claimed; the existing isolated CI proof must run this extended artifact. This is synthetic-only and exposes no project archive endpoint, target data, deployment, generic storage API, or authoritative-collection claim. Low-cycle, repair/resume, and all non-identity/role collections remain M1/G2 blockers.

35. **IMPLEMENTED 2026-09-12 (synthetic fixed identity low-cycle recovery proof):** The disposable core fixture journals a validated immutable binding intent, crosses a fixed read-only authority lookup checkpoint, then applies a one-trillion-cycle reserve guard before any authority write. A PocketIC core canister starts below that reserve; its write traps without creating a record, and the fixed lookup must observe `absent` and return only `retryIdentical`. The bridge replenishes only that disposable core with PocketIC's test-only cycle API, then the no-input retry redelivers the retained logical-ID/version/hash tuple, deliberately loses its reply, and reconciles only by the exact fixed lookup. `CycleReserve.mo` fixes the proof-only reserve decision and its vectors reject one-less-than-reserve while accepting the exact boundary. `mops test CycleReserve`, `mops check storage_authority`, `mops check core_canister` (including stable compatibility), the linked three-Wasm build and SHA-256 validation, JavaScript/shell syntax checks, and `git diff --check` pass. The workspace PocketIC bridge timed out during startup before canister creation, so no local execution pass is claimed; the existing isolated CI job is required to execute the extended proof. It uses no DFX, wallet, identity, external network, target data, deployment, public cycle endpoint, or production budget. The fixed reserve is not a production capacity decision; G2 still requires measured collection-specific low-cycle limits, alerts, and recovery policy. Role low-cycle, repair/resume, and all non-identity/role collections remain M1/G2 blockers.

36. **IMPLEMENTED 2026-09-12 (synthetic fixed role low-cycle recovery proof):** The same disposable core proof now exercises its independent role-assignment journal below the one-trillion-cycle reserve before the core is replenished. The fixed role write traps after journaling and its exact tuple-only authority lookup observes `absent`, permitting only `retryIdentical`; no role record is created. After PocketIC's test-only replenishment of that disposable core, the no-input role retry redelivers the retained logical-ID/version/hash tuple, deliberately loses its reply, and reconciles only by the exact fixed role lookup. This uses the existing `CycleReserve` boundary and does not add a project Candid method, public cycle endpoint, target data, deployment, wallet, identity, external network, or production budget. JavaScript/shell syntax checks and the existing fixture build/checksum validation are required; the isolated CI PocketIC proof must execute the extended artifact before it is credited as execution evidence. The fixed reserve remains proof-only; G2 still requires measured collection-specific low-cycle limits, alerts, recovery policy, repair/resume, and proof for all non-identity/role collections.

37. **IMPLEMENTED 2026-09-12 (synthetic fixed identity/role repair-resume proof):** Added separate operator-only, no-input repair methods to the disposable core fixture. Each reads only its own retained intent, reconciles through the fixed tuple-only authority lookup, can resend only that exact retained tuple after an `absent` observation, and journals/looks up only the corresponding archive tuple before activation. The proof injects a pre-await interruption for new binding and role records, lets each repair reach an unavailable archive (which leaves it inactive), EOP-upgrades the core, denies outsider repair ingress, then re-permits the bounded archive sink and requires each retained tuple to activate only after its exact receipt. The synthetic archive admits exactly four fixed logical IDs covering the two prior activation branches and these two repair branches; it still receives no payload. `mops test IdentityRoleIntent`, `mops test IdentityRoleArchiveRecovery`, core stable compatibility, fixture type checks, JavaScript/shell syntax checks, and `git diff --check` pass. The isolated linked-Wasm build/checksum and PocketIC execution are CI evidence still to be collected; this item claims no local execution pass. It adds no project Candid surface, data, deployment, generic storage API, or authoritative-collection claim. Collection-specific capacity policy and all financial, replay, migration, and other collection proofs remain M1/G2 blockers.

38. **IMPLEMENTED 2026-09-12 (fixed treasury payment-operation intent contract only):** Added the pure `canisters/treasury/PaymentOperationIntent.mo` contract and vectors. It accepts only a bounded immutable logical ID, operation ID, obligation ID, asset ID, positive integer base-unit amount, explicit bounded asset decimals, and 32-byte destination/content hashes; it stores no destination address or signing material. A future treasury caller must journal that exact version/hash tuple before its fixed storage call. An unknown reply is acknowledged only by the exact tuple; absence permits only an identical retry, and a changed version/hash conflicts rather than creating replacement transaction material. `mops test PaymentOperationIntent` validates acknowledgement, identical retry, changed-version conflict, zero amount, malformed destination hash, unsupported decimals, and malformed operation ID. This adds no treasury actor behavior, Candid method, ZenDB collection, storage-authority call, signer, chain call, target data, deployment, or authority claim. The required payment-operation/obligation/journal/chain-receipt saga, at-most-one-transfer proof, archive, low-cycle, repair/resume, and all other collection proofs remain M1/G2 blockers.

39. **IMPLEMENTED 2026-09-12 (fixed treasury payment-operation archive-acknowledgement contract only):** Added the pure `canisters/treasury/PaymentOperationArchiveRecovery.mo` contract and vectors. Its receipt contains only the bounded payment-operation logical ID, immutable version, and 32-byte content hash; it carries no amount, asset, destination, obligation, signed bytes, transaction material, or chain receipt. Only an exact valid retained tuple acknowledges an archive; an unavailable or mismatched receipt remains pending, while malformed source or receipt tuples are blocked. `mops test PaymentOperationArchiveRecovery` proves exact acknowledgement, unavailable/mismatched receipts, and malformed logical-ID/hash negatives. This adds no treasury actor behavior, Candid method, ZenDB collection, storage-authority call, archive actor call, signer, chain call, target data, deployment, or authority claim. The required payment-operation/obligation/journal/chain-receipt saga, at-most-one-transfer proof, actual archive failure/upgrade/low-cycle/repair-resume execution, and all other collection proofs remain M1/G2 blockers.

40. **IMPLEMENTED 2026-09-12 (private embedded treasury payment-operation collection boundary):** Added `canisters/storage_authority/EmbeddedPaymentOperationStore.mo`, which opens only the fixed `treasury_payment_operation_v1` collection with its unique immutable logical-ID constraint. Its private record carries the validated operation ID, obligation ID, asset ID, integer base-unit amount, explicit decimals, and 32-byte destination/content hashes; it cannot carry a destination address, signing material, transaction bytes, or chain receipt. The storage authority reopens this one collection after EOP rather than recreating it, but exposes no treasury write/read/recovery Candid method, generic collection/action/document/filter interface, ZenDB grant/configuration API, target data, deployment, signer, or chain call. `mops test EmbeddedPaymentOperationStore`, `mops test PaymentOperationIntent`, `mops check storage_authority`, and `git diff --check` pass. `mops check-stable storage_authority` exceeded the interactive command ceiling after initialization, so no stable-suite pass is claimed. A future fixed treasury caller must first journal the exact operation tuple and then prove duplicate/lost-reply, interruption/upgrade, archive activation, low-cycle, repair/resume, and at-most-one-transfer behavior before this collection can be authoritative.
41. **IMPLEMENTED 2026-09-12 (fixed embedded treasury payment-operation surface):** The un-deployed storage authority now exposes exactly two treasury-canister-only methods for `treasury_payment_operation_v1`: immutable write and bounded tuple-only recovery lookup. Both bind the caller to the persisted treasury principal in the method body; unauthorized writes return `blocked` and lookups return `conflict`, revealing no existence signal. The adapter rejects malformed or oversized candidates, caps its logical-ID search at two records, inserts only an absent logical ID, and acknowledges an existing record only when both immutable version and 32-byte content hash match. It returns no amount, asset, destination hash, obligation, operation ID, document ID, signer, transaction material, or chain receipt. There is no caller-selected collection/action/document/filter, generic CRUD, ZenDB grant/configuration API, treasury actor caller, durable treasury intent, archive call, signer, chain call, target data, deployment, or authoritative-collection claim. `mops test EmbeddedPaymentOperationStore`, `mops test PaymentOperationIntent`, `mops test PaymentOperationArchiveRecovery`, `mops check storage_authority`, `didc check canisters/storage_authority/storage_authority.did`, and `git diff --check` pass. `mops check-stable storage_authority` again exceeded the 25-second interactive boundary after initialization, so no stable-suite pass is claimed. A future fixed treasury caller must journal its exact operation tuple before awaiting this surface and prove duplicate/lost-reply, interruption/EOP upgrade, archive activation, low-cycle, repair/resume, and at-most-one-transfer behavior before any authority claim.
42. **IMPLEMENTED 2026-09-12 (fixed treasury payment-operation saga contract only):** Added pure `canisters/treasury/PaymentOperationSaga.mo` and vectors. A valid immutable operation enters archive work only after its fixed storage-authority lookup acknowledges the exact retained logical-ID/version/32-byte-hash tuple. A lost or mismatched archive receipt leaves the operation inactive; only the retained exact receipt can activate it. The contract accepts no fresh operation input during archive recovery and contains no actor state, Candid method, storage-authority call, archive call, signer, transaction material, chain call, target data, deployment, or authority claim. `mops test PaymentOperationSaga` proves pre-acknowledgement archive denial, exact storage acknowledgement, missing/mismatched archive receipt pending behavior, exact activation, and changed-version storage conflict. A future persistent treasury caller must journal before its authority await and prove duplicate/lost-reply, interruption/EOP upgrade, actual archive failure, low-cycle, repair/resume, and at-most-one-transfer behavior before any collection or custody authority claim.

43. **IMPLEMENTED 2026-09-12 (synthetic fixed treasury lost-reply fixture):** Added `fixtures/storage_authority/TreasuryPaymentOperationLostReply.mo` and `scripts/icp/build-treasury-payment-operation-lost-reply-proof.sh`. The disposable persistent fixture accepts an immutable payment-operation candidate only from its configured synthetic operator, journals the validated exact input before its fixed storage-authority await, deliberately loses an acknowledged reply, and reconciles only with the treasury-only bounded tuple lookup. Its no-input retry can resend only that retained logical-ID/version/hash tuple after an `absent` result; it cannot accept substitute operation, obligation, asset, amount, destination, signer, transaction, or chain material. The builder compiles only the real fixed storage-authority artifact and the synthetic treasury fixture in a disposable staging directory, checksum-records them only after a complete build, and does not start a replica or read DFX state, identities, wallets, credentials, or network configuration. `mops test PaymentOperationIntent`, `mops test EmbeddedPaymentOperationStore`, `mops check storage_authority`, and shell syntax checking pass. The linked-Wasm builder exceeded this workspace's 30-second interactive command ceiling before completion, so no artifact-build, PocketIC execution, duplicate-delivery, interruption/EOP-upgrade, archive, low-cycle, repair/resume, or at-most-one-transfer pass is claimed. This adds no target treasury actor behavior, Candid method, deployment, authority, custody, signing, or chain call.
44. **IMPLEMENTED 2026-09-12 (synthetic treasury recovery runner):** Added `scripts/icp/run-treasury-payment-operation-lost-reply-proof.sh` and its isolated PocketIC bridge. It verifies SHA-256-published disposable artifacts, copies the pinned PocketIC binary byte-for-byte to a writable `/tmp` directory, and uses only synthetic principals/canisters. The bridge denies outsider direct storage ingress and treasury recovery, loses an acknowledged fixed treasury write, EOP-upgrades both the actual storage-authority fixture and disposable treasury fixture, then accepts recovery only when the bounded treasury-only lookup returns the retained exact logical-ID/version/hash tuple. It redelivers that exact immutable input once and again requires exact reconciliation, proving the duplicate path cannot create a different operation key. The PocketIC process did not reach canister creation within the workspace's interactive 30-second ceiling, so no local execution pass is claimed; the SHA-256 artifacts, shell/JavaScript syntax, focused Motoko vectors, and storage-authority check pass. This runner has no DFX, wallet, identity, network configuration, signer, destination address, transaction material, chain call, target data, deployment, custody, or authority behavior. Archive activation, interruption-before-await, low-cycle, repair/resume, measured capacity, and at-most-one-transfer execution remain required M1/G2 proof work.
45. **IMPLEMENTED 2026-09-12 (synthetic treasury pre-await interruption/retry extension):** The disposable treasury fixture now persists one validated immutable payment-operation intent and deliberately traps before issuing its fixed storage-authority call. Its bridge EOP-upgrades the treasury fixture, requires its treasury-only bounded lookup to report `absent`/`retryIdentical`, then calls the sole no-input retry path; that path may send only the durable logical-ID/version/32-byte-hash tuple, deliberately loses its reply, and can acknowledge recovery only after an exact tuple lookup. The fixture accepts no substitute operation, obligation, asset, amount, destination, signer, transaction material, or chain input during recovery. `mops test PaymentOperationIntent`, `mops check storage_authority`, direct fixture type-check, Candid syntax, JavaScript/shell syntax, and `git diff --check` pass. The amended linked-Wasm build and PocketIC execution were not run in this interactive workspace, so this is not execution evidence. It adds no target treasury Candid method, deployment, custody, signing, chain call, target data, or authority claim. Archive activation, low-cycle, repair/resume, capacity, and at-most-one-transfer execution remain M1/G2 proof work.

46. **IMPLEMENTED 2026-09-12 (synthetic treasury archive-activation extension):** The disposable treasury fixture now begins archive work only after its fixed storage lookup has acknowledged the retained payment-operation logical-ID/version/32-byte-hash tuple. The new disposable archive sink accepts only either of the two proof IDs and the acknowledgement tuple—never amount, asset, destination, obligation, signer, transaction bytes, or chain receipt. The PocketIC bridge first revokes archive availability, requires the archive call to reject and the operation to remain inactive through a treasury EOP upgrade, then permits the sink, deliberately loses its exact acknowledgement, EOP-upgrades both sink and treasury, and activates only after the retained tuple-only lookup returns the exact receipt. Focused payment-operation intent/archive/saga vectors, the storage-authority check, both direct fixture type checks, JavaScript/shell syntax checks, and `git diff --check` pass. The amended linked-Wasm build and isolated PocketIC execution have not run in this interactive workspace, so this is not execution evidence. It adds no target treasury Candid method, deployment, custody, signer, chain call, target data, generic storage API, or authority claim. Low-cycle, repair/resume, capacity, and at-most-one-transfer execution remain M1/G2 proof work.

47. **IMPLEMENTED 2026-09-12 (synthetic treasury low-cycle recovery extension):** The disposable treasury fixture now journals a validated immutable payment-operation intent, crosses only its fixed read-only tuple lookup checkpoint, and applies the shared one-trillion-cycle reserve guard before any treasury authority write. The PocketIC bridge creates only that synthetic treasury below the proof reserve, requires the guarded write to trap with the fixed lookup still `absent`/`retryIdentical`, then replenishes only the disposable fixture with PocketIC's test-only cycle API. Its no-input retry may redeliver only the retained logical-ID/version/32-byte-hash tuple, deliberately loses its reply, and can acknowledge only after the exact bounded treasury lookup. The reserve is not a production capacity policy and introduces no cycle endpoint. Focused payment-operation, embedded-store, and reserve vectors; storage-authority and direct fixture checks; JavaScript/shell syntax; Candid syntax; and `git diff --check` pass. The linked-Wasm build and isolated PocketIC execution exceed this workspace's interactive command ceiling, so no local execution pass is claimed. This adds no target treasury behavior, Candid method, deployment, custody, signer, transaction material, chain call, target data, generic storage API, or authority claim. Repair/resume, measured capacity policy, and at-most-one-transfer execution remain M1/G2 proof work.

48. **IMPLEMENTED 2026-09-12 (synthetic treasury repair/resume extension):** The disposable treasury fixture now adds two operator-only no-input repair methods. The authority repair reads only its retained immutable payment-operation journal, reconciles through the fixed treasury-only tuple lookup, and after an `absent` observation may resend only the stored logical-ID/version/32-byte-hash tuple; a present record is accepted only by the same exact tuple. The archive repair reads only the retained archive tuple and cannot activate on availability or operator action—only an exact receipt lookup activates it. The PocketIC bridge injects a distinct pre-await interruption, EOP-upgrades the treasury, rejects outsider repair ingress, proves no-input authority repair and exact reconciliation, then forces archive unavailability/inactivity through another upgrade before a deliberately lost receipt is recovered after archive and treasury upgrades. The bounded synthetic sink accepts this third fixed proof ID and still receives no amount, asset, destination, obligation, signer, transaction, or chain data. Focused payment-operation intent/archive/saga/embedded-store vectors, storage-authority and treasury stable checks, JavaScript/shell syntax, and `git diff --check` pass. The amended linked-Wasm build and isolated PocketIC run are not claimed as local execution evidence. This adds no target treasury Candid method, deployment, custody, signer, chain call, target data, generic storage API, or authority claim. Measured capacity policy and at-most-one-transfer execution remain M1/G2 proof work.

49. **IMPLEMENTED 2026-09-12 (synthetic payment-operation at-most-one-dispatch extension):** Added the pure tuple-only `PaymentOperationTransferRecovery` contract, passing exact/missing/mismatched/malformed receipt vectors, and a disposable persistent transfer sink. The sink admits only its configured synthetic treasury principal, retains one immutable logical-ID/version/32-byte-hash tuple, returns that exact tuple on a duplicate, and rejects any competing tuple; it accepts no amount, asset, destination, obligation, signer, transaction bytes, chain receipt, chain endpoint, or value. Once the synthetic treasury has become active only through the existing exact storage/archive acknowledgements, its deliberately lost downstream reply is reconciled solely by a retained tuple lookup; a duplicate delivery can submit only that same retained tuple and the PocketIC bridge requires the sink's dispatch count to remain one. The isolated builder and runner now hash-verify the fourth fixture Wasm alongside the authority, treasury, and archive artifacts. `mops test PaymentOperationTransferRecovery`, payment-operation intent/archive/saga vectors, `mops check storage_authority`, both direct fixture type checks, JavaScript/shell syntax checks, and `git diff --check` pass. The linked-Wasm build and PocketIC execution remain unrun in this interactive workspace, so no execution pass is claimed. This is a valueless fixture-only idempotency proof: it adds no target treasury Candid method, deployment, custody, signer, transaction material, chain call, generic storage API, or authority claim. Measured capacity policy and actual chain-adapter at-most-one-value-transfer proof remain M1/G2/G3 work.

50. **IMPLEMENTED 2026-09-12 (synthetic treasury recovery CI coverage):** `.github/workflows/m1-treasury-payment-operation-lost-reply.yml` runs the isolated four-Wasm PocketIC proof on path-filtered pull requests and manual dispatches, with a 20-minute job limit, pinned Node/Motoko/PocketIC dependencies, synthetic-only inputs, no-DFX/no-wallet boundary, and artifact/log retention. It covers the fixture's low-cycle, interruption, authority/archive repair, EOP upgrade, and at-most-one valueless dispatch assertions, but an unrun workflow is not execution evidence. The workflow adds no target treasury Candid method, deployment, custody, signer, chain call, target data, generic storage API, or authoritative-collection claim. Measured capacity policy and actual chain-adapter at-most-one-value-transfer proof remain M1/G2/G3 work.

51. **IMPLEMENTED 2026-09-12 (migration-receipt intent contract only):** Added the pure `canisters/archive_router/MigrationReceiptIntent.mo` contract and vectors. It accepts only bounded printable logical/migration/table identifiers, a non-empty chunk of at most the catalogue's 500 ordinary records, and separate 32-byte payload/content hashes. A future authenticated importer must persist that immutable version/hash receipt intent before its fixed `migration_receipt_v1` write; an unknown reply is acknowledged only by the exact retained tuple, while absence permits only an identical retry and a changed version or hash is a conflict. The contract neither decodes chunks nor accesses PostgreSQL, credentials, source rows, ZenDB, an importer API, deployment, or target data. `mops test MigrationReceiptIntent`, `mops check archive_router` (including its stable check), and `git diff --check` pass. The isolated treasury proof artifacts were also rebuilt and SHA-256-verified locally, but PocketIC timed out during startup before canister creation, so no Treasury PocketIC execution result is claimed. A fixed archive-authority surface, authenticated importer journal, source-projection binding, duplicate/lost-reply/interruption/upgrade/low-cycle/repair-resume execution, and the disposable PostgreSQL rehearsal remain M1/G2 proof work.
52. **IMPLEMENTED 2026-09-12 (fixed embedded migration-receipt adapter):** Added the private `canisters/storage_authority/EmbeddedMigrationReceiptStore.mo` adapter and two fixed archive-principal-only `migration_receipt_v1` methods. It admits only the already bounded receipt metadata (logical ID, migration/table/chunk identity, row count, payload hash, version, and content hash), creates unique logical-ID and `(migrationId, sourceTable, chunk)` constraints, and returns only a two-record-bounded version/hash recovery tuple. The archive principal is checked in each method; unauthorized writes are blocked and unauthorized lookups return conflict without an existence signal. No caller can select a collection, document ID, filter, source row, source credential, importer principal, or payload, and no generic ZenDB/grant/configuration surface is exposed. `test/EmbeddedMigrationReceiptStore.test.mo` passes exact-duplicate, changed-version/hash, absent, malformed-hash, malformed-ID, and empty-chunk vectors; `mops check storage_authority` and `git diff --check` pass. This is an un-deployed fixed boundary, not an authenticated importer or recovery execution proof: durable importer journaling, source-projection binding, PocketIC duplicate/lost-reply/interruption/upgrade/low-cycle/repair-resume execution, and disposable PostgreSQL rehearsal remain required before G2.

53. **IMPLEMENTED 2026-09-12 (synthetic fixed migration-receipt recovery proof):** Added `fixtures/storage_authority/MigrationReceiptLostReply.mo` and its isolated build/run bridge. The disposable importer fixture retains one durable, validated receipt intent before its fixed archive-authority write; it deliberately loses a successful reply and, after EOP upgrades of both fixture and authority, acknowledges only an exact fixed tuple lookup. Duplicate delivery can write only the same retained tuple; a distinct pre-await interruption survives an importer EOP upgrade, observes `absent`, and can retry only through a no-input method that resends the original immutable migration/table/chunk/version/hash input. The PocketIC bridge uses synthetic principals, chunked SHA-256-verified fixture Wasms, and no DFX, identity, wallet, external network, PostgreSQL, source data, credentials, target importer API, or deployment. JavaScript and shell syntax checks, `mops test EmbeddedMigrationReceiptStore`, `mops check storage_authority`, and `git diff --check` pass. The two-Wasm build exceeded this workspace's interactive 30-second ceiling before publishing artifacts, so no local PocketIC execution pass is claimed. This remains a synthetic recovery proof only: authenticated importer policy, source-projection binding, collection-specific low-cycle/repair-resume proof, and disposable PostgreSQL base/delta rehearsal remain M1/G2 blockers.

54. **IMPLEMENTED 2026-09-12 (synthetic migration-receipt low-cycle and repair/resume extension):** The disposable importer now journals a validated immutable receipt intent, crosses only its fixed read-only tuple lookup checkpoint, and applies the shared one-trillion-cycle reserve guard before every archive-authority receipt write. The PocketIC bridge creates only that synthetic importer below the proof reserve, proves the authority record remains absent and recovery is `retryIdentical`, then replenishes only the fixture with PocketIC's test-only cycle API before allowing its no-input retry to resend the retained tuple and reconcile an exact acknowledgement. An operator-only no-input repair method reads only the durable journal and fixed lookup: absence may resend only the persisted migration/table/chunk/version/hash tuple, and presence is accepted only by exact tuple reconciliation. The bridge rejects outsider repair ingress and proves repair after a separate pre-await interruption and importer EOP upgrade. Focused migration-receipt intent, embedded-store, and cycle-reserve vectors; direct fixture type checking; `mops check storage_authority`; JavaScript/shell syntax; and `git diff --check` pass. `mops check-stable storage_authority` exceeded the workspace's 30-second interactive ceiling after toolchain startup, so no stable-check pass is claimed; the linked-Wasm build and PocketIC execution remain CI/external validation work. This remains a valueless synthetic proof: it adds no PostgreSQL access, source rows, credentials, target importer Candid method, deployment, target data, ZenDB grant/configuration API, or authoritative-collection claim. The disposable PostgreSQL base/delta rehearsal, authenticated importer/source-projection binding, measured capacity policy, and successful isolated execution remain M1/G2 blockers.

55. **IMPLEMENTED 2026-09-12 (synthetic migration-receipt recovery CI coverage):** `.github/workflows/m1-migration-receipt-lost-reply.yml` runs the isolated two-Wasm PocketIC proof for path-filtered pull requests and manual dispatches, with a 20-minute job limit, pinned Node/Motoko/PocketIC dependencies, synthetic-only inputs, no-DFX/no-wallet boundary, and artifact/log retention. It covers the fixture's low-cycle, interruption, repair, EOP upgrade, duplicate delivery, and exact tuple-only recovery assertions, but an unrun workflow is not execution evidence. The workflow adds no PostgreSQL access, source data, credential, target importer Candid method, deployment, target data, ZenDB grant/configuration API, or authoritative-collection claim. The disposable PostgreSQL base/delta rehearsal, authenticated importer/source-projection binding, measured capacity policy, and successful isolated execution remain M1/G2 blockers.

56. **IMPLEMENTED 2026-09-12 (synthetic migration-receipt local execution):** The isolated migration-receipt PocketIC runner now owns its disposable local server process and waits a bounded, configurable 30–120 seconds for the server port, instead of being limited by `pic-js-mops`' fixed 30-second startup poll. It kills the process and removes the temporary port directory on either startup failure or normal completion; it still uses only the already checksum-verified fixture Wasms, a byte-identical `/tmp` copy of the pinned PocketIC binary, and synthetic principals/canisters, with no DFX, identity, wallet, external network, PostgreSQL, source data, credential, target importer API, deployment, target data, or ZenDB grant/configuration API. On 2026-09-12, the builder completed and `run-migration-receipt-lost-reply-proof.sh` passed its low-cycle, no-input retry, lost-reply, duplicate-delivery, EOP-upgrade, interruption, outsider-denial, and repair assertions. `mops test EmbeddedMigrationReceiptStore`, `mops check storage_authority`, its stable check, JavaScript/shell syntax checks, and `git diff --check` also pass. This remains a valueless synthetic recovery proof, not an authenticated importer or authoritative collection claim. The disposable PostgreSQL base/delta rehearsal, authenticated importer/source-projection binding, and measured capacity policy remain M1/G2 blockers.

57. **IMPLEMENTED 2026-09-12 (synthetic treasury recovery execution):** GitHub Actions [run `34710316625`](https://github.com/vporton/meritocracy/actions/runs/34710316625) passed the isolated SHA-256-verified four-Wasm PocketIC proof on commit `0f5e947c17c6be6a26a2611757e74124f6131cb2`. It exercised the synthetic treasury's low-cycle guard and test-only replenishment, pre-await interruption, lost-reply and exact duplicate recovery, authority/archive repair, archive non-activation and exact-receipt activation across EOP upgrades, and tuple-only at-most-one valueless dispatch. The runner now uses strict Boolean and Nat assertions for `isActive` and dispatch-count results; the two prior runs (`34709306159` and `34709812341`) exposed and corrected only those runner assertion-type defects and are retained as negative-path CI evidence. The fixtures use only synthetic principals/canisters and contain no DFX, identity, wallet, network configuration, signer, destination, transaction, chain call, target data, deployment, custody, generic storage API, or authoritative-collection behavior. This closes the synthetic treasury execution-evidence gap only. Measured capacity policy and a real chain-adapter at-most-one-value-transfer proof remain M1/G2/G3 work.

58. **IMPLEMENTED 2026-09-13 (workflow completion-receipt replay contract only):** Added the pure `canisters/workflow/CompletionReceiptIntent.mo` contract and negative vectors. It admits only bounded printable logical, cycle, and operation identifiers plus an immutable version and 32-byte content hash; payloads, caller identity, provider responses, task inputs, and workflow execution remain outside the contract. A future workflow canister must retain the exact input before its fixed `workflow_completion_receipt_v1` write. A lost reply is acknowledged only by the retained version/hash tuple; absence permits only identical redelivery and changed version/hash fails closed. `mops test CompletionReceiptIntent` and `git diff --check` pass. This adds no workflow Candid method, storage-authority mutation, target data, deployment, or authoritative-collection claim. A fixed workflow-only adapter, durable caller journal, duplicate/lost-reply/interruption/upgrade/low-cycle/repair-resume execution, and measured capacity policy remain M1/G2 blockers.

59. **IMPLEMENTED 2026-09-13 (fixed embedded workflow-completion receipt adapter):** Added the private `canisters/storage_authority/EmbeddedWorkflowCompletionReceiptStore.mo` adapter and two fixed workflow-principal-only `workflow_completion_receipt_v1` methods. It accepts only the bounded completion logical ID, cycle ID, operation name, desired version, and content hash; unique logical-ID and `(cycleId, operationName)` constraints fail closed, while an exact version/hash replay is acknowledged. The recovery lookup is bounded to two records and returns only the version/hash tuple; unauthorized writes return `blocked` and unauthorized lookups return `conflict` without exposing receipt existence. No caller can select a collection, document ID, filter, workflow payload, task input, provider response, identity, or configuration, and no generic ZenDB/grant API is exposed. The generated Candid baseline records the additive fixed methods. All 18 `mops test` vectors, `mops check storage_authority`, backward Candid compatibility against the prior baseline, and `git diff --check` pass; the stable check exceeds this workspace's interactive command ceiling and is not claimed. This is an un-deployed fixed boundary, not a workflow canister, durable workflow journal, recovery-execution proof, target data, deployment, or authoritative-collection claim. Synthetic duplicate/lost-reply/interruption/EOP-upgrade/low-cycle/repair-resume execution and measured capacity policy remain M1/G2 blockers.

60. **IMPLEMENTED 2026-09-13 (workflow completion-receipt local-runner recovery boundary):** The isolated workflow-completion-receipt PocketIC runner now starts and owns its disposable local server directly, waiting only a configurable 30–120 seconds for the port-file handshake rather than using `pic-js-mops`' fixed startup helper. It kills the server and removes its temporary port directory on startup failure or normal completion. The runner continues to use only checksum-verified disposable Wasms, a byte-identical `/tmp` copy of the pinned PocketIC binary, synthetic principals/canisters, and the fixed bounded receipt tuple; it contains no DFX, identity, wallet, external network, task input, provider response, target workflow endpoint, deployment, target data, or generic storage/grant API. JavaScript and shell syntax checks, `mops test CompletionReceiptIntent`, `mops test EmbeddedWorkflowCompletionReceiptStore`, and `mops check storage_authority` pass. The linked-Wasm build still exceeds this workspace's interactive 30-second ceiling before an execution result can be collected, so no local PocketIC pass is claimed; the existing 15-minute path-filtered CI job remains the execution evidence route. This remains a valueless synthetic recovery proof, not an authoritative collection or workflow implementation; a target workflow caller and measured capacity policy remain M1/G2 blockers.

61. **IMPLEMENTED 2026-09-13 (synthetic workflow completion-receipt local execution):** The isolated build completed, SHA-256-verified the disposable authority and workflow Wasms, and `scripts/icp/run-workflow-completion-receipt-lost-reply-proof.sh` passed locally. The proof exercised the fixture's below-reserve no-write/`retryIdentical` result, test-only cycle replenishment and no-input retry, deliberately lost reply, exact duplicate delivery, pre-await interruption, operator-only repair, outsider denials, and EOP upgrades of the workflow and authority before exact-tuple reconciliation. Its runner owns and removes its temporary PocketIC server and byte-identical writable PocketIC binary copy. Focused receipt/store vectors, the storage-authority type and stable-compatibility checks, and `git diff --check` also pass. It uses only synthetic principals/canisters and carries no DFX, identity, wallet, external network, task input, provider response, target workflow endpoint, deployment, target data, or generic storage/grant API. This closes the synthetic workflow execution-evidence gap only; it is not a target workflow or authoritative-collection claim. A target workflow caller and measured capacity policy remain M1/G2 blockers.

62. **IMPLEMENTED 2026-09-16 (read-only inventory PostgreSQL catalog correction):** The M1 aggregate-only inventory query now selects `pg_sequences.sequencename AS sequence_name` and orders by `sequencename`; `sequence_name` is an `information_schema.sequences` field and does not exist in PostgreSQL's `pg_sequences` view. `backend/tests/icp-postgres-inventory.test.ts` locks this catalog contract without connecting to a database. The focused test, `npm run build --workspace backend`, and `git diff --check` pass. The command retains its `SERIALIZABLE, READ ONLY, DEFERRABLE` transaction and read-only startup default, does not log connection details or values, and performs no data/DDL/replication mutation. A successful DBA-approved aggregate inventory report is still required M1 evidence.

63. **IMPLEMENTED 2026-09-16 (read-only inventory PostgreSQL `name[]` decoding correction and execution):** PostgreSQL catalog `array_agg(name)` values for foreign-key and unique-index columns are now explicitly cast to `text[]`. The Node `pg` client decoded the native `name[]` values as strings, causing the command's intentional key-shape validation to fail even though all 22 required tables were present (alongside one unrelated physical table). The explicit casts retain the same catalog-only metadata, fail-closed empty/mismatched-key checks, and query bounds; they select no row values and add no mutation or replication capability. The focused inventory test, backend TypeScript build, and diff check pass. The unchanged command then completed using the operator-approved account and wrote a private mode-`0600` aggregate report for all required tables; it is not versioned. No data/DDL/replication mutation, export, target write, or authority change occurred.

64. **IMPLEMENTED 2026-09-16 (fixed workflow-receipt capacity-proof boundary):** Added an isolated PocketIC capacity runner for the existing real `storage_authority` fixed `workflow_completion_receipt_v1` methods and its disposable workflow caller. It exercises 16 expected and 32 two-times-expected synthetic immutable receipt writes; every write deliberately loses its reply and is then acknowledged only through the bounded exact version/hash lookup. It records Candid input bytes plus separate workflow/authority PocketIC cycle deltas in a mode-`0600` CI artifact, and rejects a 513-byte logical ID before it can create a journal or reach storage. The runner verifies existing Wasm hashes, copies the pinned PocketIC binary byte-for-byte to a disposable writable path, and uses only synthetic principals/canisters; it has no DFX, identity, wallet, external network, workflow payload, provider response, target data, generic storage API, or deployment path. The existing path-filtered 20-minute workflow executes this capacity proof after its recovery proof and retains the report. Local execution is **not claimed**: this workspace prevents PocketIC from binding its loopback server (exit 101 before canister creation). PocketIC cycle deltas are emulator measurements, not a production instruction/cycle budget; capacity proof remains required for every other fixed collection before G2.

65. **IMPLEMENTED 2026-09-16 (fixed core identity/role capacity-proof boundary):** Added an isolated PocketIC capacity runner for the real `storage_authority` fixed principal-binding and role-assignment methods through its disposable core caller. It executes 16 expected and 32 two-times-expected immutable binding writes and the same number of immutable role writes; every write deliberately loses its reply and is acknowledged only through the corresponding bounded exact version/hash lookup. A 513-byte logical ID for either binding or role is rejected before a core journal or storage call. A CI run exposed that this rejection assertion was incorrectly placed after successful writes, where it reconciled a previous journal; the assertion now runs first against the fresh fixture state. The private mode-`0600` CI report records encoded Candid-input totals and separate core/authority PocketIC cycle deltas. The runner SHA-256-verifies existing disposable Wasms, copies the pinned PocketIC binary byte-for-byte to a writable temporary path, and uses only synthetic principals/canisters. It has no DFX, identity, wallet, external network, OAuth/provider data, target data, generic storage API, or deployment path. The existing 20-minute core recovery workflow runs this capacity proof after recovery and retains the report. Local execution is not claimed: this workspace's interactive ceiling ended the runner after checksum verification and before it could report a PocketIC result. Emulator deltas are not a production instruction/cycle budget; the treasury, migration, workflow-domain, and all remaining fixed collection capacity proofs remain required before G2.

66. **IMPLEMENTED 2026-09-16 (fixed treasury payment-operation capacity-proof boundary):** Added an isolated PocketIC capacity runner for the real `storage_authority` fixed `treasury_payment_operation_v1` methods through its disposable valueless treasury fixture. It executes 16 expected and 32 two-times-expected immutable payment-operation writes; each deliberately loses its reply and can be acknowledged only through the fixed bounded exact version/hash lookup. A 513-byte logical ID is rejected before a treasury journal or authority call. The same fresh-fixture ordering correction as the core runner is applied, so the rejection cannot reconcile a previous successful journal. The mode-`0600` CI report records encoded Candid-input bytes and separate treasury/authority PocketIC cycle deltas. The runner SHA-256-verifies disposable Wasms, copies the pinned PocketIC binary byte-for-byte to a writable temporary path, and uses only synthetic principals/canisters; it has no DFX, wallet, identity, external network, destination address, signer, transaction material, chain call, target data, generic storage API, deployment, custody, or authority path. The existing path-filtered 20-minute treasury recovery workflow now runs it after recovery and retains the report. Local execution is not claimed: the linked fixture artifacts built, but PocketIC exits `101` before opening its disposable loopback server in this workspace. Emulator deltas are not a production instruction/cycle budget; the migration, workflow-domain, and all remaining fixed collection capacity proofs remain required before G2.

67. **IMPLEMENTED 2026-09-16 (fixed migration-receipt capacity-proof boundary):** Added an isolated PocketIC capacity runner for the real `storage_authority` fixed `migration_receipt_v1` methods through the existing disposable synthetic importer. It exercises 16 expected and 32 two-times-expected immutable receipt writes; every write deliberately loses its reply and is then acknowledged only through the bounded exact version/hash lookup. A 513-byte logical ID is rejected before it can create an importer journal or reach storage. The runner emits a private mode-`0600` CI report with encoded Candid-input totals and separate importer/authority PocketIC cycle deltas. It SHA-256-verifies existing disposable Wasms, copies the pinned PocketIC binary byte-for-byte to a temporary writable path, and uses only synthetic principals/canisters; it has no DFX, PostgreSQL, source data, credentials, identity, wallet, external network, importer API, target data, generic storage API, or deployment path. The existing path-filtered 20-minute migration-receipt recovery workflow now runs this capacity proof after recovery and retains the report. Local execution is not claimed: the linked fixture build/run did not complete within this workspace's interactive command ceiling. PocketIC cycle deltas are emulator measurements, not a production instruction/cycle budget; authenticated importer/source-projection binding, the disposable PostgreSQL base/delta rehearsal, and capacity proofs for all remaining fixed collections remain required before G2.

68. **IMPLEMENTED 2026-09-16 (treasury-journal immutable recovery contract only):** Added pure `canisters/treasury/TreasuryJournalIntent.mo` and focused vectors. It accepts only a bounded immutable logical ID, nonzero journal sequence, bounded operation/account/asset identifiers, debit-or-credit direction, positive integer base-unit amount, explicit bounded asset decimals, desired version, and a 32-byte content hash. A future treasury actor must retain that exact entry before a fixed journal write; a lost reply is acknowledged only by the retained version/hash tuple, absence permits only that retained input to retry, and a changed version conflicts. Zero sequence/amount, oversized decimals, control characters, and malformed hashes cannot become an intent. `StorageTypes.mo` now describes an immutable `TreasuryJournalEntryV1`; no mutable balance is authoritative. `mops test TreasuryJournalIntent` and `git diff --check` pass. This adds no actor state or Candid method, storage-authority write, account balance, ledger/chain call, signer, destination, target data, deployment, or authority claim. A fixed journal collection, bounded balanced-set saga, lost-reply/interruption/upgrade/low-cycle/repair/capacity proof, and independent reconciliation remain M1/G2/G3 blockers.
69. **IMPLEMENTED 2026-09-16 (fixed embedded treasury-journal boundary):** Added the private `EmbeddedTreasuryJournalStore` adapter and the exact treasury-only `writeTreasuryJournalEntry`/`lookupTreasuryJournalEntry` surface for `treasury_journal_v1`. It admits only a validated immutable journal tuple, unique logical ID and journal sequence, and a two-record-bounded tuple-only recovery lookup; an exact version/hash replay is acknowledged and every changed or malformed candidate fails closed. Method-body authorization binds calls to the persisted treasury principal; unauthorized writes return `blocked` and lookups return `conflict` without revealing entry existence. The collection carries no mutable balance, destination, signer, transaction material, chain receipt, caller-selected collection/action/filter, or ZenDB grant/configuration API. Focused adapter and intent vectors, the storage-authority type check, generated Candid validation, and diff check pass. This is an un-deployed collection boundary only: no treasury caller, durable balanced-set saga, archive path, chain behavior, low-cycle/repair/capacity proof, target data, deployment, or authoritative-collection claim exists; those remain M1/G2/G3 blockers.
70. **IMPLEMENTED 2026-09-16 (synthetic treasury-journal lost-reply recovery boundary):** Added a disposable persistent treasury fixture and an isolated PocketIC runner for the real fixed `treasury_journal_v1` authority methods. The fixture retains one validated immutable journal tuple before every authority await; lost replies, exact duplicate delivery, interruption before the call, operator-only no-input repair, and EOP upgrades reconcile only through the bounded logical-ID/version/hash lookup. Browser/direct authority access and outsider recovery/repair are denied. It has no balance calculation, balanced-set activation, archive, destination, signer, transaction material, chain call, asset movement, target data, deployment, DFX identity, wallet, or generic storage API. The path-filtered 20-minute CI workflow builds hash-verified disposable Wasms, uses only synthetic principals/canisters and a writable byte-identical PocketIC binary copy, and retains logs. Local PocketIC execution is not claimed; this workspace's loopback restriction remains an execution limitation. This is a valueless single-entry recovery proof only: fixed journal capacity/low-cycle evidence, a bounded balanced-set saga, archive/reconciliation, and independent accounting reconciliation remain M1/G2/G3 blockers.
71. **IMPLEMENTED 2026-09-16 (fixed treasury-journal capacity-proof boundary):** Added an isolated PocketIC capacity runner for the real `storage_authority` fixed `treasury_journal_v1` methods through the disposable valueless treasury fixture. It executes 16 expected and 32 two-times-expected immutable debit-journal writes; every write deliberately loses its reply and is then acknowledged only through the bounded exact version/hash lookup. A 513-byte logical ID is rejected before it can create a treasury journal or reach storage. The runner emits a private mode-`0600` CI report with encoded Candid-input totals and separate treasury/authority PocketIC cycle deltas. It SHA-256-verifies existing disposable Wasms, copies the pinned PocketIC binary byte-for-byte to a temporary writable path, and uses only synthetic principals/canisters; it has no DFX, identity, wallet, external network, balance mutation, destination, signer, transaction material, chain call, target data, generic storage API, or deployment path. The existing path-filtered 20-minute treasury-journal recovery workflow now runs this capacity proof after recovery and retains the report. Local execution is not claimed: this workspace prevents PocketIC from binding its loopback server. PocketIC cycle deltas are emulator measurements, not a production instruction/cycle budget; a low-cycle write guard, bounded balanced-set saga, archive/reconciliation, and independent accounting reconciliation remain M1/G2/G3 blockers.
72. **IMPLEMENTED 2026-09-16 (synthetic treasury-journal low-cycle recovery boundary):** The disposable persistent treasury-journal fixture now retains its validated immutable tuple, crosses only a fixed tuple-free authority lookup checkpoint, and applies the one-trillion-cycle synthetic reserve before every authority write, including no-input retry and repair. The PocketIC bridge creates only that fixture below reserve, requires the guarded write to trap with its exact lookup still `absent`/`retryIdentical`, replenishes only the disposable fixture through PocketIC's test-only cycle API, then requires the no-input retry to lose its reply and reconcile only by the same logical-ID/version/hash tuple. Focused journal-intent and embedded-store vectors, storage-authority type checking, JavaScript and shell syntax checks, the checksum-published fixture build, and `git diff --check` pass. PocketIC execution is not claimed locally because this workspace prevents its loopback server from binding; the existing path-filtered 20-minute treasury-journal workflow executes the extended proof in CI. This is a valueless synthetic guard, not a production cycle budget, public cycle interface, balanced-set saga, archive/reconciliation, account balance, signer, chain call, target data, deployment, or authority claim. Measured collection-specific budgets, alerts, repair policy, bounded balanced-set activation, archive/reconciliation, and independent accounting reconciliation remain M1/G2/G3 blockers.

73. **IMPLEMENTED 2026-09-16 (bounded treasury-journal balanced-set contract only):** Added pure `canisters/treasury/TreasuryJournalBalancedSet.mo` and focused vectors. It accepts only a bounded two-to-sixteen-entry immutable posting set with a bounded set ID; every entry must pass the existing journal contract, use one operation and asset/decimal denomination, have unique logical ID and journal sequence, and make positive debit and credit base-unit totals exactly equal. It is not a mutable balance projection. The set can activate only when each positionally retained entry receives its own exact version/hash acknowledgement; absence keeps the entire set pending, and any altered tuple conflicts. Unequal totals, duplicate sequence, cross-operation, undersized, and incomplete-observation candidates fail closed. `mops test TreasuryJournalBalancedSet`, `mops check treasury_canister` (including its stable check), and `git diff --check` pass. This adds no actor state, storage-authority mutation, archive, account balance, signer, destination, transaction material, ledger/chain call, target data, deployment, or authority claim. A persistent bounded balanced-set saga with interruption/upgrade/low-cycle/repair/capacity proof, archive/reconciliation, and independent accounting reconciliation remains M1/G2/G3 blocker work.

74. **IMPLEMENTED 2026-09-16 (durable treasury-journal balanced-set transition contract only):** Added pure `canisters/treasury/TreasuryJournalBalancedSetSaga.mo` and focused vectors. A future persistent treasury actor can construct its durable state only from a valid bounded balanced set; it retains every immutable posting before any authority await, permits a write/retry only for that retained posting, and refuses a caller-supplied substitute. Individual lost replies reconcile exclusively from a bounded version/hash observation; an absent record permits the same retained tuple to retry, while a changed tuple conflicts the entire set. The state becomes active only after every retained posting is exactly acknowledged, so one debit or credit cannot make a partial set authoritative. `mops test TreasuryJournalBalancedSetSaga`, `mops check treasury_canister`, `mops check-stable treasury_canister`, and `git diff --check` pass. This adds no actor state, storage-authority mutation, balance projection, archive, account, signer, destination, transaction material, ledger/chain call, target data, deployment, or authority claim. A persistent actor/fixture proof across interruption, EOP upgrade, low-cycle, operator repair, capacity, archive/reconciliation, and independent accounting reconciliation remains M1/G2/G3 blocker work.

75. **IMPLEMENTED 2026-09-16 (synthetic persistent treasury-journal balanced-set recovery fixture):** Extended the disposable treasury-journal fixture with one separately retained two-to-sixteen-entry balanced set. It validates and persists the entire immutable set before its first authority await; thereafter each write/retry accepts only an index into the retained set and has no posting input. Per-entry reconciliation reads only the fixed tuple-only authority lookup; a partial exact acknowledgement remains `pending`, while the second exact acknowledgement activates the set. The PocketIC bridge now prepares a debit/credit pair, deliberately loses the debit reply, verifies the partial set remains pending across fixture EOP upgrade, then loses/reconciles the retained credit tuple and requires `active`. Direct/outsider preparation remains denied. `mops test TreasuryJournalBalancedSetSaga`, `mops check storage_authority`, a direct enhanced-orthogonal-persistence fixture type check, JavaScript and shell syntax checks, and `git diff --check` pass. The linked fixture build exceeded the interactive 30-second ceiling before artifacts were published, so no local PocketIC execution pass is claimed; the existing isolated CI workflow must execute the extended proof. This remains valueless and synthetic: it adds no balance projection, archive/reconciliation, signer, destination, ledger/chain call, target data, deployment, or authority claim. Full balanced-set interruption, low-cycle, repair, capacity, archive/reconciliation, and independent accounting proof remain M1/G2/G3 blockers.

76. **IMPLEMENTED 2026-09-16 (synthetic balanced-set interruption and repair extension):** The same disposable fixture now persists a selected member's remote-write phase and deliberately traps before its authority await. After a fixture EOP upgrade, its bounded lookup must report the retained tuple absent; only the operator-only, index-only repair method may then write that already retained tuple, deliberately lose the reply, and reconcile it by the exact tuple. An outsider cannot invoke repair, and the repaired debit leaves the bounded set pending until the separately retained credit is exactly acknowledged. The bridge contains no posting replacement input, balance projection, archive, signer, destination, transaction material, chain call, target data, deployment, or authority claim. `mops test TreasuryJournalBalancedSetSaga`, `mops check storage_authority`, JavaScript and shell syntax checks, and `git diff --check` pass. The direct linked-Wasm build exceeded the interactive 30-second ceiling before artifacts were published, so no local PocketIC execution pass is claimed; the existing isolated CI workflow remains required. Balanced-set low-cycle and capacity proof, archive/reconciliation, and independent accounting reconciliation remain M1/G2/G3 blockers.

77. **IMPLEMENTED 2026-09-16 (synthetic balanced-set low-cycle recovery extension):** The existing isolated treasury-journal PocketIC bridge now creates a second disposable authority/fixture pair whose fixed allowlist admits only that fixture. It retains a two-entry debit/credit set below the one-trillion-cycle synthetic reserve, requires the first retained debit write to trap after its tuple-only checkpoint with the authority record absent, and keeps the set pending. Only PocketIC's test-only replenishment of that separate fixture permits its index-only, no-input retry; its lost reply must reconcile solely through the exact retained version/hash lookup. The set remains pending after the replenished debit and activates only when the separately retained credit is also exactly acknowledged. This adds no public cycle interface, operational cycle budget, balance projection, archive, signer, destination, transaction material, ledger/chain call, target data, deployment, generic storage API, or authority claim. `node --check scripts/icp/treasury-journal-lost-reply-pocketic-proof.cjs`, `mops test TreasuryJournalBalancedSetSaga`, `mops check storage_authority`, and `git diff --check` pass. The linked-Wasm/PocketIC execution remains CI evidence because this workspace cannot complete it interactively. Balanced-set capacity, archive/reconciliation, and independent accounting reconciliation remain M1/G2/G3 blockers.

78. **IMPLEMENTED 2026-09-16 (bounded balanced-set capacity-proof extension):** The existing isolated treasury-journal capacity runner now exercises one maximum 16-entry retained debit/credit set alongside each 16 expected and 32 two-times-expected fixed journal workload. It rejects a 17-entry set before durable set retention or storage, then deliberately loses and exactly reconciles every retained member only through its bounded version/hash lookup; the set becomes `active` only after all 16 exact acknowledgements. The private mode-`0600` CI report records the combined encoded Candid-input totals, write/lookup counts, maximum set size, and separate fixture/authority PocketIC cycle deltas. `node --check scripts/icp/treasury-journal-capacity-pocketic-proof.cjs`, `mops test TreasuryJournalBalancedSetSaga`, `mops check storage_authority`, and `git diff --check` pass. Local PocketIC execution is not claimed because this workspace prevents its loopback server from binding; the existing path-filtered 20-minute workflow executes the extension in CI. This is a valueless synthetic capacity boundary: it adds no balance projection, archive/reconciliation, signer, destination, transaction material, chain call, target data, deployment, generic storage API, or authority claim. Emulator deltas are not a production instruction/cycle budget; archive/reconciliation and independent accounting reconciliation remain M1/G2/G3 blockers.

79. **IMPLEMENTED 2026-09-18 (treasury-journal archive acknowledgement contract only):** Added pure `canisters/treasury/TreasuryJournalArchiveRecovery.mo` and focused vectors. A future durable archive saga can retain only one bounded journal-set logical-ID/version/32-byte-content-hash tuple before an archive await; a missing or competing receipt remains pending, and malformed retained or observed tuples fail closed. An exact receipt is an archival acknowledgement only: it cannot activate, delete, replace, or project a journal set, and the contract carries no balance, account, asset, amount, destination, signer, transaction, or chain-receipt material. `mops test TreasuryJournalArchiveRecovery` and `git diff --check` pass. This adds no actor state, storage-authority method, archive caller, persistent archive, reconciliation projection, target data, deployment, custody, or authority claim. A durable archive saga with lost-reply/interruption/upgrade/low-cycle/repair/capacity proofs, immutable archive export, and independent accounting reconciliation remains M1/G2/G3 blocker work.

80. **IMPLEMENTED 2026-09-18 (durable treasury-journal archive transition contract only):** Added pure `canisters/treasury/TreasuryJournalArchiveSaga.mo` and focused vectors. A future persistent treasury actor can retain exactly one valid journal-set archive tuple before its first archive await, begin or retry only that retained tuple, and reconcile an unknown reply solely against an exact receipt. A missing or competing receipt remains pending, while malformed receipt data blocks recovery; acknowledgement is terminal and cannot activate, delete, replace, or project the balanced journal set. `mops test TreasuryJournalArchiveSaga` and `git diff --check` pass. This adds no actor state, archive caller/storage, immutable export, balance projection, account, asset, amount, signer, destination, transaction/chain material, target data, deployment, custody, or authority claim. A persistent fixture proof across lost reply, interruption, EOP upgrade, low-cycle, operator repair, capacity, immutable archive export, and independent accounting reconciliation remains M1/G2/G3 blocker work.

81. **IMPLEMENTED 2026-09-18 (synthetic treasury-journal archive lost-reply/recovery fixture):** Extended the disposable persistent treasury-journal fixture with one tuple-only archive state and added a separately deployed, fixed-ID synthetic archive sink. An archive tuple can be retained only after its bounded balanced journal set is already `active`; it must name that retained set, and no later method accepts a replacement tuple or posting. The proof exercises archive unavailability, deliberately lost archive acknowledgement, interruption before the archive await, operator-only tuple-free repair, outsider rejection, and EOP upgrades of the fixture and archive sink. Missing receipts remain pending; only the sink's exact logical-ID/version/hash receipt reaches `acknowledged`. The acknowledgement never changes the balanced-set phase and the boundary contains no archive export, journal entry payload, balance, account, asset, amount, destination, signer, transaction material, chain call, target data, deployment, or authority claim. Direct enhanced-orthogonal-persistence compilation of both disposable actors, `mops test TreasuryJournalArchiveRecovery`, `mops test TreasuryJournalArchiveSaga`, `mops check storage_authority`, JavaScript syntax checks, and `git diff --check` pass. The linked disposable fixture build still exceeds this workspace's interactive command ceiling, so no local PocketIC result is claimed; the path-filtered 20-minute CI workflow builds and executes the extended proof. Archive low-cycle and capacity evidence, immutable archive export, and independent accounting reconciliation remain M1/G2/G3 blocker work.

82. **IMPLEMENTED 2026-09-18 (tuple-only treasury-journal archive capacity extension):** Archive delivery now applies the existing proof-only one-trillion-cycle reserve after retaining the exact archive tuple and before either first archive dispatch or no-input repair dispatch, so a depleted fixture cannot make an unjournaled archive call. The isolated capacity runner now deploys the fixed-ID synthetic archive sink and, after each separately measured 16 expected or 32 two-times-expected journal workload plus one active maximum 16-entry balanced set, retains one matching set-ID/version/32-byte-hash archive tuple, deliberately loses its acknowledgement, and accepts it only through the sink's exact lookup. Its private mode-`0600` report adds the archive tuple/input and lookup counts to the existing encoded input and fixture/authority emulator cycle measurements. The sink accepts only the two fixed capacity set IDs, has a two-receipt bound, and carries no posting, balance, account, asset, amount, destination, signer, transaction, chain, target data, deployment, generic storage API, or authority behavior. `mops test TreasuryJournalArchiveRecovery`, `mops test TreasuryJournalArchiveSaga`, `mops test TreasuryJournalBalancedSetSaga`, `mops check storage_authority`, direct EOP fixture compilation, JavaScript/shell syntax checks, and `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot provide its loopback server; the existing path-filtered 20-minute workflow runs the checksum-verified capacity extension. This does not establish a production cycle budget, archive export, or independent accounting reconciliation; archive low-cycle execution evidence and those M1/G2/G3 blockers remain.

83. **IMPLEMENTED 2026-09-18 (synthetic treasury-journal archive low-cycle recovery extension):** The isolated recovery bridge now creates a third disposable fixture below the existing one-trillion-cycle proof reserve and a separate fixed-ID archive sink. Its constructor accepts only one already-valid, bounded debit/credit balanced set, fixed at installation; it exposes no journal creation or replacement route. After the fixture retains the matching archive logical-ID/version/32-byte-hash tuple, its first archive dispatch is rejected below reserve, the sink has no receipt, and tuple-only reconciliation remains pending. Only PocketIC's test-only replenishment of that fixture permits the operator-only, no-input repair to dispatch the already retained tuple; its deliberately lost reply is acknowledged only through the exact sink receipt lookup. The isolated sink admits only that fixture and a fixed bounded receipt; this path carries no balance projection, archive export, account, asset, amount, destination, signer, transaction material, chain call, target data, deployment, generic storage API, or authority claim. `mops test TreasuryJournalArchiveRecovery`, `mops test TreasuryJournalArchiveSaga`, `mops test TreasuryJournalBalancedSetSaga`, `mops check storage_authority`, direct fixture type checking, JavaScript syntax, and `git diff --check` pass. The linked release-Wasm build and PocketIC execution remain CI evidence because this workspace cannot provide its loopback server within the interactive ceiling. This is a proof-only reserve guard, not a production cycle budget; immutable archive export and independent accounting reconciliation remain M1/G2/G3 blockers.
84. **IMPLEMENTED 2026-09-18 (canonical treasury-journal archive export contract only):** Added pure `canisters/treasury/TreasuryJournalArchiveExport.mo` and focused vectors. It emits a bounded, self-contained fixed length-prefixed UTF-8 envelope with a fixed format marker and the already-valid immutable balanced set in its retained order; equal inputs encode byte-identically, while a reordered set produces distinct bytes. The tuple guard permits a valid archive acknowledgement tuple to name only that set. This is a codec boundary only: it has no actor state, archive transport/storage, hash implementation, balance projection, account, signer, destination, transaction/chain material, target data, deployment, custody, or authority claim. A durable immutable archive sink/export saga must still bind the approved SHA-256 tuple to these bytes and prove lost-reply, interruption, upgrade, low-cycle, repair, capacity, and independent accounting reconciliation before M1/G2/G3 completion.

85. **IMPLEMENTED 2026-09-18 (canonical treasury-journal archive SHA-256 binding contract only):** Added pure `canisters/treasury/TreasuryJournalArchiveExportBinding.mo` and focused vectors. It derives the archive receipt's exactly-32-byte SHA-256 content hash from the already-bounded canonical export bytes and fixes the set logical ID plus an explicit nonzero archive version in one binding; it never accepts a caller-supplied hash. Re-validation re-derives both bytes and hash, so altered hash bytes, altered export bytes, and reordered postings fail closed. `mops test TreasuryJournalArchiveExportBinding` passes. This adds no actor state, archive caller/sink/storage, immutable export delivery, balance projection, account, asset, amount, signer, destination, transaction/chain material, target data, deployment, custody, or authority claim. A durable archive sink/export saga must still retain this binding before dispatch and prove lost-reply, interruption, EOP upgrade, low-cycle, operator repair, capacity, immutable receipt/export retention, and independent accounting reconciliation before M1/G2/G3 completion.

86. **IMPLEMENTED 2026-09-18 (canonical treasury-journal archive export transition contract only):** Added pure `canisters/treasury/TreasuryJournalArchiveExportSaga.mo` and focused vectors. A future persistent archive owner can derive and retain exactly one canonical export binding (fixed set ID, nonzero version, derived SHA-256 tuple, and bytes) before its first archive await. Every dispatch, reconciliation, and tuple-free retry revalidates that binding against the immutable retained balanced set; altered durable bytes, hash, or posting order fail closed. Missing or competing receipts remain pending, and an exact receipt only acknowledges archival delivery—it cannot activate, delete, replace, or project the journal set. `mops test TreasuryJournalArchiveExportSaga` passes. This adds no actor state, archive caller/sink/storage, export delivery, balance projection, account, asset, amount, destination, signer, transaction/chain material, target data, deployment, custody, or authority claim. A persistent fixture must retain this binding before dispatch and prove lost-reply, interruption, EOP upgrade, low-cycle, operator repair, capacity, immutable receipt/export retention, and independent accounting reconciliation before M1/G2/G3 completion.

87. **IMPLEMENTED 2026-09-19 (approved application-canister consolidation scaffold):** Replaced the DFX/Mops deployment manifests' `core_canister`, `workflow_canister`, `archive_router`, `storage_authority`, and `oauth_fixture` canister entries with one `application_canister`; `oauth_fixture` and the former actor sources remain test-only modules. The new persistent application actor privately imports the existing core identity/role, workflow completion, migration-receipt, and shared storage contracts, exposes an empty Candid service, and creates no state, generic storage API, grant matrix, target data, deployment, or authority. `treasury_canister` remains the sole separately built Motoko domain canister. `mops check`, `mops check-stable`, `mops build`, and `mops test` pass (two built canisters; 27 test files). This is a reversible M1 topology change only: the M5 frontend assets canister remains blocked, and actual private collection migration, actor authorization, application-to-treasury saga, capacity, upgrade, and recovery proofs remain required before G2.

88. **IMPLEMENTED 2026-09-19 (treasury storage co-location scaffold):** Promoted the treasury-specific payment-operation and immutable journal collection implementations into `canisters/treasury/EmbeddedPaymentOperationStore.mo` and `canisters/treasury/EmbeddedTreasuryJournalStore.mo`; their former storage-authority copies remain only to preserve historical isolated-fixture evidence. The unified treasury actor now owns one private embedded ZenDB stable store and opens those two fixed collections locally; it makes no call to `storage_authority` and exposes no Candid storage/payment/journal method, signer, transfer, destination, balance, or target data. The corresponding store vectors now import the treasury-owned modules and pass. `mops check treasury_canister` passes. The changed treasury stable signature has not completed under this workspace's 30-second command ceiling, so no stable-compatibility or build pass is claimed for this change; it must be regenerated and reviewed before any upgrade or G2 claim. This remains un-deployed M1 scaffolding only.

89. **IMPLEMENTED 2026-09-19 (application storage co-location scaffold):** Promoted the fixed principal-binding/role, workflow-completion-receipt, and migration-receipt embedded collection implementations into `canisters/application/`; their storage-authority copies remain only for historical isolated-fixture evidence. The persistent consolidated application actor owns one private embedded ZenDB stable store and opens those four fixed collections locally, reopening rather than recreating them after enhanced orthogonal persistence upgrade. It exposes no Candid storage, identity, role, workflow, migration, grant, or configuration method; no caller can select a collection/action, and no target data, import capability, OAuth flow, or authority is enabled. The focused receipt vectors now import the application-owned modules. `mops test` passes all 27 test files, `mops check application_canister` passes, and `git diff --check` passes. The changed application stable signature and release build did not complete within this workspace's 30-second command ceiling, so no stable-compatibility, build, deployment, collection-mutation, or authoritative-store claim is made. Regenerate/review the baseline and prove application authorization, bounded mutation, interruption/upgrade/low-cycle/repair, capacity, archive/export, and migration rehearsal before G2.

90. **IMPLEMENTED 2026-09-19 (consolidated target-canister validation coverage):** Added `.github/workflows/m1-consolidated-canister-validation.yml`, a read-only 20-minute pull-request/manual CI job scoped to the consolidated application and treasury implementation, their private module dependencies, and pinned Motoko manifests. From the lock-pinned Mops CLI it installs only locked dependencies, checks both target actors (including their committed empty-actor stable baselines and Candid compatibility), builds their release Wasm artifacts, and runs the Motoko contract vectors. It retains only generated validation artifacts for 14 days. The workflow does not invoke DFX, PocketIC, a network replica, a canister call, deployment, data import, a wallet, an identity, a signer, an external provider, or a production system. It is validation coverage only; no successful remote workflow execution is claimed until its run artifact is reviewed, and no baseline is promoted because neither target actor has been deployed. Application authorization, bounded private mutation, interruption/upgrade/low-cycle/repair, capacity, archive/export, and migration rehearsal remain M1/G2 blockers.

91. **IMPLEMENTED 2026-09-19 (caller-bound application authorization contract only):** Added the pure `canisters/application/CallerAuthorization.mo` contract and focused vectors. A future application public method can permit a self-scoped resource or fixed service caller only when its authenticated Candid `caller` exactly equals a non-anonymous resource/configuration principal; anonymous callers fail closed and different non-anonymous principals are forbidden. The contract is only a reusable M1 invariant: it does not choose production principals, roles, capabilities, governance, OAuth, or public application methods, all of which remain G2 decisions. The consolidated application imports the decision type solely as a compile-time contract anchor and still exposes its empty Candid service; there is no state mutation, collection read/write, import, payment, treasury call, target data, deployment, or authority claim. `mops test CallerAuthorization` passes and application type checking emitted its success marker before this workspace's interactive command ceiling. Bounded private mutation, durable recovery, capacity, archive/export, and migration rehearsal remain M1/G2 blockers.

92. **IMPLEMENTED 2026-09-19 (consolidated application embedded identity/role proof boundary):** Added the disposable `fixtures/application/EmbeddedIdentityRoleStoreProof.mo`, which imports the current application-private adapter and mirrors the target actor's initialized-flag create/reopen discipline. Its two fixture-only methods exercise one synthetic principal binding and one role assignment: exact retries acknowledge, changed version/hash conflicts, malformed IDs and anonymous principals are blocked, and the checksum-verified same-Wasm EOP upgrade must retain the records. New build/run scripts use only the lock-pinned Motoko compiler, an SHA-256-verified disposable Wasm, a byte-identical writable copy of PocketIC, and synthetic principals; they do not access DFX state, wallet, identity, external network, target data, OAuth, deployment, or a project Candid storage method. `mops test IdentityRoleRecovery`, `mops test CallerAuthorization`, `mops check application_canister`, shell/JavaScript syntax checks, the fixture build, and `git diff --check` pass. The local PocketIC runner reached its bounded startup timeout before it created a canister, so no execution pass is claimed; `.github/workflows/m1-application-embedded-identity-role.yml` is the isolated 20-minute execution path and retains checksums/logs. This is current-topology private-adapter evidence only: it does not add a public application method or establish target authorization policy, workflow/migration recovery, archive/export, capacity, low-cycle, repair, importer, OAuth, treasury, deployment, or authoritative collection behavior.

93. **IMPLEMENTED 2026-09-19 (consolidated application embedded workflow-receipt proof boundary):** Added the disposable `fixtures/application/EmbeddedWorkflowCompletionReceiptStoreProof.mo` and a separate checksum-verified PocketIC build/run/CI path for the current application-private `workflow_completion_receipt_v1` adapter. It mirrors the target actor's private create/reopen discipline and contains only the immutable logical-ID/cycle/operation/version/hash receipt tuple—no workflow input, provider response, target data, generic storage surface, or target Candid method. The proof discards a completed synthetic ingress response and accepts a redelivery only when the exact immutable tuple acknowledges; changed version/hash, malformed logical ID, and empty cycle fail closed, and the same-Wasm EOP upgrade must preserve the receipt. `mops test CompletionReceiptIntent`, `mops test EmbeddedWorkflowCompletionReceiptStore`, `mops check application_canister`, both consolidated actors' Candid/stable compatibility checks, their release builds, fixture build, shell/JavaScript syntax checks, and `git diff --check` pass. This workspace cannot bind PocketIC's loopback server, so no local runtime execution pass is claimed; `.github/workflows/m1-application-embedded-workflow-receipt.yml` runs only the synthetic fixture in a 20-minute CI job and retains checksums/logs. This is private-adapter evidence only: it does not add a workflow implementation, target authorization, low-cycle/repair/capacity proof, migration/import behavior, deployment, or authoritative collection claim.

94. **IMPLEMENTED 2026-09-19 (consolidated application embedded migration-receipt proof boundary):** Added the disposable `fixtures/application/EmbeddedMigrationReceiptStoreProof.mo` and a separate checksum-verified PocketIC build/run/CI path for the current application-private `migration_receipt_v1` adapter. It mirrors the target actor's private create/reopen discipline and carries only bounded synthetic migration/table/chunk metadata plus immutable payload/version/content hashes—no PostgreSQL client, source row, credential, decoder, importer capability, generic storage surface, or target Candid method. A discarded initial synthetic ingress reply can be acknowledged only by redelivering the exact receipt; changed version, content hash, payload hash, malformed logical ID, zero row count, and a competing `(migration, table, chunk)` key fail closed. The application adapter now also compares all immutable chunk metadata and payload hash before accepting an existing logical ID, preventing a changed source chunk from being accepted as an idempotent retry. The same-Wasm EOP upgrade must retain both the receipt and its conflict behavior. The pinned Motoko 1.4.1 compiler passes the focused pure receipt vector and checks both the disposable fixture and application actor; shell/JavaScript syntax checks and `git diff --check` pass. The release fixture build exceeds this workspace's 30-second interactive command ceiling and PocketIC loopback is unavailable, so neither local artifact nor runtime execution is claimed; `.github/workflows/m1-application-embedded-migration-receipt.yml` is the isolated 20-minute execution path and retains only fixture checksums/logs. This is private-adapter evidence only: it does not add an importer, source connection, authorization policy, migration activation, low-cycle/repair/capacity proof, deployment, or authoritative collection claim.

95. **IMPLEMENTED 2026-09-19 (synthetic canonical treasury-journal archive-export recovery boundary):** Added separate disposable persistent fixture and fixed archive-sink actors under `fixtures/treasury/`, with a checksum-verified PocketIC build/run/CI path. The fixture accepts its bounded debit/credit journal set only at installation, derives and durably retains exactly one canonical SHA-256 archive binding before any await, and exposes no later posting, tuple, byte, hash, or replacement input. Its operator-only no-input recovery first reconciles the exact retained tuple and, only when absent, can dispatch that same retained bytes-plus-tuple binding. The sink admits only its fixture principal, bounds the bytes, derives and verifies their SHA-256 receipt hash, permits one exact binding, and returns only a Boolean exact-retention result to the fixture; it has no generic read/export, balance, account, asset, destination, signer, transaction, chain, target-data, or authority behavior. The bridge covers outsider denial, immutable preparation, deliberately lost reply, pre-await interruption, tuple/bytes-free repair, and EOP upgrades of the fixture and sink; acknowledgement also requires the sink to retain byte-identical canonical export bytes rather than a tuple alone. The pinned compiler builds both enhanced-orthogonal-persistence Wasms; shell/JavaScript syntax and the focused pure export-saga vector pass. This workspace cannot retrieve the pinned PocketIC binary because restricted DNS cannot resolve its toolchain source, so no local runtime execution pass is claimed; `.github/workflows/m1-treasury-journal-archive-export.yml` is the isolated 20-minute synthetic execution path and retains checksums/logs. This adds no treasury public Candid method, target actor state, archive service, immutable production export, balance projection, custody, deployment, or authority claim. Low-cycle and capacity execution for the canonical bytes-bound path, immutable production archive policy, and independent accounting reconciliation remain M1/G2/G3 blockers.

96. **IMPLEMENTED 2026-09-19 (synthetic canonical treasury-journal archive-export capacity boundary):** Added a checksum-verified PocketIC capacity runner for the disposable canonical archive-export fixture and its fixed sink. It separately exercises 16 expected and 32 two-times-expected fixed archives; every archive derives one bounded 16-entry debit/credit set, durably retains its SHA-256-bound canonical bytes before dispatch, deliberately loses its sink reply, reconciles only the exact receipt tuple, and requires the sink's byte-identical retention check. An 18-entry set fails during fixture installation before any durable binding or archive dispatch. The private mode-`0600` CI report records the archive/receipt counts, entries per archive, and encoded balanced-set input totals. The runner uses only disposable fixture Wasms, checksum-verified PocketIC copied to a writable temporary directory, and synthetic principals; it has no target actor, DFX state, wallet, identity, signer, balance, transfer, destination, transaction, chain call, target data, deployment, generic archive API, or authority claim. JavaScript/shell syntax and direct enhanced-orthogonal-persistence fixture compilation pass. Local PocketIC execution is not claimed because this workspace cannot run a local PocketIC proof; `.github/workflows/m1-treasury-journal-archive-export.yml` runs the bounded proof in a separate isolated 20-minute CI job. This is not a production instruction/cycle budget; low-cycle execution, immutable production archive policy, and independent accounting reconciliation remain M1/G2/G3 blockers.

97. **IMPLEMENTED 2026-09-20 (synthetic canonical treasury-journal archive-export low-cycle recovery boundary):** The same disposable canonical archive-export fixture now applies the existing proof-only one-trillion-cycle reserve after durably retaining its fixed SHA-256 bytes-plus-tuple binding and before either first sink dispatch or tuple/bytes-free repair dispatch. The isolated runner creates one fixture below that reserve, confirms its attempted archive has no sink receipt while its retained state remains pending, replenishes only that disposable fixture through PocketIC's test-only API, and then permits only operator repair of the already-retained binding followed by exact receipt reconciliation and byte-identical sink-retention verification. The fixture takes no later posting, tuple, byte, hash, or replacement input; the sink remains fixed-principal and bounded. `node --check`, `git diff --check`, and direct pinned Motoko enhanced-orthogonal-persistence fixture compilation pass. This workspace's local PocketIC runtime remains unavailable, so no local execution pass is claimed; the existing checksum-verified, isolated 20-minute CI workflow executes the proof. This remains a valueless synthetic reserve guard, not a production cycle budget, archive service, export policy, target actor method, balance projection, custody, signer, transfer, chain call, deployment, or authority claim. Immutable production archive policy and independent accounting reconciliation remain M1/G2/G3 blockers.

98. **IMPLEMENTED 2026-09-20 (consolidated application identity/role capacity boundary):** Added a separate checksum-verified PocketIC capacity runner for the current application-private principal-binding and role-assignment adapters. It creates only disposable synthetic fixtures and independently exercises 16 expected and 32 two-times-expected immutable binding/role writes. Every record is immediately redelivered with its exact immutable tuple and must acknowledge; a 513-byte logical ID is blocked before a private collection insert. The mode-`0600` CI report records the per-collection write/retry counts and encoded Candid-input totals. The existing isolated 20-minute application identity/role workflow runs the new proof after its recovery/upgrade fixture and retains the report. The runner has no application Candid method, caller authorization policy, OAuth provider data, DFX state, wallet, identity, external network, target data, import, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. This restricted workspace's Mops toolchain management is not initialized and its PocketIC loopback execution is unavailable, so no local build or runtime pass is claimed; CI remains the execution boundary. The result is a synthetic collection boundary, not a production instruction/cycle budget; application authorization, durable caller intent/recovery, low-cycle/repair, all remaining collection capacity, archive/export, and migration rehearsal remain M1/G2 blockers.

99. **IMPLEMENTED 2026-09-20 (consolidated application identity/role low-cycle recovery boundary):** Extended only the disposable application-private identity/role fixture with two independently retained synthetic immutable intents. Each intent is validated and retained before the fixed one-trillion-cycle proof reserve is checked; below that reserve neither private collection write occurs, a changed replacement is refused, and a later retry accepts no input. After PocketIC test-only replenishment of that fixture, each retry can write only its retained tuple and the consumed intent cannot replay. The isolated workflow now executes the checksum-verified low-cycle runner beside its existing recovery and capacity boundaries. It has no application public method, target actor state, caller authorization policy, OAuth data, DFX state, wallet, identity, signer, external network, target data, importer, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. The local pinned Mops toolchain is not initialized and local PocketIC loopback is unavailable, so no local fixture build or runtime pass is claimed; the isolated 20-minute CI job remains the execution boundary. This fixed reserve is not a production cycle budget; application authorization, measured collection budgets/alerts, remaining collection recovery/capacity, archive/export, and migration rehearsal remain M1/G2 blockers.

100. **IMPLEMENTED 2026-09-20 (consolidated application workflow-receipt capacity boundary):** Added a separate checksum-verified PocketIC capacity runner for the current application-private workflow-completion receipt adapter. It creates only disposable synthetic fixtures and separately exercises 16 expected and 32 two-times-expected immutable receipt writes. Each receipt is immediately redelivered with its exact logical-ID/cycle-ID/operation/version/hash tuple and must acknowledge; a 513-byte logical ID is rejected before a private collection insert. The private mode-`0600` CI report records write/retry counts and encoded Candid-input totals. The existing isolated 20-minute application workflow-receipt workflow runs this capacity proof after its lost-reply/EOP fixture and retains the report. The runner has no workflow implementation, application Candid method, caller authorization policy, provider response, target data, import, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. The local Motoko/PocketIC execution environment cannot provide its emulator endpoint, so no local fixture build or runtime pass is claimed; CI remains the execution boundary. This is a synthetic collection boundary, not a production instruction/cycle budget; application authorization, durable caller intent/recovery, low-cycle/repair, remaining collection capacity, archive/export, and migration rehearsal remain M1/G2 blockers.

101. **IMPLEMENTED 2026-09-21 (consolidated application workflow-receipt low-cycle recovery boundary):** Extended only the disposable application-private workflow-completion receipt fixture with one retained validated immutable receipt intent. It records that intent before checking the existing one-trillion-cycle proof reserve; below reserve it makes no private collection write, rejects replacement input, and permits only a no-input retry. After PocketIC test-only replenishment of that fixture, the retry writes only the retained logical-ID/cycle-ID/operation/version/hash tuple and the consumed intent cannot replay. The existing checksum-verified isolated 20-minute workflow now executes this low-cycle runner beside its lost-reply/EOP and capacity boundaries. It has no workflow implementation, application public method, caller authorization policy, provider response, target data, importer, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. The local fixture build and PocketIC execution are not claimed: this workspace's Motoko test/build command attempted emulator access but cannot establish the disposable loopback endpoint, so CI remains the execution boundary. The fixed reserve is not a production cycle budget; application authorization, repair/resume, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

102. **IMPLEMENTED 2026-09-21 (consolidated application migration-receipt capacity boundary):** Added a checksum-verified PocketIC capacity runner for the application-private `migration_receipt_v1` adapter. It creates only disposable synthetic fixtures and separately exercises 16 expected and 32 two-times-expected immutable migration/table/chunk receipt writes. Every receipt is immediately redelivered with the identical bounded logical-ID/migration-ID/table/chunk/row-count/payload/version/content-hash tuple and must acknowledge; a 513-byte logical ID is rejected before private collection insertion. The private mode-`0600` CI report records write/retry counts and encoded Candid-input totals. The existing isolated 20-minute application migration-receipt workflow runs this capacity proof after its lost-reply/EOP fixture and retains the report. The runner has no importer, PostgreSQL connection, source row, public application method, caller authorization policy, target data, or deployment. JavaScript and shell syntax plus `git diff --check` pass. This workspace cannot establish PocketIC's disposable loopback endpoint, so no local runtime pass is claimed; CI remains the execution boundary. This is a synthetic collection boundary, not a production instruction/cycle budget; importer authorization, durable low-cycle/repair recovery, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

103. **IMPLEMENTED 2026-09-21 (consolidated application migration-receipt low-cycle recovery boundary):** Extended only the disposable application-private migration-receipt fixture with one retained validated immutable receipt intent. It retains that intent before checking the existing one-trillion-cycle proof reserve; below reserve it makes no private collection write, rejects replacement input, and permits only a no-input retry. After PocketIC test-only replenishment of that fixture, the retry writes only the retained logical-ID/migration-ID/table/chunk/row-count/payload/version/content-hash tuple and the consumed intent cannot replay. The existing checksum-verified isolated 20-minute workflow now executes this low-cycle runner beside its lost-reply/EOP and capacity boundaries. It has no importer, PostgreSQL connection, source row, application public method, caller authorization policy, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. The local fixture build and PocketIC execution are not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. The fixed reserve is not a production cycle budget; importer authorization, repair/resume, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

104. **IMPLEMENTED 2026-09-21 (consolidated application migration-receipt EOP low-cycle resume boundary):** Extended only the existing disposable low-cycle runner. It retains the validated immutable migration receipt before the fixed one-trillion-cycle reserve, confirms its no-input retry cannot write below reserve, then performs a checksum-verified same-Wasm EOP-preserving upgrade while that retained intent is pending. The post-upgrade retry is still blocked below reserve; after PocketIC test-only replenishment it can write only the original logical-ID/migration-ID/table/chunk/row-count/payload/version/content-hash tuple, and a consumed intent cannot replay. The existing isolated 20-minute workflow runs this extension with the lost-reply/EOP, capacity, and low-cycle checks. It has no importer, PostgreSQL connection, source row, application public method, caller authorization policy, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This is not a repair authority or production cycle budget; explicit operator repair/resume policy, importer authorization, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

105. **IMPLEMENTED 2026-09-21 (consolidated application workflow-receipt EOP low-cycle resume boundary):** Extended only the existing disposable workflow-receipt low-cycle runner. It retains the validated immutable receipt before the fixed one-trillion-cycle reserve, confirms its no-input retry cannot write below reserve, then performs a checksum-verified same-Wasm EOP-preserving upgrade while that intent is pending. The post-upgrade retry is still blocked below reserve; after PocketIC test-only replenishment it can write only the original logical-ID/cycle-ID/operation/version/content-hash tuple, and a consumed intent cannot replay. The existing isolated 20-minute workflow runs this extension with the lost-reply/EOP, capacity, and low-cycle checks. It has no workflow implementation, application public method, caller authorization policy, provider response, target data, importer, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This is not a repair authority or production cycle budget; explicit operator repair/resume policy, application authorization, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

106. **IMPLEMENTED 2026-09-21 (consolidated application identity/role EOP low-cycle resume boundary):** Extended only the existing disposable identity/role low-cycle runner. It retains each validated immutable principal-binding and role-assignment input before the fixed one-trillion-cycle reserve, confirms that replacement inputs are refused, then performs a checksum-verified same-Wasm EOP-preserving upgrade while both intents are pending. Both no-input retries remain blocked below reserve; after PocketIC test-only replenishment each can write only its original logical-ID/version/content-hash tuple and neither consumed intent can replay. The existing isolated 20-minute workflow runs this extension with its lost-reply/EOP, capacity, and low-cycle checks. It has no application public method, caller authorization policy, OAuth data, importer, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This is not a repair authority or production cycle budget; explicit repair/resume policy, application authorization, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

107. **IMPLEMENTED 2026-09-21 (canonical treasury-journal archive-export EOP low-cycle resume boundary):** Extended only the existing disposable canonical archive-export runner. It first retains the derived immutable SHA-256 bytes-plus-tuple binding, proves the archive call is blocked below the fixed one-trillion-cycle reserve with no sink receipt, then performs a checksum-verified same-Wasm EOP-preserving upgrade while that binding is still pending. The upgraded fixture retains its `archiveStarted` state and remains pending before replenishment; after PocketIC test-only replenishment, the existing no-input operator repair can dispatch only that retained binding, and exact receipt reconciliation still requires byte-identical sink retention. The existing isolated 20-minute archive-export workflow executes this extension with its lost-reply, interruption, repair, capacity, and low-cycle paths. It has no treasury public method, production archive service, balance projection, signer, transfer, chain call, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This is not a production cycle budget or archive policy; immutable production archive/export policy and independent accounting reconciliation remain M1/G2/G3 blockers.

108. **IMPLEMENTED 2026-09-21 (consolidated application migration-receipt operator-repair boundary):** Extended only the disposable application-private migration-receipt fixture with a distinct interruption state: it durably retains a validated immutable receipt but issues no private write, rejects the normal no-input retry, and permits repair only from the fixture's fixed synthetic operator principal. An outsider is blocked, including after the existing checksum-verified same-Wasm EOP-preserving upgrade; the authorized no-input repair can then write only the retained logical-ID/migration-ID/table/chunk/row-count/payload/version/content-hash tuple, and a consumed repaired intent cannot replay. The existing isolated 20-minute workflow executes this extension beside the lost-reply/EOP, capacity, and low-cycle proofs. It has no importer, PostgreSQL connection, source row, application public method, target caller authorization policy, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This fixture-only principal is not an application role or repair policy; production importer authorization and repair/resume policy, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

109. **IMPLEMENTED 2026-09-21 (consolidated application workflow-receipt operator-repair boundary):** Extended only the disposable application-private workflow-completion receipt fixture with a distinct interruption state: it durably retains a validated immutable receipt but issues no private write, rejects the normal no-input retry, and permits repair only from the fixture's fixed synthetic operator principal. An outsider is blocked, including after the existing checksum-verified same-Wasm EOP-preserving upgrade; the authorized no-input repair can then write only the retained logical-ID/cycle-ID/operation/version/content-hash tuple, and a consumed repaired intent cannot replay. The existing isolated 20-minute workflow executes this extension beside the lost-reply/EOP, capacity, and low-cycle proofs. It has no workflow implementation, application public method, target caller authorization policy, provider response, target data, deployment, or authority behavior. JavaScript syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This fixture-only principal is not an application role or repair policy; production application authorization and repair/resume policy, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

110. **IMPLEMENTED 2026-09-21 (consolidated application identity/role operator-repair boundary):** Extended only the disposable application-private identity/role fixture with separate interruption states for a validated immutable principal-binding intent and role-assignment intent. Each retains its tuple but issues no private write, rejects an ordinary no-input retry, and permits no-input repair only from the fixture's fixed synthetic operator principal. An outsider is blocked after the existing checksum-verified same-Wasm EOP-preserving upgrade; the authorized repairs can write only their original logical-ID/version/content-hash tuples, and neither consumed repaired intent can replay. The existing isolated 20-minute workflow executes this extension beside its lost-reply/EOP, capacity, and low-cycle proofs. It has no application public method, caller authorization policy, OAuth data, importer, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator loopback endpoint; CI remains the execution boundary. This fixture-only principal is not an application role or repair policy; production application authorization and repair/resume policy, remaining collection evidence, archive/export, and migration rehearsal remain M1/G2 blockers.

111. **IMPLEMENTED 2026-09-21 (consolidated treasury payment-operation private-adapter proof boundary):** Added a disposable persistent fixture and an isolated checksum-verified PocketIC build/run/CI route for the current treasury-owned `treasury_payment_operation_v1` adapter. The fixture mirrors only the target actor's private create/reopen shape and exposes one synthetic immutable payment-operation write; it deliberately discards an initial ingress reply, accepts only the same logical-ID/version/content-hash retry, rejects changed version/hash, malformed logical IDs, and zero amounts, and retains that behavior after a same-Wasm EOP upgrade. It contains a destination hash only—no address, signer, key, transaction material, chain receipt, ledger call, balance, target data, or target Candid method. JavaScript and shell syntax plus `git diff --check` pass. The local Mops/PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; the isolated 20-minute CI workflow is the execution boundary. This is not a custody, transfer, application-to-treasury saga, low-cycle, repair/resume, capacity, archive, deployment, or authority claim; those remain M1/G2/G3 blockers.

112. **IMPLEMENTED 2026-09-21 (consolidated treasury payment-operation private-adapter capacity boundary):** Added a checksum-verified PocketIC capacity runner for the current treasury-private `treasury_payment_operation_v1` adapter. It creates only disposable synthetic fixtures and separately exercises 16 expected and 32 two-times-expected immutable operation writes. Every write is immediately redelivered with its identical logical-ID/operation-ID/obligation-ID/asset/positive integer amount/decimals/destination-hash/version/content-hash tuple and must acknowledge; a 513-byte logical ID is blocked before the private collection insertion. The private mode-`0600` CI report records write/retry counts, encoded Candid-input totals, and fixture cycle deltas. The existing isolated 20-minute treasury fixture workflow runs this capacity proof after its recovery/EOP route and retains the report. It has no treasury Candid method, signer, key, address, transaction, ledger call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This is a synthetic collection boundary, not a production instruction/cycle budget; low-cycle, repair/resume, archive, application-to-treasury saga, and custody/at-most-one-value-transfer proofs remain M1/G2/G3 blockers.

113. **IMPLEMENTED 2026-09-21 (consolidated treasury payment-operation private-adapter EOP low-cycle resume boundary):** Extended only the disposable treasury payment-operation fixture with a retained immutable operation tuple and the existing fixed one-trillion-cycle proof reserve. It proves a valid tuple is retained but makes no private write below reserve, rejects a changed tuple while pending, and remains blocked after a checksum-verified same-Wasm EOP-preserving upgrade. Following PocketIC test-only replenishment, the no-input retry can write only the original logical-ID/operation-ID/obligation-ID/asset/positive integer amount/decimals/destination-hash/version/content-hash tuple; a consumed intent cannot replay. The isolated 20-minute treasury fixture workflow executes this low-cycle route beside recovery/EOP and capacity evidence. It has no treasury Candid method, signer, key, address, transaction, ledger call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This proof-only reserve is not a production cycle budget or repair policy; operator repair/resume, archive, application-to-treasury saga, and custody/at-most-one-value-transfer proofs remain M1/G2/G3 blockers.

114. **IMPLEMENTED 2026-09-21 (consolidated treasury payment-operation private-adapter operator-repair boundary):** Extended only the disposable treasury payment-operation fixture with a distinct interruption state: it durably retains a validated immutable operation tuple but issues no private write, rejects the normal no-input retry, and permits repair only from the fixture's fixed synthetic operator principal. An outsider is blocked, including after the existing checksum-verified same-Wasm EOP-preserving upgrade; the authorized no-input repair can then write only the retained logical-ID/operation-ID/obligation-ID/asset/positive integer amount/decimals/destination-hash/version/content-hash tuple, and a consumed repaired intent cannot replay. The existing isolated 20-minute workflow executes this extension beside recovery/EOP, capacity, and low-cycle proofs. It has no treasury Candid method, signer, key, address, transaction, ledger call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This fixture-only principal is not a treasury role or repair policy; production operator repair/resume, archive, application-to-treasury saga, and custody/at-most-one-value-transfer proofs remain M1/G2/G3 blockers.

115. **IMPLEMENTED 2026-09-21 (consolidated treasury journal private-adapter proof boundary):** Added a disposable persistent fixture and isolated checksum-verified PocketIC build/run/CI route for the current treasury-owned `treasury_journal_v1` adapter. The fixture mirrors only the target actor's private create/reopen shape and writes one synthetic immutable debit posting; it deliberately discards an initial ingress reply, accepts only the same logical-ID/journal-sequence/version/content-hash retry, rejects changed version/hash, malformed logical IDs, and zero amounts, and retains that behavior after a same-Wasm EOP upgrade. It contains no balance projection, destination, signer, key, transaction material, chain receipt, ledger call, target data, or target Candid method. JavaScript and shell syntax checks pass; the existing focused journal vectors remain the adapter's pure negative encoding coverage. The local PocketIC execution is not claimed because this workspace cannot establish its disposable loopback endpoint; the isolated 20-minute CI workflow is the execution boundary. This is not a balanced-set, archive, custody, transfer, application-to-treasury saga, capacity, low-cycle, repair/resume, deployment, or authority claim; those remain M1/G2/G3 blockers.

116. **IMPLEMENTED 2026-09-21 (consolidated treasury journal private-adapter capacity boundary):** Added a checksum-verified PocketIC capacity runner for the treasury-private `treasury_journal_v1` adapter. It creates only disposable synthetic fixtures and separately exercises 16 expected and 32 two-times-expected immutable debit postings. Each write is immediately redelivered with its exact logical-ID/journal-sequence/operation/account/asset/direction/positive integer amount/decimals/version/content-hash tuple and must acknowledge; a 513-byte logical ID is rejected before private collection insertion. The private mode-`0600` CI report records write/retry counts, encoded Candid-input totals, and fixture cycle deltas. The existing isolated 20-minute treasury-journal workflow runs this capacity proof after recovery/EOP evidence and retains the report. It has no treasury Candid method, balance, destination, signer, key, transaction, ledger call, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This is a synthetic collection boundary, not a production instruction/cycle budget; balanced-set, archive, low-cycle/repair, application-to-treasury saga, and independent accounting evidence remain M1/G2/G3 blockers.

117. **IMPLEMENTED 2026-09-21 (consolidated treasury journal private-adapter EOP low-cycle resume boundary):** Extended only the disposable treasury-journal fixture with a retained immutable posting tuple and the existing fixed one-trillion-cycle proof reserve. It proves a valid tuple is retained but makes no private write below reserve, rejects a changed tuple while pending, and remains blocked after a checksum-verified same-Wasm EOP-preserving upgrade. Following PocketIC test-only replenishment, the no-input retry can write only the original logical-ID/journal-sequence/operation/account/asset/direction/positive integer amount/decimals/version/content-hash tuple; a consumed intent cannot replay. The isolated 20-minute treasury-journal workflow executes this low-cycle route beside recovery/EOP and capacity evidence. It has no treasury Candid method, balance, destination, signer, key, transaction, ledger call, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This proof-only reserve is not a production cycle budget or repair policy; balanced-set, archive, operator repair/resume, application-to-treasury saga, and independent accounting evidence remain M1/G2/G3 blockers.

118. **IMPLEMENTED 2026-09-21 (consolidated treasury journal private-adapter operator-repair boundary):** Extended only the disposable treasury-journal fixture with a distinct interruption state: it durably retains a validated immutable posting tuple but issues no private write, rejects the normal no-input retry, and permits repair only from the fixture's fixed synthetic operator principal. An outsider is blocked, including after the existing checksum-verified same-Wasm EOP-preserving upgrade; the authorized no-input repair can then write only the retained logical-ID/journal-sequence/operation/account/asset/direction/positive integer amount/decimals/version/content-hash tuple, and a consumed repaired intent cannot replay. The existing isolated 20-minute workflow executes this extension beside recovery/EOP, capacity, and low-cycle proofs. It has no treasury Candid method, balance, destination, signer, key, transaction, ledger call, target data, deployment, or authority behavior. JavaScript and shell syntax plus `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This fixture-only principal is not a treasury role or repair policy; balanced-set, archive, application-to-treasury saga, and independent accounting evidence remain M1/G2/G3 blockers.

119. **IMPLEMENTED 2026-09-21 (consolidated treasury journal balanced-set private-adapter boundary):** Extended only the disposable treasury-journal fixture with a bounded two-sided immutable set route over the current treasury-private adapter. It validates the complete set, including exact integer debit/credit equality, before its first private write; both original postings acknowledge, exact set redelivery is idempotent, an unbalanced replacement is blocked before a private write, and a changed retained version conflicts. The existing checksum-verified same-Wasm EOP upgrade retains the set, whose exact redelivery still acknowledges. The isolated 20-minute treasury-journal workflow covers this beside existing recovery, capacity, low-cycle, and repair evidence. It has no treasury Candid method, mutable balance projection, destination, signer, key, transaction, ledger call, target data, deployment, or authority behavior. JavaScript syntax and `git diff --check` pass. Local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint; CI remains the execution boundary. This is a synthetic bounded double-entry collection proof, not a production balanced-set saga or accounting reconciliation; archive, application-to-treasury saga, and independent accounting evidence remain M1/G2/G3 blockers.

120. **IMPLEMENTED 2026-09-21 (consolidated treasury journal canonical archive private-adapter boundary):** The disposable canonical archive-export fixture now opens the current treasury-owned `treasury_journal_v1` private adapter and writes every member of its constructor-fixed, already-valid bounded balanced set through that adapter before retaining the derived SHA-256-bound archive binding or dispatching to its synthetic sink. Any private write failure or conflict leaves no archive route. The existing isolated checksum-verified proof continues to cover lost replies, interruption, EOP upgrade, low-cycle replenishment, operator-only tuple-free repair, exact receipt reconciliation, and byte-identical sink retention; its capacity route rejects an invalid set before this private collection path. This adds no treasury Candid method, generic storage access, archive service, mutable balance, destination, signer, key, transaction, ledger call, target data, deployment, or authority behavior. It is synthetic private-collection evidence only, not an archive policy, application-to-treasury saga, or independent accounting reconciliation; those remain M1/G2/G3 blockers.

121. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive export contract only):** Added pure `canisters/treasury/PaymentOperationArchiveExport.mo` and focused vectors. It emits one bounded, self-contained length-prefixed UTF-8 envelope for a valid immutable payment-operation input, including its exact integer amount and destination hash but never a destination address, signing material, transaction bytes, chain receipt, actor state, or archive transport. Equal inputs encode byte-identically; changes to the amount, destination hash, or content hash produce distinct bytes; invalid operation material fails closed. Its receipt guard binds acknowledgement only to the same logical-ID/version/content-hash tuple. This is a codec boundary only: it has no Candid method, persistent archive, private-store mutation, ledger/chain call, custody, deployment, or authority claim. A durable bytes-and-SHA-256-bound payment-operation archive saga, recovery/capacity evidence, and at-most-one-value-transfer proof remain M1/G2/G3 blockers.

122. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive binding contract only):** Added pure `PaymentOperationArchiveExportBinding.mo`, which derives and fixes the SHA-256 receipt tuple for the already-bounded canonical payment-operation archive bytes. It accepts no archive bytes, receipt, destination address, signer, transaction, ledger, or chain material; recovery can only re-derive the immutable input and fails closed on changed operation material, bytes, or receipt hash. Focused vectors prove the binding is 32 bytes, differs from the private-store content hash, and rejects altered amount, bytes, and archive hash. This adds no Candid method, persistence, archive transport, private-store mutation, custody, deployment, or authority claim. A durable retained-before-await archive saga, lost-reply/upgrade/capacity/repair evidence, and at-most-one-value-transfer proof remain M1/G2/G3 blockers.

123. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive lost-reply boundary):** Added a disposable persistent treasury/archive-sink fixture and checksum-verified PocketIC build/run/CI route for the canonical payment-operation archive binding. Before its synthetic archive await, the treasury fixture retains only the bounded canonical bytes plus their SHA-256 receipt tuple; it deliberately loses the successful reply, rejects malformed payment input before the sink, and accepts no replacement input during reconciliation or retry. The sink is treasury-caller-only, verifies the bytes-to-receipt SHA-256 binding, rejects a changed same-logical-ID binding, and persists exact redelivery idempotently. The proof covers outsider denial, lost reply, same-Wasm EOP upgrade of both synthetic actors, exact receipt reconciliation, and one retained binding after duplicate delivery. It has no target treasury Candid method, archive service, address, signer, key, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax, fixture Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow is the execution boundary. This is not a capacity, low-cycle, repair/resume, archive-policy, application-to-treasury saga, custody, or at-most-one-value-transfer proof; those remain M1/G2/G3 blockers.

124. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive capacity boundary):** Added a separate disposable persistent treasury/sink pair and checksum-verified PocketIC capacity route for canonical payment-operation archive bindings. It validates each entire synthetic batch before its first archive await, admits at most 32 SHA-256-bound canonical bindings, rejects a 33-member batch and a 513-byte logical ID before any synthetic archive insertion, and accepts exact redelivery idempotently. The isolated CI route separately exercises 16 expected and 32 two-times-expected bindings and records only counts, encoded Candid-input totals, and synthetic treasury cycle deltas in a private mode-`0600` report. It has no target treasury Candid method, archive service, signer, key, address, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax, fixture Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow is the execution boundary. This is a bounded synthetic archive-collection measurement, not a production instruction/cycle budget, archive policy, application-to-treasury saga, custody, or at-most-one-value-transfer proof; low-cycle and repair/resume evidence remain M1/G2/G3 blockers.

125. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive low-cycle EOP recovery boundary):** Extended only the disposable canonical payment-operation archive fixture and its existing checksum-verified PocketIC route. It retains the immutable canonical bytes plus SHA-256 receipt tuple before the fixed one-trillion-cycle proof reserve; below reserve it makes no synthetic archive dispatch, and a changed binding is rejected once retained. A same-Wasm EOP-preserving upgrade leaves that binding pending; after PocketIC test-only replenishment, the no-input retry can dispatch only the retained binding, deliberately lose its reply, and acknowledge only through the sink's exact receipt lookup. The route contains no target treasury Candid method, archive service, address, signer, key, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax, fixture Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the existing isolated 20-minute CI workflow is the execution boundary. The fixed reserve is not a production cycle budget; operator repair/resume, archive policy, application-to-treasury saga, custody, and at-most-one-value-transfer evidence remain M1/G2/G3 blockers.

126. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive operator-repair/resume boundary):** Extended only the disposable canonical payment-operation archive fixture and its existing checksum-verified PocketIC route. A synthetic interruption retains the immutable canonical bytes plus SHA-256 receipt tuple before any archive await; replacement payment input is rejected. Following a same-Wasm EOP-preserving upgrade, an outsider cannot invoke repair. The fixed synthetic operator's no-input repair first looks up the exact retained receipt and, only when absent, can redeliver that same retained binding; its reply remains deliberately unknown, so activation occurs only after an exact receipt lookup following EOP upgrades of both disposable treasury and sink. The route contains no target treasury Candid method, archive service, address, signer, key, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax, fixture Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the existing isolated 20-minute CI workflow is the execution boundary. This fixture-only operator is not a production repair policy; archive policy, application-to-treasury saga, custody, and at-most-one-value-transfer evidence remain M1/G2/G3 blockers.

127. **IMPLEMENTED 2026-09-21 (canonical treasury payment-operation archive private-adapter boundary):** The disposable canonical payment-operation archive fixture now opens the current treasury-owned `treasury_payment_operation_v1` private adapter and writes the already-valid immutable operation through it before retaining its SHA-256-bound canonical archive binding or dispatching to its synthetic sink. Any private write failure or conflict leaves no archive route. The existing isolated checksum-verified CI proof route covers malformed-input rejection, lost replies, interruption, EOP upgrade, low-cycle replenishment, operator-only tuple-free repair, exact receipt reconciliation, and exact duplicate sink retention. JavaScript and shell syntax plus `git diff --check` pass. Local Motoko compilation and PocketIC execution are not claimed: the Mops toolchain manager is not initialized and this workspace cannot establish the disposable emulator endpoint, so the existing isolated 20-minute CI workflow remains the execution boundary. This adds no treasury Candid method, generic storage access, archive service, balance, destination address, signer, key, transaction, ledger/chain call, target data, deployment, or authority behavior. It is synthetic private-collection evidence only, not an archive policy, application-to-treasury saga, custody, or at-most-one-value-transfer proof; those remain M1/G2/G3 blockers.

128. **IMPLEMENTED 2026-09-21 (application-to-treasury immutable outbox/inbox contract only):** Added pure `canisters/application/TreasuryOperationSaga.mo` and focused vectors. The sole allowed application-domain cross-canister boundary now has a bounded immutable logical-ID/version/32-byte-hash outbox intent contract: a future application actor must retain it before awaiting treasury; a lost reply is reconciled only against the identical retained treasury receipt tuple; absence permits only that same retry; changed or malformed observations fail closed; and only an exact receipt activates the application-side result. The contract accepts no payment operation, amount, asset, destination, signer, transaction, chain receipt, caller principal, Candid method, actor state, or network call. The cached pinned Motoko 1.4.1 compiler type-checks the contract and vectors with the locked base package, and `git diff --check` passes. `mops test TreasuryOperationSaga` could not run locally because Mops repeatedly attempted unavailable PocketIC network access; no runtime pass is claimed. This is not a public application method, treasury authorization policy, durable actor implementation, PocketIC recovery/upgrade execution, archive policy, custody, transfer, deployment, or authority claim; those remain M1/G2/G3 blockers.

129. **IMPLEMENTED 2026-09-21 (application-to-treasury durable outbox/inbox lost-reply boundary):** Added a disposable persistent application/treasury fixture pair and checksum-verified PocketIC CI route for the sole application-to-treasury tuple boundary. The application fixture retains only the bounded immutable logical-ID/version/32-byte-hash outbox tuple before its synthetic treasury await; its successful reply is deliberately lost. The synthetic treasury inbox is application-caller-only, retains an exact receipt idempotently, and rejects changed same-logical-ID tuples. After same-Wasm EOP upgrades of both disposable actors, only the exact retained inbox receipt activates the application result; duplicate delivery retains one receipt. The route also denies outsider recovery/inbox lookup and rejects malformed input before it reaches treasury. It adds no target Candid method, payment operation, amount, asset, destination, signer, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax, fixture Wasm build, SHA-256 verification, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow is the execution boundary. This is not a treasury authorization policy, durable target actor, archive policy, custody, transfer, or at-most-one-value-transfer proof; those remain M1/G2/G3 blockers.

130. **IMPLEMENTED 2026-09-21 (application-to-treasury outbox/inbox capacity boundary):** Added separate disposable persistent application/treasury capacity fixtures and a checksum-verified PocketIC CI route for the sole tuple boundary. The application validates the complete batch before its first synthetic treasury await, admits at most 32 bounded immutable logical-ID/version/32-byte-hash tuples, and rejects a 33-member batch or 513-byte logical ID before the treasury inbox receives a tuple. Exact 16-entry expected and 32-entry two-times-expected batches redeliver idempotently; the private mode-`0600` report records only counts, encoded Candid-input totals, and synthetic application cycle deltas. It has no target Candid method, payment operation, amount, asset, destination, signer, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript and shell syntax, fixture-Wasm build route, and `git diff --check` pass; local PocketIC execution is not claimed because the workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow is the execution boundary. This bounded synthetic collection evidence is not a production instruction/cycle budget, treasury authorization policy, archive policy, custody, transfer, or at-most-one-value-transfer proof; low-cycle and operator repair/resume remain M1/G2/G3 blockers.

131. **IMPLEMENTED 2026-09-21 (application-to-treasury outbox/inbox low-cycle EOP recovery boundary):** Added a disposable persistent application fixture and checksum-verified PocketIC CI route for the sole tuple boundary. It retains the validated immutable logical-ID/version/32-byte-hash tuple before the fixed one-trillion-cycle proof reserve; below reserve no treasury inbox call occurs, a replacement tuple is rejected, and retry has no input. A same-Wasm EOP-preserving upgrade retains the pending tuple without opening another route; after PocketIC test-only replenishment, only that no-input retry may submit the original tuple, and the consumed intent cannot replay. Outsider retry is denied. It has no target Candid method, payment operation, amount, asset, destination, signer, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript/shell syntax, fixture-Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow is the execution boundary. The fixed reserve is not a production cycle budget; treasury authorization policy, operator repair/resume, archive policy, custody, transfer, and at-most-one-value-transfer evidence remain M1/G2/G3 blockers.

132. **IMPLEMENTED 2026-09-22 (application-to-treasury outbox/inbox operator-repair boundary):** Extended only the disposable application-side tuple fixture and its existing checksum-verified PocketIC route. A synthetic interruption retains the validated immutable logical-ID/version/32-byte-hash tuple before any treasury await; replacement input is refused and the ordinary no-input retry is closed. After a same-Wasm EOP-preserving upgrade, an outsider remains unable to repair. Only the fixture's fixed synthetic operator can invoke a tuple-free repair, which dispatches only the retained tuple; the consumed repaired tuple cannot replay. The route adds no target Candid method, treasury authorization policy, payment operation, amount, asset, destination, signer, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript/shell syntax, fixture-Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow remains the execution boundary. This fixed synthetic operator is not a production repair policy; treasury authorization, archive policy, custody, transfer, and at-most-one-value-transfer evidence remain M1/G2/G3 blockers.

133. **IMPLEMENTED 2026-09-22 (application-to-treasury repair lost-reply reconciliation boundary):** Extended only that disposable application-to-treasury fixture and its existing checksum-verified PocketIC route. A fixed synthetic repair operator now deliberately loses a successful repair reply after retaining the immutable tuple and crossing the treasury boundary; repair stays pending and cannot activate from that reply. After the existing same-Wasm EOP upgrade, only that fixed operator may reconcile through the treasury inbox's exact tuple lookup. An exact retained receipt acknowledges the repair; outsiders are denied and a consumed repair cannot replay. The route adds no target Candid method, treasury authorization policy, payment operation, amount, asset, destination, signer, transaction, ledger/chain call, balance, target data, deployment, or authority behavior. JavaScript/shell syntax, fixture-Wasm build, and `git diff --check` pass; local PocketIC execution is not claimed because this workspace cannot establish the disposable emulator endpoint, so the isolated 20-minute CI workflow remains the execution boundary. This fixture-only operator is not a production repair policy; archive policy, custody, transfer, and at-most-one-value-transfer evidence remain M1/G2/G3 blockers.

134. **IMPLEMENTED 2026-09-22 (application-to-treasury private-outbox adapter boundary):** Added `application_treasury_outbox_v1`, a fixed application-private collection containing only the bounded immutable logical-ID/version/32-byte-hash tuple for the one permitted application-to-treasury route. It has a unique logical ID, two-result-bounded tuple-only lookup, exact duplicate acknowledgement, and fail-closed changed-tuple conflict; it exposes no Candid or generic storage surface and retains no payment operation, amount, asset, destination, caller, signer, transaction, or chain material. The consolidated application actor opens or reopens it privately. The existing disposable low-cycle/repair fixture now writes its validated tuple through the same adapter before retaining any dispatchable intent; private-write failure leaves no treasury route. Its checksum-verified same-Wasm EOP route continues to cover reserve/replenishment, interruption, fixed-operator repair, deliberately lost repair reply, outsider denials, and exact treasury-inbox reconciliation. `mops check application_canister`, `mops test TreasuryOperationSaga`, fixture Wasm build, JavaScript/shell syntax, and `git diff --check` pass. Local PocketIC execution remains CI-only because the workspace cannot establish the disposable emulator endpoint. This is private-collection fixture evidence only, not a public application method, treasury authorization policy, payment/custody/transfer path, archive policy, deployment, or authority claim; independent at-most-one-value-transfer and G2/G3 evidence remain required.

135. **IMPLEMENTED 2026-09-22 (application-to-treasury private-inbox adapter boundary):** Added `treasury_application_inbox_v1`, a fixed treasury-private collection that admits only the bounded immutable logical-ID/version/32-byte-hash acknowledgement tuple for the one permitted application-to-treasury route. It has a unique logical ID, two-result-bounded tuple-only lookup, exact duplicate acknowledgement, and fail-closed changed-tuple conflict; it exposes no Candid or generic storage surface and retains no payment operation, amount, asset, destination, caller, signer, transaction, or chain material. The consolidated treasury actor opens or reopens it privately. Focused vectors reject a malformed logical ID and non-32-byte hash, and prove exact tuple acknowledgement versus changed version/hash conflict. This adds no public treasury route, payment/custody/transfer path, archive policy, deployment, or authority claim. A synthetic fixture must still prove this target-owned adapter across recovery before a target treasury route could claim recovery evidence; independent at-most-one-value-transfer and G2/G3 evidence remain required.

136. **IMPLEMENTED 2026-09-22 (treasury-private application inbox EOP recovery boundary):** The existing disposable synthetic treasury sink now opens the same treasury-owned `treasury_application_inbox_v1` adapter as the consolidated treasury instead of retaining an array. It remains application-caller-only and accepts only the bounded immutable logical-ID/version/32-byte-hash tuple; exact redelivery is acknowledged while changed tuples fail closed. The existing checksum-verified route upgrades both fixture actors with EOP preservation after a deliberately lost reply, then requires exact tuple lookup to reconcile it; outsider lookup remains denied and duplicate delivery retains one tuple. `mops test EmbeddedApplicationOperationInboxStore`, direct pinned Motoko EOP fixture type checking, JavaScript syntax, and `git diff --check` pass. The linked fixture-Wasm build exceeds this workspace's interactive command ceiling, so no local PocketIC execution is claimed; the existing isolated 20-minute CI workflow remains the execution boundary. This is synthetic private-collection recovery evidence only, not a public treasury route, authorization policy, payment/custody/transfer path, archive policy, deployment, or authority claim; independent at-most-one-value-transfer and G2/G3 evidence remain required.

137. **IMPLEMENTED 2026-09-22 (application-to-treasury private outbox/inbox capacity boundary):** The existing bounded 16-entry expected and 32-entry two-times-expected synthetic capacity route now opens the same application-owned `application_treasury_outbox_v1` and treasury-owned `treasury_application_inbox_v1` adapters as the consolidated actors instead of retaining capacity tuples only in fixture arrays. Each fully validated tuple is durably acknowledged by the private outbox before its synthetic treasury await, then by the private inbox; exact redelivery remains idempotent. The existing 33-member batch and 513-byte logical-ID negatives still fail before any inbox insertion. The treasury fixture exposes only a fixed 64-lookup bounded presence probe for the two constructor-fixed test namespaces, never collection enumeration. `mops test EmbeddedApplicationOperationInboxStore`, the checksum-verified fixture-Wasm build, JavaScript syntax, and `git diff --check` pass. Local PocketIC execution remains CI-only because the workspace cannot establish the disposable emulator endpoint. This is synthetic private-collection capacity evidence only, not a public route, treasury authorization policy, payment/custody/transfer path, archive policy, deployment, production cycle budget, or authority claim; independent at-most-one-value-transfer and G2/G3 evidence remain required.

138. **IMPLEMENTED 2026-09-22 (application-private outbox idempotency contract boundary):** The fixed `application_treasury_outbox_v1` adapter now centralizes its exact duplicate decision: only the retained same-version/32-byte-hash tuple is acknowledged; an absent, version-changed, hash-changed, malformed-ID, or malformed-hash observation fails closed. The write path uses that same decision, so its unit vectors cover the predicate exercised before any application-to-treasury route. `mops test EmbeddedTreasuryOperationOutboxStore`, `mops test EmbeddedApplicationOperationInboxStore`, `mops check application_canister`, and `git diff --check` pass. This adds no Candid method, treasury authorization policy, payment/custody/transfer path, archive policy, deployment, or authority claim; current-topology recovery execution, independent at-most-one-value-transfer, and G2/G3 evidence remain required.

Acceptance:

- Counts, sizes, max field lengths, status histograms, null/unique collisions, orphan checks, exact decimal ranges, sequence values, and unmanaged-table contents are recorded for all 22 physical tables.
- Benchmarks cover expected, 2×, and failure-limit sizes; every production query has an index/cursor plan below the instruction budget.
- The self-hosted storage-authority proof demonstrates that identity/role updates, financial journals/operations, replay receipts, and migration receipts preserve their stated invariant across duplicate requests, traps, upgrades, and lost cross-canister replies. It proves pending data cannot become authoritative before acknowledgement, unknown operations reconcile by logical ID/version/hash without overwriting a concurrent result, and every required multi-document operation is a tested bounded local transaction or durable saga; any native exception is approved, collection-specific, and documented in `SCHEMA_MAPPING.md`.
- Storage-authority direct-ingress and inter-canister negative tests prove that browsers, users, import operators, unrelated canisters, and bootstrap/deployer principals cannot read sensitive collections or mutate any collection. Post-deploy and post-upgrade audits match the fixed application-caller/governance allowlist, exact collection/action policy, and governance-only administration; no ZenDB grant API is publicly reachable.
- A disposable-PostgreSQL rehearsal proves that the logical slot/publication is created before every base snapshot; every base worker imports that exported snapshot; decoded transactions are contiguous from the slot consistent point through a final barrier LSN; update/delete replica identities and prepared-transaction behavior are handled; and a consumer crash never acknowledges an unverified delta. It also proves that source-side redaction excludes secret/bearer/raw-token values from base exports, publications, outbox rows, reports, and logs. A generic trigger outbox is not an automatic production fallback unless G2 proves an equivalent durable commit-order feed.
- The existing AGPL-3.0-only license and metadata baseline remains intact. ZenDB's exact source/dependency/Candid/Wasm hashes and logical-ID/hash reconciliation strategy are pinned and proven; upgrade and schema-version migration tests pass; all authoritative and archive data remains canonically exportable independent of ZenDB.
- Candid and stable signature baselines are committed; application types represent money exactly. The exact `identify` package/API hash and provider-capability matrix are recorded, and the caller-bound OAuth-attempt vectors reject an anonymous caller, state/caller swap, expiry, and unsupported provider before G2; this is design evidence, not a live provider deployment.
- The dependency inventory identifies every legacy rollback and target frontend/toolchain production dependency, records the advisory result, actual reachability, owner, containment, and upgrade/removal decision, and rejects an unaccepted high/critical advisory in a bundle proposed for deployment.
- Migration dry-run vectors produce byte-identical canonical chunks and hashes across repeated runs.

Rollback: remove un-deployed scaffolding/benchmarks; no legacy behavior or data has changed. A completed public AGPL-3.0 grant is not treated as revoked by rolling back later code; any license correction follows a documented, approved legal/notice disposition.

### G2 — Database schema and migration design approval

Status: BLOCKED by M1.

### M2 — Core Motoko domain and authorization

Status: BLOCKED by G2.

Dependencies: G2.

Invariants:

- A non-anonymous Candid caller principal is the authority root for every public method. Internet Identity and a verified OAuth subject are peer authentication factors for adding/recovering that caller binding; no bearer session, callback, request user ID, or caller-supplied principal grants authority by itself.
- Login identities, public handles, KYC evidence, and payout destinations are separate records.
- Canister quotas are bound to authenticated caller principal, provider, action, and bounded cost/bytes. A canister does not receive a trustworthy end-user IP address and never accepts one from a caller. If IP-based abuse controls remain necessary, a separately approved edge boundary enforces them and is not an authorization input.
- Raw KYC/identity evidence has a G2-approved purpose, data-controller/legal basis, retention/erasure schedule, backup treatment, access-audit role, and accounting/anti-evasion exception before it can be collected or imported. User-profile and ban-voting projections default to excluding raw social identifiers and user payout wallet addresses unless the G2 public-disclosure/consent decision explicitly permits a field and purpose; published treasury receiving addresses remain separately governed asset/scope configuration.
- Multi-record core changes use either one bounded ZenDB-side method proven atomic against the G2 pin or a durable idempotent intent/write/acknowledgement saga; no application-to-ZenDB call is treated as a single atomic message.
- User deletion cannot delete financial/evaluation/audit history.

Small changes/commits:

1. Core types, repositories, uniqueness/FK enforcement, cursor pagination, and upgrade tests.
2. Internet Identity integration and principal binding/recovery.
3. Use the exact M1-pinned Motoko `identify` OAuth package and implement the caller-bound OAuth factor protocol: a non-anonymous Candid caller begins a short-lived one-use attempt with provider, purpose, caller binding, state/nonce, redirect allowlist, and PKCE challenge; the certified React callback returns the authorization code/state/verifier only to `completeOAuth` from that same caller. The canister verifies the attempt, expiry, state, PKCE, configured client/redirect, provider-specific authorization-code/token/profile response, and immutable subject before it creates or recovers a principal binding; issuer/audience claims are verified where the provider supplies them. The callback or code never authenticates a method by itself, and tokens/codes/verifiers never enter browser history after handling, logs, or public/archive data. The M1 provider capability record selects a proven PKCE/public-client or confidential-client flow; any client secret is a scoped, encrypted, rotatable canister credential, never a browser value. Provider tokens are discarded after subject verification unless an approved feature needs one, in which case they are encrypted, scope-minimized, access-audited, and rotatable. Legacy identities require re-verification where source data lacks immutable IDs; unsupported provider flows are blocked or explicitly G2-approved as retired, never downgraded to a bearer or mutable-handle login.
4. KYC/liveliness integration, including a required `join-proxy` design/prototype. Inspect the pinned `https://github.com/vporton/join-proxy` service and `https://github.com/vporton/join-proxy-client.mo` client; use the client only against an allowlisted, configurable HTTPS proxy URL. Define the reusable-attestation/session protocol, consent and data-minimization boundary, proof freshness and subject binding, provider terms/eligibility, failure/expiry behavior, cost-accounting, and a direct-provider fallback that cannot double-charge or weaken AML/KYC controls. The endpoint still requires signature verification, event deduplication, monotonic state, AML precedence, and encrypted evidence policy.
5. Named role capabilities, pause-only incident role, immutable audit events, principal/provider/action/cost quotas, and governance-call interfaces. If approved, integrate an external edge quota configuration without trusting forwarded/caller-supplied IP data.
6. Ban voting/holds/compensation eligibility and deterministic UTC epochs.
7. Certified/sanitized public views.

Acceptance:

- Authorization matrix, horizontal-access, identity-confusion, replay, deleted-user, OAuth caller-binding/state/PKCE/configured-client+redirect/provider-response/issuer-where-present/subject-conflict/expired-attempt tests, KYC ordering, callback, quota, public-projection, and upgrade tests pass in PocketIC/local replica. Tests prove a callback, copied code, copied state, bearer token, anonymous caller, different caller, or caller-supplied IP cannot add/recover a binding or bypass a quota; an exact duplicate completion is idempotent; and an OAuth-authenticated recovery preserves the no-cross-account-merge and step-up/notification rules.
- `join-proxy` is not optional implementation debt: before M2 can complete, a pinned compatible client/service contract, configurable endpoint, privacy/security review, reuse-versus-direct-provider decision, and tests for reused, expired, mismatched, duplicate, unavailable, and direct-fallback KYC flows are recorded. Reuse must never convert an unverified proxy response into verified KYC or cause two provider charges for one logical verification.
- Before M2 can complete, record and test the G2-approved PII lifecycle: purpose/legal basis, field minimization, encryption/key access, retention/cryptographic erasure, backup and restore behavior, access-audit review, anti-evasion/accounting holds, and the public social/user-wallet disclosure policy. The default implementation exposes no raw user identifier in certified public or ban-voting views.
- Exact indexed ownership replaces serialized-JSON substring matching.
- The legacy app remains buildable and unchanged except explicitly approved compatibility hooks.

Rollback: undeploy local/testnet canisters or reinstall test-only state; production remains on Node/PostgreSQL.

### M3 — Direct evaluations, durable timers, and external integrations

Status: BLOCKED by M2.

Dependencies: M2.

Invariants:

- Jobs are at-least-once and idempotent; no in-memory lock is authoritative.
- Timers are recoverable from stable schedules after upgrades or cycle outages.
- Evaluation workflows do not create or persist live task, dependency, claim/lease, provider-batch, provider-item, or intermediate-operation records. A versioned Motoko function executes each fixed, bounded sequence of `llm` calls directly and keeps intermediate values local to that invocation.
- A direct evaluation reads the already-authoritative user/cycle/eligibility inputs and writes no execution state before its first `llm` call. Its only new durable business output is the canonical final result/source set; stable recurring schedules may retain aggregate due cursors and final completion receipts, but never per-evaluation task or intermediate state.
- Every direct evaluation has hard bounds on operation count, request/response size, cycles, and provider spend. Interruption or failure publishes no partial canonical result; an authorized caller or due scheduler may restart the whole bounded sequence, and deterministic cycle/result keys prevent duplicate final publication.
- ZenDB stores canonical results, sources, redacted audit metadata, and approved archive payloads according to the M1 authoritative-collection proof; it does not store new AI tasks. Legacy task/batch/dependency rows remain read-only migration history.

Small changes/commits:

1. Timer scheduler with stable deadlines/cursors, bounded user batches, completion receipts, and upgrade recovery; it invokes evaluations directly and creates no evaluation-task records.
2. Versioned first, repeat, and quarterly Motoko evaluators that execute the intended 14/6/5 bounded operations directly with the `llm` Motoko package, passing dependency outputs through local typed values rather than a stored DAG.
3. One shared HTTPS integration adapter with allowlisted hosts and redirects, HTTPS-only credential transport except documented loopback development, bounded request/response parsing, deadline, retry/backoff/circuit, provider/action cost budget, correlation/operation ID, and redacted audit. Deterministic transforms are used where replicated consensus requires them.
4. Canonical result/source publication with deterministic cycle/result keys. Do not implement OpenAI batch submission, polling, provider-item mappings, a task queue, or resumable intermediate task state; retain those legacy rows only in historical migration collections.
5. World GDP, token-price, email HTTPS-provider, the M2 caller-bound OAuth factor protocol, and Didit integration parity.
6. ZenDB result/audit/archive collection routing with cursor pagination, canonical export, reindex, collection-vN migration, and fault-injected authoritative mutation recovery.

Acceptance:

- Crash/restart/timeout/duplicate-trigger and timer-upgrade tests pass. An interrupted direct evaluation exposes no partial canonical result; rerunning it can publish at most one final result set for the deterministic cycle key.
- First, repeat, and quarterly code paths reproduce the intended 14/6/5-operation workflows and verified outcomes without writing live task, dependency, lease, batch, provider-item, or intermediate-operation records to stable memory or ZenDB.
- Tests enforce the configured operation/request/response/cycle/spend bounds and prove that later operations receive exactly the required earlier outputs through local typed values.
- External API keys are scoped/rotatable/spend-capped and excluded from logs/snapshots; compromise response is rehearsed. OAuth, KYC, email, GDP/price, and chain adapters all pass timeout, oversized/malformed response, redirect/host, circuit-open, correlation, and credential-transport negative tests.
- A ZenDB outage or partial final-result mutation cannot authorize or duplicate a canonical result/payment; the result publication saga makes the final record either unpublished or exactly recoverable without persisting execution tasks.

Rollback: route test traffic back to legacy services; discard test canisters. No production cutover.

### M4 — Deterministic migration tooling and shadow reconciliation

Status: BLOCKED by M3.

Dependencies: M3 and the G2-approved design, including the tested exact ZenDB pin, logical-ID strategy, RBAC grant matrix, remote-mutation saga, collection-vN upgrade protocol, and PostgreSQL delta-capture/redaction contract. If a required capability exists only in an unmerged or unpinned pull request, M4 remains blocked until an exact accepted commit is pinned and its behavior is proven, or G2 approves a documented design alternative.

Invariants:

- Export is read-only, snapshot-consistent, canonical, ordered, and deterministic.
- Import is authenticated, bounded, idempotent, resumable, and never calls wallet/ledger methods.
- Stable legacy IDs are preserved; all exceptions are explicit.
- Financial reconciliation is independent from general row/hash reconciliation.

Small changes/commits:

1. Read-only exporter, canonical codec, manifest/signature, chunker, and report schema.
2. Dry-run transformer and referential/unique/status validators.
3. Motoko/ZenDB staging-import protocol in which only the application importer is callable by the approved import principal. It records a durable local intent before every remote write; derives each staged record's application logical ID from the manifest/source key; uses unique `(migration,table,chunk)` plus payload hash for the authoritative ZenDB receipt; reconciles an unknown result by key/hash; and exposes a confirmed receipt only after the staged data and receipt hashes are acknowledged. If staging plus receipt must commit atomically inside ZenDB, use one bounded ZenDB-side method proven against the G2 pin rather than claiming cross-canister atomicity.
4. Full snapshot shadow import and repeated resume/fault injection.
5. Disposable-PostgreSQL delta-capture prototype: create the approved publication and logical slot before the base export, keep the replication connection open while base workers adopt its exported snapshot, and stream complete transactions from the slot consistent point through a final barrier LSN. Do not acknowledge/advance a slot until the corresponding canonical delta transaction is durable and target-acknowledged. Prove update/delete replica identities, prepared-transaction policy, crash/resume, slot-WAL exhaustion, and source schema freeze. Direct publication excludes sensitive tables; an audited source-side redacted projection/outbox may carry only non-secret metadata inside the same logical stream. A polling or generic trigger outbox without a proven total commit order is not a fallback.
6. Independent financial/history reconciler and ambiguous-payment exception workflow.

Acceptance:

- Repeated exports from one snapshot are byte-identical.
- Interrupted imports resume by reconciling local intents with ZenDB receipts by logical key and hash; duplicate identical chunks are no-ops; changed hashes are rejected; no blind duplicate insert is possible; partial record fragments and unacknowledged batches cannot become visible, including after interruption before, during, or after a remote ZenDB mutation.
- The importer has no direct ZenDB role. The application import capability expires after signed finalization; any separate staging-only application writer grant is revoked or downgraded, while a normal owning-application grant remains only where the approved live collection matrix requires it. Post-finalization negative tests and a grant audit prove import cannot resume or exceed that matrix.
- A base root and delta root prove contiguous source coverage from the logical slot's consistent point through the final barrier LSN. Each source transaction is staged and activated as one ordered target logical transaction/saga; no partial source transaction is visible and no later commit becomes authoritative before an earlier unresolved commit. Slot/publication/outbox identity, redaction-projection hashes, and no-secret scans are in the signed report.
- Counts/hashes/relations/uniques/source IDs match for all 22 physical tables.
- Secret values never appear in canonical data or reports.
- No method reachable in migration mode can transfer assets.

Rollback: discard shadow canisters and replication artifacts according to the runbook; PostgreSQL remains authoritative.

### M5 — Frontend canister and differential parity

Status: BLOCKED by M2–M4.

Dependencies: consolidated application/treasury implementation and test migration.

Invariants:

- The frontend is a certified static ICP asset application with strict CSP and no third-party executable JavaScript.
- Calls use generated Candid actors. Internet Identity and OAuth are peer authentication factors, but every state-changing method authorizes only its non-anonymous Candid caller after the M2 caller-bound OAuth exchange has added or recovered that principal binding; no bearer token, OAuth callback, or caller-supplied user ID is authority.
- Legacy frontend/backend stays deployable through the rollback window; the target uses the retained React frontend only after the approved canister route is enabled.
- The target never stores an application bearer session in browser storage. A legacy REST rollback route may not be presented as an authenticated target path; if it remains Internet-facing, its separately approved compatibility hardening includes strict CSP without `unsafe-inline` and an XSS/session-theft test.

Small changes/commits:

1. Retain the React/Vite/TypeScript client and build it with a pinned Node.js toolchain into a certified frontend-canister asset bundle. Replace its legacy REST bearer-token path with generated Candid actors alongside the legacy REST client behind an environment flag. Node.js is a reproducible build dependency only and never runs in the frontend canister; the legacy frontend/backend remains runnable until M10.
2. Internet Identity and the M2 caller-bound OAuth (`identify`) UI flows: create a browser-held non-anonymous Candid identity before OAuth start; preserve state/nonce/PKCE verifier only for the bounded attempt; deliver the provider redirect to an allowlisted certified React route; immediately remove code/state from browser history; and complete only through the authenticated Candid actor. Include social/KYC evidence-link flows and the M2 `join-proxy` decision. Do not restore REST sessions, URL tokens, or a frontend-only OAuth identity binding.
3. User, evaluation, logs, voting, admin/governance, and treasury read parity.
4. Certified asset canister, SPA aliasing, alternative-origin plan, strict no-`unsafe-inline` CSP, browser-storage-free target authentication, and reproducible build.
5. Differential fixture/E2E suite against legacy and ICP behavior.

Acceptance:

- Every UI route and user-visible state in `PARITY_CHECKLIST.md` passes browser E2E tests; the retained React assets rebuild byte-for-byte from the pinned Node.js lockfile/toolchain, are served as certified frontend-canister assets, use no browser-stored application bearer, and do not restore the legacy REST bearer-token path. OAuth E2E covers start/callback/complete, refresh/new-browser recovery, state/code/caller swaps, expiry/replay, provider-subject conflict, redirect tampering, browser-history/log redaction, and CSP/XSS attempts.
- Stale `/api/posts` helpers and documented-but-absent endpoints receive an explicit `NOT_APPLICABLE` or implementation decision; no silent omission.
- Asset certification, module hash, controller, CSP, network-switch, and custom-domain tests pass.

Rollback: switch the environment flag/DNS back to legacy frontend; no authority has moved.

### M6 — Wallet design spike and threat model

Status: BLOCKED by G2 and M2.

Dependencies: approved schemas/roles; test keys only.

Invariants:

- No real funds or legacy production keys.
- A test controller compromise is contained by the reviewed test-governance/SNS harness's proposal delay, pause role, policy caps, reconciliation procedures, and tested recovery; no canister is blackholed. This proves the controller contract, not that a production SNS already controls a canister.
- Every chain has an explicit operation-ID, nonce/sequence/UTXO, finality, reorg, and ambiguous-result design.

Small changes/commits:

1. ICRC/ICP and ck-token unified-treasury test-ledger prototype.
2. BTC testnet/regtest Chain Fusion prototype.
3. EVM Sepolia EVM-RPC/t-ECDSA prototype.
4. SOL devnet and other network feasibility spikes; classify unsupported/maturity risks rather than faking parity.
5. Use pinned local SNS testing or PocketIC SNS/NNS subnets to prototype the SNS-root controller contract, unified treasury, pause/recovery, and adversarial threat model. Verify that the test canister accepts governance action only through that configured root, that the production handoff artifacts are complete, and that no test canister has an empty controller list. Do not claim that a production canister is SNS-controlled or deploy a mainnet canister at M6.

Acceptance:

- Duplicate, reentrant, timed-out, upgrade-interrupted, `TooOld`, nonce conflict, UTXO conflict, provider inconsistency, finality, and reorg tests pass.
- Exact cycle/fee/storage budgets and funding alarms are measured.
- An independent reviewer signs off on the design evidence, including the recorded human SNS launch/ownership decision and an executable G4-only mainnet-testflight/recovery runbook.

Rollback: delete only valueless test accounts/canisters; retain reports.

### G3 — Wallet custody and authorization approval

Status: BLOCKED by M6.

### M7 — Wallet implementation on test networks

Status: BLOCKED by G3.

Dependencies: G3.

Invariants:

- The unified treasury journal enforces operation idempotency before every ledger or Chain Fusion action.
- ICRC accounts/subaccounts and direct Chain Fusion addresses are selected per reviewed asset/network adapter; ck assets remain distinct from native external assets.
- Payout pause, policy caps, and operation idempotency cannot be bypassed by application shortcuts; their modification requires the approved governance controller and delay. The test controller must exercise the same authorization interface as the selected SNS root, but cannot stand in for a production SNS handoff.

Small changes/commits:

1. Append-only accounting/liability/payment-operation journal.
2. One test-governed Chain Fusion treasury canister that owns the journal, payment operations, ICRC accounts/subaccounts, and direct-chain derivation/signing state. It is not blackholed and has no separate vault canister. Pin and test the production SNS-root authorization interface locally/PocketIC; reserve the mainnet SNS-testflight root handoff, then the separately reviewed production SNS handoff, for G4.
3. ICRC/ICP and ck-token adapters.
4. Direct BTC/EVM adapters, followed by independently reviewed network adapters.
5. Payout destination proof/change-delay, direct-to-treasury donation/deposit, scoped treasury, confirmation/reconciliation, SNS recovery, and incident tooling.

Acceptance:

- Supply conservation and double-entry/property tests pass across all states.
- Legacy known duplicate/ambiguous cases are fixtures and cannot trigger automated sends. Before any test-network treasury implementation starts, an interim legacy-sender inventory proves all real-value automatic senders are paused or covered by a separately approved, expiring safety exception; no missing legacy wallet key is generated.
- Test networks demonstrate direct donation to a published treasury address/account, funding, mint/update-balance, payout, retry, confirmation, reorg recovery, pause, governance-controller upgrade/recovery, cycle top-up, and controller-compromise response with valueless assets. They include duplicate scanner delivery, two unrelated donations with the same memo, and a forged/unbound memo; each source observation credits exactly once and no memo grants identity, entitlement, or a different scope.

Rollback: pause the test treasury, reconcile, and discard test state. Production remains legacy.

### M8 — Full testnet dress rehearsal and cutover package

Status: BLOCKED by M4, M5, and M7.

Dependencies: every parity item whose implementation is pre-G4 is implemented; each G4-only parity item has an executable signed testflight, deployment, or cutover package.

Invariants:

- PostgreSQL remains authoritative during rehearsal.
- No production/private secret is used in testnet.
- Rehearsal follows the exact production scripts with only environment/IDs changed.
- The rehearsal exercises only local/PocketIC SNS or the reviewed pre-production governance controller. It does not assert a mainnet SNS handoff or an NNS-controlled Chain Fusion integration, which remain G4-runbook steps.

Acceptance:

- Full snapshot plus delta import, read-only shadow, final freeze/drain, reconciliation, frontend switch, rollback, and forward-recovery are rehearsed and timed.
- The rehearsal reproduces the exact ZenDB source/dependency/Candid/Wasm pin, exercises lost-result logical-ID/version/hash reconciliation, drains every native intent, and proves bootstrap/importer/staging grants are absent and live collection grants exactly match the approved matrix before target writes are enabled.
- The rehearsal creates its capture publication/slot before the exported base snapshot, proves the continuous slot-consistent-point-to-barrier-LSN chain under decoder restart and WAL pressure, and proves direct CDC, redacted outbox, artifacts, and logs contain no secret/bearer/raw-token value.
- All parity entries reachable before G4 are verified or explicitly approved as changed/not-applicable. Each G4-only entry has a complete, independently reviewed runbook and is not reported as verified before its authorized testflight/deployment evidence exists.
- Independent security and migration review is clean. The review includes a production dependency/advisory inventory: unused wallet/browser adapters are removed, every remaining high/critical advisory has a documented exploitability decision and containment, and no unaccepted high/critical finding affects a shipped legacy rollback bundle or certified frontend asset.
- Module hashes, controller transitions, cycles/freezing thresholds, monitoring, backup/export, and incident contacts are in the machine-readable report.
- The report includes the selected SNS launch/ownership decision, validated local/PocketIC SNS evidence, and the isolated G4 mainnet-testflight manifest/recovery procedure; it does not treat either as a completed mainnet handoff.

Rollback: execute `docs/icp/ROLLBACK_PLAN.md` against testnet and prove legacy recovery.

### G4 — Testnet-to-production migration approval

Status: BLOCKED by M8.

### M9 — Production deployment, migration, and observation

Status: BLOCKED by G4.

Dependencies: G4 approval and named operators present.

Invariants:

- Follow the signed runbook; no ad-hoc correction or transfer.
- Legacy PostgreSQL stays intact/read-only through the rollback window.
- Asset movement is separately reconciled and manually authorized.
- Before any production canister, data, DNS, or custody action, execute the G4-approved non-custodial mainnet SNS testflight on isolated canister IDs with a test-only derivation/environment and only its approved cycle budget. Verify SNS-root-only control, proposal delay, pause/recovery, upgrade, monitoring, and the documented recovery/abort path. A testflight failure aborts before production deployment; its canisters never receive production data or custodial assets.

Acceptance:

- Source/destination counts/hashes, constraints, histories, and exact finances reconcile.
- Deployed modules/controllers/config/cycles and exact ZenDB source/dependency/Candid/Wasm/RBAC hashes match approved values; all intents are acknowledged or explicitly blocked before writes are enabled.
- The production base snapshot is the approved slot-exported snapshot; its capture chain, replica identities, redaction hashes, transaction roots, and final barrier LSN match the signed report before ICP writes are enabled.
- Production smoke, auth, certified frontend, timers, external integrations, and read-only financial tests pass.
- The mainnet SNS testflight report proves its isolation, cycle accounting, controller/recovery result, and absence of production data, production derivation paths, custodial assets, and payment authority before the production deployment proceeds.
- Observation window closes with no unresolved severity-1/2 issue; final export/backup and cutover report are archived.

Rollback: execute the phase-specific branch in `ROLLBACK_PLAN.md`. After any ICP-side financial write, rollback means pause plus reconcile/forward-repair or a verified reverse delta; it never means blindly enabling the old sender.

### M10 — Legacy retirement

Status: BLOCKED by M9 observation window.

Dependencies: production acceptance and rollback-window closure.

Acceptance:

- The legacy Node.js backend, REST bearer-token path, and their deploy dependencies are retired only after the rollback window closes. The retained React/Vite/TypeScript frontend remains a pinned, reproducibly built frontend-canister asset bundle; no Node.js runtime runs in the canister. Legacy wallet keys are revoked/retired and their dispositions audited.
- PostgreSQL/Fly data is retained or destroyed under the approved retention policy, with a verified immutable export.
- Old DNS, legacy OAuth callback registrations, cron jobs, deploy tokens, API secrets, and funded addresses are disabled. The approved certified-frontend OAuth redirect registrations remain only while their pinned provider capability, caller-binding, and credential-rotation evidence is current.
- ICP TODO is removed only now, and final parity/completion evidence is recorded.

Rollback: none after approved destruction; therefore destruction is the final, explicitly enumerated G4-runbook phase.

## Current risk register

| Risk | Severity | Required disposition |
| --- | --- | --- |
| External send may succeed before DB commit; stale execution resets can resend | Critical | Treat all ambiguous legacy payments as exceptions; never auto-retry during migration; target journal/reconcile-before-retry |
| Pending transaction hash includes an unstored current timestamp and lowercases case-sensitive addresses | Critical | New canonical operation IDs; preserve old hash only as historical input |
| Failed transaction and failed distribution can both remain payable | Critical | Independent obligation/attempt model and pre-cutover duplicate analysis |
| Non-EVM scoped addresses do not select the scoped signer | Critical | Per-scope Chain Fusion derivation/subaccount proof; reconcile displayed address to authority |
| KYC failure code intentionally consumes backlog claims | Critical policy conflict | Preserve claims as held liabilities until explicit policy at G2/G3; no silent forfeiture |
| Plaintext DB/process wallet secrets | Critical | Fingerprint only, rotate/retire, never import value; controlled legacy-to-unified-treasury transfer only after G4. Until a separately approved interim hardening proves otherwise, pause real-value legacy senders and prohibit automatic key generation. |
| SNS handoff/testflight is treated as a generic testnet controller or occurs before the permitted mainnet boundary | High governance/ordering | G3 records the human SNS launch/ownership decision and proves the interface locally/PocketIC; G4 alone authorizes an isolated non-custodial mainnet testflight with a bounded approved cycle budget and recovery/abort procedure before production deployment |
| Direct treasury donation memo is mistaken for authenticated donor, entitlement, or scope authority | High accounting/authorization | Scope comes only from the published account/address and an observation credits once by ledger/chain identity; memos are bounded untrusted metadata unless a separate authenticated proof is accepted |
| OAuth callback/code is treated as a Candid login or bearer authority | High authentication | M2 pins `identify` and binds each one-use OAuth state/PKCE/provider-subject exchange to a non-anonymous Candid caller; only the resulting caller-principal binding authorizes methods, with replay/swap/redirect/provider-response/issuer-where-present/subject-conflict tests and no token/code logging |
| Serialized legacy task user IDs leak/cross-match users | High | Parse imported history into a typed exact owner index, restrict unresolved rows, and never use legacy task data for live execution |
| Mutable OAuth handles are account keys | High | Reverify and bind immutable provider subject IDs |
| KYC callbacks lack durable event order/idempotency | High | Provider event journal, exact session/freshness, monotonic AML-first state |
| Raw KYC/identity evidence lacks a recorded legal basis, lifecycle, backup, access-audit, or public-disclosure policy | High privacy/compliance | G2 records purpose/data controller/legal basis, minimization, encryption/key access, retention/cryptographic erasure, backup/restore, access audit, accounting/anti-evasion exceptions, and per-field public disclosure/consent. Default user-profile/ban-voting views exclude raw user identifiers; treasury receiving configuration is separate. |
| Browser bearer storage or unsafe inline CSP remains reachable during the rollback window | High frontend/authentication | Target uses no browser-stored bearer and strict CSP. Any Internet-facing legacy rollback route requires a separately approved compatibility hardening and XSS/session-theft test; otherwise it is unavailable to public traffic. |
| Static admin/cron credentials and process-local limits obscure accountable authority or aggregate abuse control | High authorization/availability | Target uses named principal capabilities, append-only audit, stable principal/provider/action/cost quotas, and timers. IP controls, if needed, are enforced by an approved edge and never trusted from Candid callers. Rotate and constrain legacy shared secrets. |
| Unbounded or insecure external OAuth/KYC/RPC calls | High integration/security | Shared allowlisted HTTPS adapter with deadline, byte/schema bounds, redirect/credential transport policy, circuit/backoff, provider/action budget, and correlation ID; loopback HTTP only by explicit development configuration. |
| Production dependency advisories in wallet/browser stacks | High supply chain | Inventory actual use, remove unused adapters, upgrade only with compatibility tests, document containment for every remaining high/critical advisory, and block shipment of an unaccepted affected bundle. |
| Ban-voting/public projections correlate social identities with wallet addresses | Human privacy/product decision | G2 records whether each field is necessary, lawful/consented, and public; absent that decision, target projections expose connection/eligibility evidence rather than raw identifiers. |
| Task/cron locks are process-local and jobs are not durable | High | Replace cron locks with stable due cursors and idempotent completion receipts; direct bounded AI evaluations create no persisted task/lease queue and restart whole on interruption |
| AI compact migration tie-breaking was nondeterministic and successful raw data was nulled | High history risk | Export unmanaged exception table; build conflict report; preserve available source evidence and explicit missing markers |
| Tests can hard-delete configured DB financial rows | High operational | Add test-DB hard guard before database suites |
| Frontend lint is inoperative after ESLint 10 because no flat config exists | Medium operational | Resolved 2026-08-07 with the compatible ESLint 9 flat-config baseline; `npm run lint --workspace frontend` passes. Future lint-policy tightening must be a separately scoped legacy-frontend change. |
| ZenDB AGPL-3.0, young project, independent RBAC, generated document IDs in the currently reviewed API, and no in-place schema migration | High dependency | Treat the completed AGPL-3.0-only repository change as baseline; pin exact source/dependency/Candid/Wasm hashes; prove a logical-ID/hash reconciliation strategy and least-privilege collection grants; revoke bootstrap roles; use collection-vN migrations and durable mutation recovery; and retain independent canonical export. No future or unmerged PR is treated as available. |
| Base snapshot/delta gap, unordered trigger outbox, replica-identity loss, slot-WAL exhaustion, or sensitive logical-decoding output | Critical migration/security | At G2 prove the slot-exported-snapshot/consistent-point protocol, complete commit-order transaction application, replica identity, WAL capacity/abort thresholds, crash resume, and source-side redaction. Create production artifacts only at G4 before base export; a generic outbox is not an unproven fallback. Preserve the slot/artifacts through rollback and stop on any coverage, redaction, or retention failure. |
| Live cardinalities and row sizes unknown | High sizing | Read-only inventory before G2; no storage approval without evidence |
