#!/usr/bin/env node
// M1 synthetic capacity boundary for real fixed core identity/role adapters.
const path = require("node:path"), fs = require("node:fs"), crypto = require("node:crypto");
const { PocketIc, PocketIcServer } = require(path.resolve(__dirname, "../../node_modules/ic-mops/node_modules/pic-js-mops"));
const { IDL } = require(path.resolve(__dirname, "../../node_modules/ic-mops/node_modules/@icp-sdk/core/lib/cjs/candid/index.js"));
const { Principal } = require(path.resolve(__dirname, "../../node_modules/ic-mops/node_modules/@icp-sdk/core/lib/cjs/principal/index.js"));
const [bin, authorityWasm, coreWasm, reportPath] = process.argv.slice(2);
if (!bin || !authorityWasm || !coreWasm || !reportPath) throw new Error("Expected PocketIC binary, authority Wasm, core Wasm, and report path");
const Hash = IDL.Vec(IDL.Nat8), P = IDL.Principal, Factor = IDL.Variant({ internetIdentity: IDL.Null, oauth: IDL.Null });
const Config = IDL.Record({ core: P, workflow: P, treasury: P, archive: P, evidence: P, governance: P });
const Binding = IDL.Record({ logicalId: IDL.Text, desiredVersion: IDL.Nat64, contentHash: Hash, userId: IDL.Nat64, principal: P, factor: Factor, provider: IDL.Opt(IDL.Text), subjectHash: IDL.Opt(Hash) });
const Role = IDL.Record({ logicalId: IDL.Text, desiredVersion: IDL.Nat64, contentHash: Hash, principal: P, role: IDL.Text });
const Recovery = IDL.Variant({ acknowledge: IDL.Null, retryIdentical: IDL.Null, conflict: IDL.Null, blocked: IDL.Null });
const coreIdl = ({ IDL: C }) => C.Service({ writeThenLoseReply: C.Func([Binding], [], []), reconcileLostReply: C.Func([], [Recovery], []), writeRoleThenLoseReply: C.Func([Role], [], []), reconcileLostRoleReply: C.Func([], [Recovery], []) });
const Chunk = IDL.Record({ hash: Hash }), Upload = IDL.Record({ canister_id: P, chunk: Hash });
const Install = IDL.Record({ arg: Hash, chunk_hashes_list: IDL.Vec(Chunk), mode: IDL.Variant({ install: IDL.Null }), sender_canister_version: IDL.Opt(IDL.Nat64), store_canister: IDL.Opt(P), canister_id: P, target_canister: P, wasm_module_hash: Hash });
const management = Principal.fromText("aaaaa-aa");
function expect(v, tag, label) { if (Object.keys(v).length !== 1 || !(tag in v)) throw new Error(`${label}: expected ${tag}`); }
async function rejected(f, label) { try { await f(); } catch (_) { return; } throw new Error(`${label}: expected rejected ingress`); }
async function mgmt(pic, sender, method, type, value) { return pic.client.updateCall({ canisterId: management, sender, method, payload: new Uint8Array(IDL.encode([type], [value])) }); }
async function install(pic, sender, id, file, arg) { const wasm = fs.readFileSync(file), chunks = []; for (let i = 0; i < wasm.length; i += 1_000_000) { const chunk = new Uint8Array(wasm.subarray(i, Math.min(i + 1_000_000, wasm.length))); await mgmt(pic, sender, "upload_chunk", Upload, { canister_id: id, chunk }); chunks.push({ hash: new Uint8Array(crypto.createHash("sha256").update(chunk).digest()) }); } await mgmt(pic, sender, "install_chunked_code", Install, { arg: new Uint8Array(arg), chunk_hashes_list: chunks, mode: { install: null }, sender_canister_version: [], store_canister: [], canister_id: id, target_canister: id, wasm_module_hash: new Uint8Array(crypto.createHash("sha256").update(wasm).digest()) }); }
const hash = n => Uint8Array.from({ length: 32 }, (_, i) => (n + i) % 256);
async function scenario(pic, installer, operator, name, count) {
  const authorityId = await pic.createCanister({ sender: installer, controllers: [installer], cycles: 20_000_000_000_000n });
  const coreId = await pic.createCanister({ sender: installer, controllers: [installer], cycles: 20_000_000_000_000n });
  const config = { core: coreId, workflow: Principal.fromUint8Array(Uint8Array.of(9, 1)), treasury: Principal.fromUint8Array(Uint8Array.of(9, 2)), archive: Principal.fromUint8Array(Uint8Array.of(9, 3)), evidence: Principal.fromUint8Array(Uint8Array.of(9, 4)), governance: Principal.fromUint8Array(Uint8Array.of(9, 5)) };
  await install(pic, installer, authorityId, authorityWasm, IDL.encode([Config], [config])); await install(pic, installer, coreId, coreWasm, IDL.encode([P, P, P], [operator, authorityId, config.archive]));
  const before = { authority: await pic.getCyclesBalance(authorityId), core: await pic.getCyclesBalance(coreId) }, core = pic.createActor(coreIdl, coreId), subject = Principal.fromUint8Array(Uint8Array.of(8, 1)); core.setPrincipal(operator); let encodedInputBytes = 0;
  for (let n = 0; n < count; n += 1) { const binding = { logicalId: `principal-binding:v1:capacity:${name}:${n}`, desiredVersion: 1n, contentHash: hash(n), userId: BigInt(n + 1), principal: subject, factor: { internetIdentity: null }, provider: [], subjectHash: [] }; encodedInputBytes += IDL.encode([Binding], [binding]).byteLength; await rejected(() => core.writeThenLoseReply(binding), `${name} binding ${n}`); expect(await core.reconcileLostReply(), "acknowledge", `${name} binding ${n} recovery`); const role = { logicalId: `role-assignment:v1:capacity:${name}:${n}`, desiredVersion: 1n, contentHash: hash(n + count), principal: subject, role: "auditor" }; encodedInputBytes += IDL.encode([Role], [role]).byteLength; await rejected(() => core.writeRoleThenLoseReply(role), `${name} role ${n}`); expect(await core.reconcileLostRoleReply(), "acknowledge", `${name} role ${n} recovery`); }
  const invalid = { logicalId: "x".repeat(513), desiredVersion: 1n, contentHash: hash(255), userId: 255n, principal: subject, factor: { internetIdentity: null }, provider: [], subjectHash: [] };
  await rejected(() => core.writeThenLoseReply(invalid), `${name} over-limit binding is rejected before storage`);
  expect(await core.reconcileLostReply(), "blocked", `${name} over-limit binding creates no journal`);
  const after = { authority: await pic.getCyclesBalance(authorityId), core: await pic.getCyclesBalance(coreId) }; return { name, bindingWrites: count, roleWrites: count, lookups: count * 4, encodedInputBytes, authorityCycleDelta: (before.authority - after.authority).toString(), coreCycleDelta: (before.core - after.core).toString() };
}
async function main() { const server = await PocketIcServer.start({ binPath: bin, ttl: 60, showRuntimeLogs: false, showCanisterLogs: false }), pic = await PocketIc.create(server.getUrl()); try { const installer = Principal.fromUint8Array(Uint8Array.of(6, 41)), operator = Principal.fromUint8Array(Uint8Array.of(6, 42)), scenarios = [await scenario(pic, installer, operator, "expected", 16), await scenario(pic, installer, operator, "two_x", 32)]; const report = { schemaVersion: 1, component: "M1 fixed core identity/role adapter capacity proof", emulator: "PocketIC synthetic-only", scenarios, rejection: { logicalIdBytes: 513, result: "rejected before journal or storage in each scenario" }, limitations: ["Cycle deltas are emulator measurements, not a mainnet instruction or production-cycle budget.", "This covers only the fixed core identity/role adapters; other collection-specific capacity proofs remain required before G2."] }; fs.writeFileSync(reportPath, `${JSON.stringify(report, null, 2)}\n`, { mode: 0o600, flag: "wx" }); console.log(JSON.stringify(report)); } finally { await pic.tearDown(); await server.stop(); } }
main().catch(error => { console.error(error.stack || error.message); process.exitCode = 1; });
